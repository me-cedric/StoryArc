# Implementation status

What is built against what is specified, capability by capability. Written because the
change task lists track four changes and the specs describe seventeen capabilities, so
"how much of StoryArc exists" had no answer anywhere.

Keep this current. A status document that lags is worse than none, because it is believed.

## The count

The seventeen specs hold **367 scenarios** today, counted on 2026-09-06 from
`docs/openspec/specs/*/spec.md` by counting `#### Scenario:` headings, which is the only form
any of the seventeen files uses. This paragraph claimed **316** and had claimed it since
`5c0ed392` on 2026-08-30; the specs have grown by 51 scenarios since, and nothing re-counted
them.

**The rows below cover 295 of those 367.** Seventy-two scenarios have no verdict of any kind,
and they are named in the paragraph immediately above the verdict table rather than folded
into a "missing" count they have not earned. `reading-themes` and `page-transitions` were split out
of `ebook-reader` and `comic-reader` on 2026-08-30 when the reader-theming change was synced;
their 35 scenarios were read in their old homes, and the two new rows say so rather than
claiming a fresh audit.

The 295 that do carry a verdict were read against the app source on both platforms, scenario
by scenario, with `path:line` evidence — 268 of them over the audit rounds ending 2026-08-30
and the row edits after them, and 27 more on 2026-09-06 when `library-browsing` and
`native-experience` were re-audited. The earlier figure printed here was 266, which is the
same two-verdict discrepancy the last paragraph before the table describes; it is not a
second one.
That produces **298 verdicts**: three scenarios in `collections-and-reading-lists` carry two
independent clauses each, and averaging a half-built scenario is how a half-built scenario
reads as done, so those were scored twice. Where a verdict from an earlier round disagreed
with the code — reading aloud, Kavita server search, the five `sources` gaps, the two
`comic-reader` defects all landed after it was taken — the code won and the verdict was
re-checked by hand before it was written down here.

**These counts are summed from the rows below, and last re-derived on 2026-09-06 by a script that reads the rows rather than by eye. That is a change worth saying out loud.** They used to be maintained by hand and had stopped agreeing with the
rows: the header claimed 21 `one platform only` while the rows named 17, and claimed 137
`built and tested` where the rows held 142. Four hands editing rows in one day, each
adjusting the header from whatever base its worktree was cut at, is how that happens and
would have kept happening.

**Saying the header is derived did not make it derived, and that is the finding of this
pass.** The paragraph above claimed a re-derivation at 17:00 on 2026-08-31. It was not one.
The commit that wrote the claim (`84d7b61e`) left the table at 149 / 89 / 16 / 16 / 1, which
is what the rows held at `7e54f7f0` — four row edits earlier. `6c931e61` is the last commit
where the table and the rows actually agreed. Between it and 2026-09-06 the table stood still
while three rows moved under it: `library-browsing` turned one `missing` into `built and
tested`, `offline-downloads` turned two, and `kavita-server` closed its last one-platform
scenario. That is +4 `built and tested`, −3 `missing` and −1 `one platform only` the header
never took. This pass then moved two more rows, `library-browsing` and `native-experience`,
by re-auditing them. A derivation is a thing a tool does, so
this one was done by a throwaway script over the row text and is reported move by move in the
delivery note. The script was not kept: it is six lines of regular expression against prose
that will be rewritten, and a committed parser of prose is a second thing to keep true.

The rows now come to **372 verdicts against 367 scenarios**, and **every scenario in
every spec now carries one**. The audit backlog is closed. It was 69 scenarios deep on
2026-09-06 in the morning: `reading-themes` and `page-transitions` had never been audited in
their own right, and `ebook-reader`, `settings-and-about`, `publication-formats` and
`comic-reader` each counted fewer scenarios than their spec holds. Six capabilities were
read scenario by scenario that day, every citation was checked against the line it names,
and a verdict that could not be evidenced was moved down rather than kept.

**The five extra verdicts are clauses scored twice, on purpose.** Three scenarios in
`collections-and-reading-lists` carry two independent halves. *Choosing a background* in
`reading-themes` gained a second on 2026-09-06: its derivation is built and tested, and its
preview-before-apply half is missing on both. *Scanning a folder* in `local-library` gained
one the same day, for the same reason. Averaging a half-built scenario is how a half-built
scenario reads as done.

**Six capabilities were re-audited on 2026-09-06 because their rows predated a summer of
code churn.** `collections-and-reading-lists` proved the risk: it called a delete
confirmation and a cover picker missing, and both had shipped on 2026-08-31 in `63917c7f`.
`sources`, `network-share`, `kavita-server`, `opds-catalog` and `local-library` were re-read
against the source that day, every citation checked against the line it names.

| Verdict | Count | Means |
| --- | --- | --- |
| built and tested | 220 | code on both platforms, and a test on each side asserts it |
| built, asserted by nothing | 120 | code on both platforms, and at least one side has no test asserting it. **Widened on 2026-09-06.** The cell used to read "no test anywhere touches it". That stopped being true when `library-browsing`'s *An ordering says that it is an ordering* entered the bucket with four Android language tests and nothing on iOS |
| one platform only | 22 | code on one side; on the other it is absent, or declared and never reached |
| missing | 10 | no running implementation on either platform |

**At least seven of the twenty-two `one platform only` are not gaps.** The `local-library` spec
splits *Adding a folder* and *Remembering an opened file* per platform by construction, so
each consumes a slot while being complete on both sides — four slots. The
`native-experience` re-audit of 2026-09-06 adds two more of the same kind: *iOS reader
chrome* and *Android reader chrome* each name one platform in the scenario itself. The
remaining fifteen are not all divergences either, and this stops short of claiming a number
for them: the three `one platform only` inside `native-experience`'s carried 28 were scored
before this file recorded which scenario each slot belonged to, so they cannot be sorted into
gaps and non-gaps without re-reading them. "Where the two platforms differ" below names the
divergences it knows about and carries its own count, which was taken separately and is not
derived from this table.

**None of those numbers says anyone watched the behaviour happen.** That is a third axis,
and it is per-row: a great deal of what landed in the last few rounds compiles, lints,
passes its unit tests and has been seen by nobody, because the Simulator control this repo
uses has been down for most of them and an emulator was not always available. Every row
below says which of its parts were driven, on what, and which were not. Where a row says
"not driven", read it literally.

A capability is **built** when every scenario has code on both platforms, **partial** when
some do, **absent** when none do. All seventeen are `partial`. Two rows were wrongly marked
`built` before this audit and are not any more; nothing here is marked `built` on optimism.

| Capability | State | Where it stands |
| --- | --- | --- |
| `publication-formats` | partial | 24 scenarios: 20 built and tested on both, 2 built and asserted by nothing, 2 one-platform. **A publication's publisher, description, series and series index are read out of an EPUB, on both platforms.** The series is stated two ways and both are read: EPUB 3 defines `belongs-to-collection` refined by `group-position`, EPUB 2 defines nothing at all, and Calibre's `calibre:series` filled that gap so widely that a reader's library is mostly that. A file carrying both and disagreeing is a file whose publisher knew better than its converter, so the defined form wins -- and `series.epub` in the corpus carries both, disagreeing on purpose, so a test can see which. **It also fixed a defect worth naming**: an EPUB's `series` was its own *title*, so every book was a series of one named after itself and `reading-themes`, which scopes a theme to the series, could not let two volumes of one work share a setting. **The eight scenarios the format-scope merge added were audited on 2026-09-06, and five of them hold on both platforms.** Two more were scored down when the verdicts were re-checked against the code, and both re-checks are written out below. *Refusal is named, and each kind of refusal is its own kind.* A 7-Zip is refused as `CB7` rather than as a broken file (`PublicationIndexer.swift:102`, `PublicationIndexer.kt:118`), and the dialog that carries the refusal names the container and lists what the app does read (`RefusedFile.swift:41`, `app/src/main/res/values/strings.xml:5`). A solid RAR4 is refused before a page is drawn, because only the first entry of a solid archive is readable (`ComicArchive.swift:291`, `ComicArchive.kt:361`), and the publication is still listed and marked unopenable rather than dropped (`PublicationIndexer+Building.swift:52`, `PublicationIndexer.kt:385`). A container signature with nothing behind it is *unreadable*, not *unsupported*, and a test a side writes eight bytes of RAR signature to prove it (`ComicArchiveTests.swift:204`, `ComicArchiveTest.kt:221`). **That last scenario is scored down to *built, asserted by nothing*, because what the reader is told is wrong on both platforms and no test reads it.** The model keeps damaged apart from unsupported; the sentence does not. An unreadable file becomes `OpenedFile.Outcome.unreadable` (`OpenedFile.swift:55`, `OpenedFile.kt:104`), and the refusal dialog draws the message (`RefusedFile.swift:44`, `RefusedFileDialog.kt:23`), which says the file is not one StoryArc recognises and then lists the formats it reads. That is the format sentence the scenario forbids for a truncated download, and neither platform ever says *damaged*. The wording is not shared either: iOS interpolates its own list (`RefusedFile.swift:29`) and reads "CBZ, CBR, CBT, EPUB, PDF, M4B and other audiobooks", where Android's resource reads "CBZ, CBR, CBT, EPUB, PDF and audiobooks" (`app/src/main/res/values/strings.xml:6`) -- so a fix found by searching for one sentence misses the other platform. The two tests named above assert the layer below the reader and stop there: `ComicArchiveTests.swift:204` and `ComicArchiveTest.kt:221` prove the archive throws `Unreadable` rather than `UnsupportedContainer`, and no test on either platform reads the message. **Streaming is decided once, in one place, and both apps read the same rule.** `StreamingOffer.of` is four lines a side and answers open, download or refuse from the capability, from whether the bytes are local, and from whether the decoder wants a file of its own (`StreamingOffer.swift:61`, `StreamingOffer.kt:71`). The share browser is the caller a reader reaches: a tap goes to `offerOrOpen` (`SmbBrowserView.swift:216`, `SmbBrowserScreen.kt:119`) and the answer to a download offer goes to `openWhatArrived` (`SmbBrowserView.swift:248`, `SmbBrowserScreen.kt:134`). A CBZ on a share is read where it lies, a solid archive is offered with the size the share stated, and one already on the device opens with no notice -- asserted case for case by `StreamingOfferTests.swift` and `ShareOpeningTests.swift` against `StreamingOfferTest.kt` and `ShareOpeningTest.kt`, over publications the tests write rather than publications the indexer produced. The index is ranged reads and the test says so structurally: a corpus archive opens over a fake server in eight reads or fewer on each side (`HttpSourceTests.swift:313`, `HttpSourceTest.kt:349`). **The first of the two one-platform scenarios is *A remote publication is catalogued without being transferred*.** Android indexes a CBR straight from a share's headers -- page count, page order, sizes, cover and streaming state, with no entry decompressed (`PublicationIndexer.kt:115` into `ComicArchive.kt:346`, `PublicationIndexer.kt:445`). iOS never arrives: its `.rar` branch returns a bare record unless a caller supplies `decoderPath` (`PublicationIndexer.swift:204`), and no production caller supplies one, so a remote CBR is catalogued with no page count, no cover and `streaming: .refused`. The header reader that would answer exists and is tested -- `RarComicArchive` at `ComicArchive.swift:258` -- and nothing calls it while the bytes are remote. **The corpus does not prove the CBR case it is most asked about.** `rar4-compressed.cbr` and `rar5-compressed.cbr` carry `test.txt` and `test.bin`, not pages, and every RAR in the corpus that does carry pages stores them uncompressed, so no fixture exercises a compressed CBR of images. **The second one-platform scenario hides behind that gap: *Opening a solid archive from a remote source*.** A solid RAR5 is the download-only case that scenario exists for, and on Android the offer is never made. `isReadableWhenLocal` is true for a solid RAR5 (`RarReader.kt:92`), so the archive is not refused as solid. The share hands the indexer a source and no decoder path (`SmbBrowserScreen.kt:338` into `PublicationIndexer.kt:218`), so `canDecode` is false (`ComicArchive.kt:374`), every compressed page entry is skipped, and `RarComicArchive.open` throws `UnsupportedContainer` (`ComicArchive.kt:388`). The indexer turns that into `Unsupported` (`PublicationIndexer.kt:237`) and `offerOrOpen` reports an unexpected failure instead of offering the download (`ShareOpening.kt:79`). iOS reaches the offer for the same file, because its `.rar` branch returns a bare record without opening the archive (`PublicationIndexer.swift:204`) and `needsLocalFile` decides the offer before the container is asked (`ShareOpening.swift:41`). **No test catches this, because every test stubs the indexer out**: `ShareOpeningTest.kt:38` and `ShareOpeningTests.swift:48` hand `offerOrOpen` a publication the test wrote itself, and `StreamingOfferTest.kt` drives the pure `StreamingOffer.of`, so nothing runs a RAR through the share path on either platform. The same Android throw reaches any compressed CBR on a share, so a reader meets an unexpected-failure notice where a download offer belongs. The corpus also holds no plain folder of ordered images, which is a listed format: `ImageFolderArchive` is exercised only against folders the tests build themselves. **CB7 is deferred work, not an open question.** The direction was settled on 2026-08-30 -- the format should open rather than be refused for ever -- and the build was then deferred at its first step, which is vendoring: libarchive is already in `third_party/`, but the 7-Zip reader and LZMA are not among its 26 sources, so starting means taking on xz-utils as a second upstream, and xz is the project that shipped CVE-2024-3094 in its release tarballs. [ADR-0013](../decisions/0013-cb7-support.md) records the decision, the deferral and the vendoring constraints for whoever picks it up. Until then the named refusal stands and is covered by `comics/refused.cb7`, so nothing regresses and no promise is made that is not kept. This row should keep saying so rather than tidying it away. |
| `comic-reader` | partial | 30 scenarios: 19 built and tested on both, 10 built and asserted by nothing, 1 one-platform. **Five scenarios this row had never read were audited on 2026-09-06, and four of them hold on both platforms.** *Scroll axis for webtoons* is one mirrored function -- vertical when a page is reflowable or at least twice as tall as it is wide, horizontal otherwise (`TransitionChoices.swift:21`, `TransitionChoices.kt:39`) -- measured from the first decoded page in the reader (`ReaderModel.swift:143`, `ReaderViewModel.kt:156`), overridden into the shelf settings (`ShelfMemory.swift:174`, `ReaderViewModel.kt:187`; iOS keeps a `settingScrollAxis` helper that writes the axis and the transition together, Android writes the same two fields at the call site, and `ShelfMemory.kt:64` only declares the stored `scrollAxis`), and asserted case for case (`ShelfMemoryTests.swift:173`, `ReadingThemeTest.kt:457`). *Everything else is in the menu, and labelled* is built as five doors over eleven icons (`ReaderMenu.swift:32`, `ReaderMenuSheet.kt:119`), opened by the chrome's own menu button (`ReaderChrome.swift:62` into `ReaderView.swift:364`, and `ReaderScreen.kt:1006`); a test a side reads the menu's own source and the four language files, and checks that every icon's job survived and that every row is named in words (`ReaderMenuTests.swift:198`, `ReaderMenuTest.kt:93`). *Fewer controls is not fewer ways in* has a test of the same shape, naming each surviving gesture and the file it lives in (`ReaderGestureTests.swift:118`, `ReaderGestureTest.kt:73`). *Where the reader is, at a glance* draws the coarse position behind the contents row and states it in words on the row, with the fill cleared of semantics so the page number is not announced twice (`ReaderMenuProgress.swift:22`, `ReaderMenuSheet.kt:238`; `ReaderProgressTests.swift:118`, `ReaderProgressTest.kt:78`). **The fifth is asserted by nothing**: *Curl over image pages* asks the curl to take the already-decoded page rather than a re-raster, and both readers do -- `CurledPages` takes a `CGImage` and a `Bitmap` (`ReaderContainers.swift:21`, `ReaderScreen.kt:873`) -- but no test names it, so a container that re-rendered the page would pass every gate. **The count moved a second time, and the reason is worth writing down.** Three of the twenty-eight verdicts this row carried belong to scenarios that left for `page-transitions` in `2e654387` -- *Page curl follows the finger*, *Page curl is interruptible* and *Reduce Motion* -- so they are retired from this tally rather than counted in two rows. Twenty-five inherited verdicts and five new ones make the thirty above. The curl paragraphs below are kept because the code is still in this reader, not because the scenarios are still in this spec. **The one-platform verdict was re-checked and still holds**: `AppSettings.turnPagesWithVolumeButtons` is declared and persisted on iOS (`AppSettings.swift:30`) and read by nothing outside its own tests, while Android maps the two keys in `MainActivity.kt:74`. **Image adjustments are built on both, the whole requirement**: brightness, contrast, inversion and greyscale, live and per series; sharpness is on iOS and on Android from API 33, where a runtime shader exists. Border trimming detects a uniform white or black margin per page and can be turned off for a page it gets wrong; the detection is covered by eight tests a side, and it refuses a gradient, a mid-grey band and a blank page. The sliders and switches were driven end to end on both, on a simulator and on an emulator; trimming itself has only been run against synthetic pages, because the fixture corpus is gradients and has no margins to trim. **Reading direction can be overruled and the reader's orientation locked, on both platforms.** A direction chosen while reading outranks the publication's metadata for that series alone, is remembered beside the theme and the transition, and applies without losing the page: the run of pages reverses and the reader stays on the one they were reading. The lock holds the app at the way up the reader is already at and hands the rest of the app back to the device on the way out; it is not remembered, because the spec scopes it to the reader. Neither has been driven on a device. The direction mapping and the per-series store are unit-tested on both platforms, and both controls were seen rendered in the reader on a simulator, but nothing here can rotate a simulator or an emulator, so the lock has never been watched holding. **A page opened on a cold launch straight into the reader arrives fitted.** It did not before: iOS sized the page against a scroll view UIKit had not laid out yet, recorded that fit as applied, and never tried again, so the page sat at its own pixel size in a corner until something else changed the page, the mode or the size. A fit is now recorded only once there is a laid-out view to apply it to, and the scroll view's own layout applies the one still owed. Driven both ways on the Simulator from the same cold launch -- unfitted before the change, fitted after it, and the settled screenshots byte-identical, so nothing else moved. Android needed no change and got none: its fit is keyed on the size `onSizeChanged` reports, which is only ever a size the layout really has, so a fit taken before the first measurement is discarded rather than recorded. That rule is pinned by a unit test on both sides. **The last five moved too**: the end of a volume offers the previous and the next, the slider shows the page it is heading for rather than moving under the finger, landscape pairs facing pages and can shift the pairing for a cover that throws the count off, a continuous scroll can draw a line where one page ends, and the prefetch gives way under memory pressure. Read on a simulator and an emulator; the pairing in landscape wants a device to be believed. **The two defects a scenario-by-scenario audit named are fixed on both platforms.** The page curl is interruptible: both readers used to stop the running settle and then compute the new drag from zero, so a settle halted at 0.8 jumped to 0.1 -- the snap the scenario exists to forbid. The drag is now an offset from where the page stands, which also lets a caught turn be pushed back rather than pinned at zero. The arithmetic moved out of the gesture handlers into `CurlTurn`, a pure type on each side asserted case for case by twelve tests apiece, because eleven of the twenty-eight scenarios this capability then held live in a view and that is why the defect survived. iOS needed one extra piece to get there: `withAnimation` moves its state to the destination at once and interpolates only what is drawn, so where the page *stands* is recovered through an `Animatable` view, and a settle a later drag took over no longer runs its completion and turns the page under a finger. **The fit choice is per series.** It was one key for the whole library, so fit-to-width chosen for a manga changed how every other comic opened; it now sits in `ShelfSettings` beside the six per-series reader choices that were already there, and Android's comment explaining the global storage is gone with the storage. The old global value is folded once into the fixed-layout default, so a reader who had chosen fit-to-width keeps opening at fit-to-width instead of finding the whole library back at fit-to-screen. Neither fix has been driven on a device: catching a settle needs a second touch inside a 300 ms window and neither `adb` nor the simulator will deliver one. **The system bars now go away with the chrome on both platforms.** Android drew edge to edge and never hid or dimmed anything, so a clock, a battery icon and the signal bars sat over every page of every reading session; the window's insets controller now follows the same `isChromeVisible` the reading controls follow, with `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`, so a swipe from an edge draws them back over the page without moving it and the centre tap that reveals the chrome brings the clock back for good. They are handed back on the way out, on resume, and on a configuration change -- a reader who cannot get their clock back would be the worse defect. The first of the three deviations at the end of this row still applies: the chrome is visible on open, so the bars are too, and both go four seconds later. Not driven on an emulator by the agent that wrote it. **One scenario is left one-platform.** Volume-button page turns are Android only -- deliberate, and iOS says so in Settings, but `AppSettings.turnPagesWithVolumeButtons` exists on iOS, is unit-tested there, and drives nothing. **Ten scenarios have no test on either platform, and they are all the ones that live in a view**: the curl handed an image page, the chrome timeout, the edge zones, the system bars that follow it, the 400 ms spinner, the thumbnail strip, the orientation lock, the stitching and its separator. What *is* asserted a side is the prefetch table, the spread layout, the page-return rules, border cropping, the fit arithmetic, the transition choices and the direction inference. **Three deviations belong to the spec rather than the code, and are recorded here so nobody rediscovers them**: the chrome is *visible* on open on both platforms, not hidden as the scenario says; the page slider is deliberately not mirrored under right-to-left on both; and chapter navigation only ever means the neighbouring publication, because no publication's internal chapter markers are read anywhere |
| `ebook-reader` | partial | **39 scenarios, and all of them are now audited.** 30 built and tested on both, 8 built and asserted by nothing, 1 missing on both. The thirteen the previous row did not cover were read against the source on 2026-09-06: the six the reader-and-search merge `07e45e97` added, the six reading-aloud scenarios, and the one added when WebView egress was denied. **The previous row's account of its own gap does not add up, and this is the reading that does.** It named seven unaudited scenarios as "the five reading-aloud scenarios, the PDF scenarios synced out of the `format-scope-and-libraries` change, and the one added when WebView egress was denied" -- but that change carried seven PDF scenarios, so the list it describes is thirteen, not seven. Six reading-aloud scenarios plus the egress one is exactly seven. The seven PDF scenarios are therefore taken as audited in the earlier round and are carried forward unchanged; they were not re-read here, and anyone who needs them proved should read them rather than trust this sentence. **One of the two `missing on both` is closed.** *Progress display* asks for "how much of the current chapter is left, in words", and the earlier audit's repo-wide grep found nothing on either platform. It is built on both now, as one shared rule rather than as two renderings: `ReadingPositionLine` and `ChapterRemainder` at `StoryArcCore/ReadingPositionLine.swift:21` and `:core:model`'s `ReadingPositionLine.kt:28`, five coarse bands measured on what is *left* rather than on what is read, and no page-number field in the type at all -- which is how *A publication that declares no chapters* forbids the page-count fallback by construction instead of by a branch. *Moving somewhere else in a reflowable publication* rides on the same rule and is built and tested on both: the contents entry is the first row of the reader's menu (`EpubReaderMenu.swift:96`, `EpubMenuSheet.kt:90`), the menu is asserted to reach it on both sides (`ReaderMenuTests.swift:149`, `ReaderMenuTest.kt:53`), and the slider that would have been the other route is asserted absent on both (`ReaderProgressTests.swift:76`, `ReaderProgressTest.kt:52`). Both are reached from a reader's action. The navigator's own locator stream writes `withinChapter` at `EpubReaderOpening.swift:252` and `EpubReaderViewModel.kt:453`; the reader's menu reads it into the contents row at `EpubReaderProgress.swift:20` and `EpubMenuSheet.kt:91`. Tested on both, case for case: `ReadingPositionLineTests.swift` and `ReadingPositionLineTest.kt:37` name the no-chapter case, the blank-title case and the five bands. **The other is still missing, and half of it moved.** *Theme follows appearance* now has the opt-in setting it lacked -- `settings.linkReadingThemeToAppearance` gates `LinkedPreset.swift:20` and `ReaderAppearance.kt:57` -- so "with that setting off the reading theme is untouched" holds on both. The two clauses that carry the scenario do not. The pair is still a fixed Paper and Quiet rather than "the light and dark reading themes the reader chose as their pair" (`AppearanceMode.swift:50`, `AppearanceMode.kt:61`), and the value is still resolved once when the book opens: `EpubReaderView.swift:72` builds the model inside `State(initialValue:)`, and `EpubReaderActivity.kt:255` reads it through `by lazy`. A device that switches to dark mid-book therefore moves the chrome and leaves the page where it was, which is the opposite of "then and there rather than at the next open". Android's own note at `EpubReaderActivity.kt:228` argues that nothing can change the answer while the activity lives; the device's night mode can, and that is exactly the case the scenario names. **The theme surface's two levels are built on both, and three of its four scenarios are tested on both.** Level one draws the live specimen, then the preset grid, then one full-width action that opens level two: `ThemeSheet.swift:39`, `:45` and `:46` on iOS, `ThemeSheet.kt:126`, `:140` and `:178` on Android. No axis control is among them, which is the clause the tests pin. Level two is a second sheet on iOS (`ThemeAxesSheet.swift:44`) and a destination with its own bar on Android (`ThemeAxesScreen.kt:108`), both over `ThemePreview` fed by the publication's own words. The reset names the preset it restores and is absent when nothing was changed: `ThemeAxesSheet.swift:277` and `ThemeAxesScreen.kt:208`. Both levels scroll and no axis label is line-limited, which is what *Both levels at the largest text size* can be checked for without a screenshot, and both suites check it symmetrically (`ThemeSheetTests.swift:272`, `ThemeSheetTest.kt:300`). The menu reaches all of it: `EpubReaderMenu.swift:47` and `EpubReaderActivity.kt:589`. **The fourth scenario is *The theme surface opens on the presets*, and it drops to built and asserted by nothing.** Its third clause is built on both. `ThemeSheet.swift:156` calls `model.adopt(preset)` and `:160` calls `dismiss()`; Android puts the same pair in the host that owns the sheet, at `EpubReaderActivity.kt:524` and `:527`. Only iOS asserts it: `ThemeSheetTests.swift:115` requires `model.adopt(preset)` and `:119` requires `dismiss()`. `ThemeSheetTest.kt` quotes the clause at `:15` in a class comment and asserts nothing about it. Its `code(name)` helper at `:42` reads whichever file a test names, and every call in the file names `ThemeSheet.kt` or `ThemeAxesScreen.kt`, so it never reads `EpubReaderActivity.kt`. A grep of the whole Android test tree finds `isShowingTheme` nowhere and `onAdopt` only as the empty stub at `ThemeSheetSemanticsTest.kt:55`. Android could therefore drop `isShowingTheme = false`, leave the sheet up over the reader's own choice, and pass every gate. That is the same shape that put *Background and lock screen* and *Reaching the end of a chapter* in the same column. **Read the four theme tests for what they are.** `ThemeSheetTests.swift` and `ThemeSheetTest.kt` read the reader's own source text and assert on it. They are symmetric on the two clauses they share, they are in the gated suites, and they are tripwires rather than proofs -- they say level one declares no slider; they never say a slider failed to appear. **The spec disagrees with itself on this surface, and the code follows the newer half.** *One sheet, two depths* says level one holds "the six presets, the font-size stepper, the page-mode control and brightness"; *The theme surface opens on the presets* says "the six preset swatches are what is shown, with no axis control among them". Both platforms draw the second, and both test suites pin the second by asserting that level one declares no axis control. They spell the axes differently, so grep for the wrong half and the symmetry looks broken: Android names `FontSizeControl` and `BrightnessControl` at `ThemeSheetTest.kt:63` and `:67`, and iOS names `private var fontSize` and `private var brightness` at `ThemeSheetTests.swift:64` and `:68`. That is a defect in the spec, not in the app, and scoring *One sheet, two depths* from the spec alone will get it wrong. **Reading aloud is built on both and four of its six scenarios are asserted by nothing.** What is asserted is the shared session. *Something else takes the audio* is built and tested on both: the pause table, the resume the platform allows, the listener's own pause that an interruption may never undo, and audio taken for good, at `PlayerInterruptionTests.swift:30` through `:112` over both source kinds and at `PlaybackSessionTest.kt:421` through `:493` with `ReadAloudSessionTest.kt:44` pinning that the voice drives that same session. *Where the listening got to* is built and tested on both -- the sentence locator is what reaches the progress store, at `ReadAloudCentre.swift:183` and `ReadAloudHost.kt:341`, asserted by `ReachedPositionTests.swift:24` and `ReadAloudSessionTest.kt:109` in the same three cases. The other four are built on both and asserted on one side or on neither. *Starting playback* begins at the reader's own locator (`EpubReadAloud.swift:53`, `EpubReaderActivity.kt:921`) and draws the sentence back into the reader by two differently named mechanisms. iOS holds a weak `follower: EpubReaderModel` at `ReadAloudCentre.swift:54` and feeds it from the `onSentence` closure at `:91`. Android declares a `SpokenSentenceFollower` interface at `ReadAloudHost.kt:28`, holds it weakly at `:157`, and feeds it from `sentenceSpoken` at `:321`. The type name is Android's only; do not look for it on iOS. Only Android asserts the scenario, at `ReadAloudHostTest.kt:106`. *Background and lock screen* is built twice over -- `UIBackgroundModes` at `project.yml:96`, the now-playing card at `NowPlaying.swift:45`, and its sentence-skip commands enabled at `NowPlaying.swift:172` and bound at `:201`, and a `mediaPlayback` foreground service holding a `MediaSession` at `ReadAloudService.kt:109` -- and the only part any test touches is the second line, `SpokenLabel`, at `PlaybackTransitionTests.swift:105` and `ReadAloudSessionTest.kt:79`. Nothing asserts that the transport carries play, pause and sentence skip, and nothing asserts that the voice survives backgrounding. *Reaching the end of a chapter* likewise: the end of the publication takes the surface and the media controls away, asserted at `PlayerInterruptionTests.swift:218` and `PlaybackSessionTest.kt:283`, but the headline clause -- the walk crossing a resource boundary without being asked -- is asserted nowhere, on either platform. It is real: Readium's own content iterator does it on iOS, and `SpokenSentences.kt:91` says the same of Android's. *A publication with nothing to say* is a bare absence on both (`EpubReaderMenu.swift:123`, `EpubMenuSheet.kt:154`, gated on `SpokenSentences.isSpeakable`) and no test names it. **Egress is built and tested on both. The iOS test runs in CI on every pull request; the Android test runs nowhere.** iOS compiles a `WKContentRuleList` and installs it on the navigator's own content controller (`PublicationEgress.swift:85`, called from `EpubReaderOpening.swift:278`); Android sets `blockNetworkLoads` and wraps Readium's `WebViewClient` to add `connect-src 'none'`, installed from the fragment-view callback so no page can be created without it (`PublicationEgress.kt:83`, called from `EpubReaderActivity.kt:340`). `PublicationEgressTests.swift` fires every egress vector at a real web view and `PublicationEgressTest.kt` does the same on a device. **`pnpm check` runs neither, and an earlier draft of this row read that as proof that neither runs anywhere. Read `.github/workflows/` before repeating it.** `.github/workflows/ios.yml:32` declares an `epub` job, named at `:33`, whose step at `:43` runs `xcodebuild test -scheme StoryArcEpub` on a simulator, on every push to `main` (`:4`) and on every pull request touching `apps/ios/**` (`:7`). All eight suites under `apps/ios/Packages/StoryArcEpub/Tests/EpubReaderFeatureTests` therefore run in CI, the egress vectors among them. The directory holds nine files, not nine suites: `EgressHarness.swift` declares no `@Test` and is the harness `PublicationEgressTests` shares. Their absence from `pnpm check` is deliberate and written down -- `apps/ios/README.md:146` says `test:ios:epub` and `build:ios` are "left out of it deliberately" because both want a simulator runtime, and `.husky/pre-push:12` says the iOS and Android gates run in CI. **The hole is on Android, and it is the one this row used to call the milder case.** `.github/workflows/android.yml:86` runs `:core:format:connectedDebugAndroidTest` and nothing else, and `:58` gates that job to pushes on `main`. `apps/android/feature/epubreader/src/androidTest/kotlin/app/storyarc/feature/epubreader/PublicationEgressTest.kt` is therefore compiled and never executed, on any machine. `pnpm test:android` at `package.json:39` runs unit tests only, and `pnpm build:android:tests` at `:40` assembles without running. The compile was checked by hand on 2026-09-06 and reported `BUILD SUCCESSFUL in 13s`, so the suite is live rather than dead the way the instrumented `ProgressStoreTest` was. It is a live test that no gate runs. `test:ios:epub` at `package.json:38` is the iOS script `pnpm check` at `:44` omits; wiring it in would duplicate a job CI already requires, so it is not the work to schedule here. **Nothing in this round was driven.** No simulator or emulator was used; every verdict above comes from the source, from the two workflow files, and from the one compile check named. |
| `reading-progress` | partial | 17 scenarios: 8 built and tested on both, 9 built and asserted by nothing. This is a good local store and a good merge table sitting on top of two wires nobody connected. **The reflowable progress badge stuck, and that is fixed.** On a two-chapter EPUB it climbed through chapter one and then read "50% read" on every page of chapter two, including the last page of the book. Placing a resource in the reading order was an exact string match on the locator's href, and a locator that came from a link carries the fragment the link pointed at while one from a search can carry a query -- neither is part of the resource's identity and neither matched, so the resource could not be placed at all. The lookup now lives in `TotalProgression` beside the arithmetic it feeds, ignores the fragment and the query on both sides of the comparison, and is pinned by six assertions a side including the last page of the last resource reading 100%. It cost more than a badge: the position is stored from the same number, so a wrong progression was a wrong resume point and a wrong "finished" flag. **Not confirmed on a device** -- the arithmetic and the matching are tested, but which of the two the emulator was hitting was never established. **Synchronisation pulls as well as pushes, on both platforms.** ADR-0006's table was written on both platforms, tested on both, and called by nothing -- `KavitaSync` only ever pushed, so a chapter read on a phone was invisible to a tablet until the tablet read it too. `ProgressPull` is the caller: it sorts each merge into records to write, records the server is behind on, and the conflicts that earn a sentence. Finding *which* publication a server's chapter is was the part that had to be right -- a server reports against its own chapter id and the local store keys on a publication identity, so this adds the inverse of the link Kavita already remembered and a lookup by stable id. A chapter this device never opened is skipped rather than adopted. The conflict notice is once, naming how many, and offers to take the other. Eight assertions a side on the merge itself. **And now it pushes what the pull found.** `ProgressPull.toPush` was computed and dropped on the floor -- `KavitaSync` consumed `toSave` and `conflicts` and nothing else -- so a chapter finished here stayed here until the other device read it too. The merge is sorted by `KavitaExchange`, a pure type in the Kavita layer with seventeen assertions a side, into what to write and what the server is owed; `KavitaSync.pull` queues the owed positions through `flush`, the one push path a failed report already uses, so a server that is not there leaves them queued rather than surfacing a failure. **Which makes ADR-0006's first rule reachable at last.** `syncedPosition` is written on every record whose position the server has actually taken -- and only then, because a position it never received is not synchronised -- so a remote that is merely ahead is adopted quietly instead of raising the alert. That needed one thing more than the write: positions compare by fraction now, because a store is entitled to keep a synced position as the one number that survives a change of typography and `ProgressStore.kt:320` does exactly that, so case equality could never match what it had just stored. A conflict the local position wins is pushed as well, which is what makes "told once" true -- unpushed, the next refresh found the same disagreement and raised the same notice. **The round trip has been driven against the mock, and the mock has a self-test of its own.** `scripts/kavita-server.mjs --self-test` starts a server on a port of its own and drives thirteen checks over HTTP -- a wrong key refused, an unauthenticated request refused, a `pageNum` of 3 reading back as four pages read and four pages read reading back as page 3, a finished chapter handing the continue point on, a mark and an unmark -- and `pnpm lint` runs it. The client's half of those same numbers is asserted on both platforms too: what `report` and `mark` post, and that the key travels as a bearer header and never in a URL. **Still nothing against a live server.** `KavitaSync` has its first assertions now, three a side, and they are about the one thing that decides whether a pull does anything at all: which local record a server's chapter is. The browser's chapter table holds the publication id a chapter was *opened* as, and that is not the id the record was written under once the same chapter has been reached two ways -- a pull then found nothing and adopted nothing. It looks the record up by the server identifier first and falls back to the stable id, and the first case fails without the lookup on both platforms. The rest of `KavitaSync` -- the sending, the flush, the settle -- is still asserted by nothing; what is proven beside it is the table, the sorting and the two writes underneath them. **The finished lifecycle is built on both**: a completion timestamp stamped once and never restamped -- reopening a finished publication writes a new position, not a new completion -- a finished publication that reopens at the beginning while keeping the record, and "Start from the beginning", confirmed, offered only where there is a position to clear. The restart was driven on an emulator. Room went to version 2 with a written migration, never a destructive fallback. Manual mark-read is built too: `AddToShelfMenu` and `AddToShelfSheet` carry it for both local publications and Kavita chapters, reaching `ProgressStore.mark` and `KavitaSync.mark`. **A publication now says what it is, not only where it is.** Every library publication used to be keyed by its path alone, so renaming or moving a file lost the reader's place -- data loss with no warning, in the one store this app promises never to lose. `contentDigest` had existed on both platforms the whole time and nothing called it: dead code on iOS, and on Android reached only for a file handed over from outside the app. It is taken now from the handle the indexer already opened to sniff the format, so both reads land on pages it is touching anyway and nothing is opened twice -- which covers every route into a publication on both platforms, a folder picked through the Storage Access Framework included, and leaves browsing what is already found no slower than it was. What is hashed is ADR-0006's shape and unchanged: the length, the first 512 KB and the last 512 KB. **Bytes, not a parse**, which is the part that had to be right -- a data descriptor zeroes the local headers, so a digest built from those would agree between two unrelated archives, and `truncated.cbz` earns an identity even though it will never index. The one thing the scheme cannot tell apart, two files of the same length differing only in the middle, is asserted rather than left to be discovered, and the algorithm itself is pinned to a literal hex digest that appears in *both* suites over a synthetic source each builds byte for byte -- the strongest guard this layer has against the drift that already cost it one silent divergence. **The key a publication is filed under deliberately did not move.** `stableID` keeps the path above the digest, and only there: collection members, reading-list entries, a download's id and the folder its bytes live in, and Kavita's chapter-to-publication table all hold that string, so a key that changed when the digest arrived would have emptied every shelf and orphaned every downloaded file on one launch -- a far larger loss than the one being fixed. It costs nothing, because no identity built in production carried both a path and a digest until now; `matches` keeps ADR-0006's order, and that is what resolves a moved file to its record. **And positions written before any of this exists have a migration.** `save` already filled missing identity components in, but only when the reader opened the publication again -- a reader who tidied their folder first never got that far. `ProgressStore.link` attaches what a scan has just learned without touching the position or `updatedAt`, because learning a file's digest is not reading it and a backfill that restamped the date would reorder "Continue reading" for the whole library on one launch. It writes nothing for a publication nobody has read and is idempotent, so a whole library can be passed through it every scan. **Its one caller is not written**: it belongs in the library view model, which the change that built this did not own. Until it is called, a pre-existing record migrates on the reader's next open rather than on the next scan, and a rename before that open still loses it -- the state before the change, pinned by an assertion a side so it cannot be quietly dropped. *Position identity* is closed on both platforms; **not confirmed on a device**. *Same publication from two sources* is closed too, for the copies the spec's *"resolved through content identity"* can join, and this round put a test a side behind it. Identical bytes reached two ways resolve to one record, which is the ordinary case, because Kavita serves the file it was given: both indexers hand the store a digest (`PublicationIndexer.swift:74`, `PublicationIndexer.kt:334`), `ProgressStore.existing` tries the digest before the path, and `ProgressStoreIdentityTests.aFolderCopyAndAServerCopyAreOneRecord` with its Android mirror asserts one publication, one record. **What stays open is the repackaged server copy**, and that part cannot be closed from inside this app. **`PublicationIdentity.ServerIdentifier` is constructed in production now**, on both platforms, at the one site that learns one: the browser fetches a chapter, writes it, indexes it, and therefore holds a file and the chapter the server calls it in the same breath. The publication it hands the reader carries that identifier, so every position the reader writes carries it too and `ProgressStore.existing`'s server branch — written, tested, and fed by nothing until now — files by it. The kept download deliberately does not record one: the library indexes that file again by its path and its digest, and a record filed under the streamed copy's path would leave the shelf copy with no position at all. `stableID` keeps the path above the server identifier for the same reason it keeps it above the digest, and the two tests that said the server outranked both now say what the rule is and why. **What that does not do is join a folder copy to a repackaged server copy.** Those two share no identity component — different bytes, different path, and only one of them has a chapter id. One surface does hold both halves: `KavitaKeep.keep` indexes the shelf copy and holds the origin in the same scope (`KavitaKeep.swift:131`). It records no server identifier there on purpose. `save` fills a component only when it is null, so the record would keep the streamed copy's cache path, and the library's own scan of the shelf file would then match neither its digest nor its path and would resume at page one. Closing it needs a claim that two different byte streams are the same publication; Kavita's API states no such thing, and deriving one from a title or a filename would merge two different books, which is worse than two records. **The repackaged case stands open on both, and the honest reason is now recorded rather than the construction gap.** One thing the construction did close: a chapter the server repackages between two opens keeps the record it already had, which the digest cannot do. One further thing surfaced while this was built: **Android's instrumented `ProgressStoreTest` had not compiled since `10662ae3`**, which inserted `finishedAtEpochMillis` ahead of `updatedAtEpochMillis` and left nineteen positional call sites binding the wrong parameter. The whole file was dead, which means no assertion in it has run since -- worth knowing before believing anything it appears to cover. The arguments are named now. **Two things stay outstanding beside it.** *The conflict alert names a count, not the two positions the spec asks it to name*, and the clear-history confirmation does not state whether server-side progress is affected. *A source with no progress mechanism should say so on its own screen*, and there is no code and no string for it on either platform -- progress *is* kept locally for such a source by construction, because the store has no idea a server exists, so it is only the sentence that is missing. **Two smaller notes for whoever picks this up.** The four tests a side that look like they cover the finished timestamp assert `ReadingProgress.finished(_:at:)`, which has no production caller; the stamping the app actually performs lives in `ProgressStore.save`/`mark` and neither `ProgressStoreTests.swift` nor `ProgressStoreTest.kt` mentions `finishedAt`. And "Start from the beginning" is on the cover context menu on iOS and the add-to-shelf bottom sheet on Android, which is where the requirement's current wording puts it -- *Restart deliberately* says it is on that menu "rather than on a publication detail screen". **It did nothing at all in iOS's list layout until 2026-08-31.** `CoverList` opened the menu with a trailing closure, which under Swift's forward-scan rule binds to the first parameter with no default -- `onRefused` -- so the restart handler kept the empty closure it defaulted to while the menu drew the button regardless. A reader on the compact shelf long-pressed a cover, tapped the action, and got no confirmation, no clear and no message. Nothing failed, which is why no test caught it and why a test asserting the button exists would have passed too. Fixed at the call site and at the shape: the handler is optional, `RestartOffer` makes "someone is listening" one of the three conditions the button is drawn on, and the confirmation moved into one modifier the grid and the list share rather than a dialog the grid had and the list did not. Android's sheet already gated on a null handler and was immune by construction; it now asserts the same rule instead of merely happening to have it, four cases a side. That was recorded here as a deviation on the grounds that neither app had such a screen. **iOS has one now, and a reader can reach it**: `publication-detail`'s page shipped in a previous round with no caller anywhere, and every cover on every iOS surface leads to it as of 2026-08-31. Which makes the standing note the wrong way round -- the restart is not the clause that deviates. Two others now are. *Continue from the library* says a tap on a part-read publication "opens at the stored position without an intermediate screen", and *Restart deliberately*'s reason clause says "the library opens a publication when its cover is tapped"; on iOS the cover opens the page and the page carries the primary action. That is the `publication-detail` proposal's stated intent -- it words the two entry verbs as a deliberate risk -- but its delta does not list `reading-progress` as MODIFIED, so the requirement and the shipped app disagree in writing. **A spec amendment to make, not a defect to fix**, and it is Android's obligation as well the moment its own mirror lands |
| `local-library` | partial | 15 scenarios and 16 verdicts: 6 built and tested on both, 6 built and asserted by nothing, 4 one-platform, 0 missing on both. **The header table has not taken the move that closed the last one**: against these rows it is one `built and tested` short and one `missing` long. The spec holds 15 scenarios, counted with `grep -c '^#### Scenario:'`. **The sixteenth verdict is one scenario scored as two clauses.** *Nested folder structure becomes series* states a rule and a fallback. Both are built and tested on both platforms since 2026-09-06. The two stay counted apart, because counting them apart is what made the fallback visible: averaging them into one bucket hid a spec clause that no code honoured. **This row is a re-audit against the source, and the row it replaces was wrong in three places.** *Deleting an imported copy* was listed as having no test on either side. Both platforms assert it, in the same words: `DownloadQueueRemovalTests.swift:82` and `DownloadQueueRemovalTest.kt:71` both name the case "a finished import names the original it is not touching", and both landed with `93d25b34` and `f44af9a0`. *Change detected while backgrounded* was called untested on both. The rule the scenario states is asserted on both; the trigger that runs it is asserted by nothing. The trigger exists on both platforms: `FolderWatcher.swift:130` fires when the scene turns active and awaits `reconcileWatchedFolders()` at `:135`, and `LibraryView.swift:334` applies that modifier to the shelf; Android runs the same method from `ON_RESUME` at `LibraryScreen.kt:292`, calling it at `:300`. `FolderSnapshot` is a mirrored pure type with ten cases a side -- `FolderSnapshotTests.swift:33` to `:52` compare a date and a size, and `:92` asserts that only the added and the changed are opened, which is the scenario's "rather than re-reading every archive" word for word. And *File type registration* was called asserted by nothing in both manifests; Android has a manifest tripwire at `AudiobookAppearsInTheOpenWithListTest.kt:35`, but it names the four audio types, not the six this scenario names, so the scenario's own claim is still asserted by nothing on both sides. **The header's "four one-platform verdicts are not gaps" holds, and its reason fits only one of the two pairs.** *Adding a folder on iOS* and *Adding a folder on Android* are one requirement written twice -- a security-scoped bookmark (`FolderBookmarks.swift:20`, taken at `LibraryModel.swift:293`) and a persistable tree permission (`LibraryScreen.kt:250`, `HomeDestination.kt:150`, `SearchScreen.kt:136`) -- and both are built. *Remembering an opened file* is not one behaviour split in two: the two scenarios ask for **opposite** outcomes. iOS stores a bookmark to the file and lists it next launch. Android's scenario states that the publication "is not kept", because the grant dies with the process, and the reader keeps it by importing instead. Both are honoured, so neither is a gap -- but "complete on both sides" is the wrong reason for the Android one, and nobody should read the header as saying Android remembers a handed-over file. **Access is revoked is built on both, and the spec's own word is not the one either app uses.** The scenario asks for the source to be marked `unauthorized`. Both apps mark a folder that no longer resolves `unreachable`, deliberately and with the same argument: `LibrarySourceHealth.swift:256` and `LibraryViewModel.kt:622` both say that a red badge on a folder that is merely unmounted would contradict "offline is a normal state", and that `local-library` names an unavailable folder separately -- which it does. The naming and the single action are built: `UnavailableFolderNotice` at `LibraryStates.swift:199`, mounted at `LibraryView.swift:351`, and `UnavailableFolders` at `LibraryEmptyStates.kt:85`, mounted at `LibraryScreen.kt:420`. Both draw the folder's name beside a re-pick button that opens the same picker, and progress survives because ADR-0006 keys it on the publication. **iOS asserts the state and Android asserts nothing**: `LocalSourceStateTests.swift:68` pins "a folder whose bookmark did not restore is unreachable, not connecting", while `LibraryViewModel.folderState` and the Android notice are reached by no test. The spec and the code disagree in writing, and that wants settling as a spec decision rather than a defect. **The scan is the strongest part of this capability, and it is mirrored case for case.** `LibraryScannerTests.swift` and `LibraryScannerTest.kt` hold twenty-two and twenty-three cases over the same fixture corpus: subdirectories walked, counts that match what was emitted, publications emitted before the scan finishes (`LibraryScannerTests.swift:97`), a cancelled consumer stopping the walk (`:132`), and a refusal carried with its reason. Resumption is `ScanJournal`, six cases a side. Cancelling writes down where the scan reached before it stops -- `LibraryModel.swift:300` and `LibraryViewModel.kt:915`. **Nested folders become series on both, and the spec's fallback now does too, so this scenario is scored twice.** **Its THEN clause is built and tested on both.** A subfolder names the series (`LibraryScannerTests.swift:29`, `LibraryScannerTest.kt:39`) and the shelf divides on it, nineteen cases a side in `LibrarySectionTests.swift` and `LibrarySectionsTest.kt`. Within a series the parsed number orders numerically (`LibraryIndexTests.swift:100`, `LibraryIndexTest.kt:101`). A reader reaches all of it: adding a folder scans it at `LibraryModel.swift:295`, the shelf asks for sections at `LibraryContent.swift:56` and `LibraryScreen.kt:697`, and the series comparator orders each section at `LibraryIndex.swift:239` and `LibraryIndex.kt:253`, by number at `:298` and `:274`. **Its AND clause was built on neither platform, and it landed on both on 2026-09-06.** The spec licenses the fallback for one subfolder -- "a subfolder whose contents cannot be ordered" -- so it sits inside the series comparator, below the series name and below the issue number: `LibraryIndex.swift:311` and `LibraryIndex.kt:291`. A series whose issues carry no number is that subfolder. **The shelf-wide tie-break was left on the display title, and that is deliberate.** `LibraryIndex.swift:91` and `LibraryIndex.kt:85` still collate it, because five of the seven sort fields tie for whole libraries -- last read and progress tie across every unopened row, date added and file size across every row that carries neither, year across a year -- and `library-browsing` requires the locale collation and the article stripping of every title comparison. The first attempt put the filename order on that line instead and reordered those five shelves; it was moved down the same day. The name compared is `fileName`, at `LibraryIndex.swift:166` and `LibraryIndex.kt:171`: the last component of the identity path, or `nil` where a publication has no file. A pair with no file keeps the collated title, because a server chapter is not a file on disk and its title is owed the reader's collation. The rule itself is one function a side -- `NaturalOrder.swift:20` and `NaturalOrder.kt:26` -- moved down out of the format layer so that `PageOrdering` and the library share it instead of keeping two copies that drift. It reads a digit as one of the ten, at `NaturalOrder.swift:72` and `NaturalOrder.kt:84`, because Swift's `isNumber` and Kotlin's `isDigit` disagree about a superscript two and would file one folder in two orders. Seven mirrored cases a side assert all of it. `FilenameOrderTests.swift:41` and `FilenameOrderTest.kt:37` put `ch10` after `ch2`; `:52` and `:48` show that case alone decides nothing; `:63` and `:59` pin the ten digits; `:74` and `:70` show that an issue number still wins, so the fallback does not fire; `:84` and `:80` show the title sort still files by the collated title; `:94` and `:90` hold a tied year sort to the collated title; `:104` and `:100` hold a series of server chapters to it. Five of each were watched failing by name before the fix. The previous row scored the whole scenario `built, asserted by nothing`, which was wrong twice over: nineteen mirrored cases a side assert the rule, and the unbuilt fallback reached the header as a zero. **Open-in is wired end to end on both and asserted by no behaviour test on either.** On iOS `StoryArcApp.swift:239` reaches `openHandedOver` at `StoryArcAppActions.swift:28`, which indexes the file, opens the reader, and remembers it. On Android `MainActivity.kt:123` on a cold start and `:88` on a new intent fill `handedOver`, which `AppIntents.kt:63` consumes and routes to `host.open`. **Neither `OpenedFile` is tested.** The iOS one lives in `apps/ios/App/`, which no gate runs: `pnpm test:ios` runs `swift test` in `StoryArcKit` only, and `build:ios:tests` compiles the app target without running it. Android's two suites over `OpenedFile.kt` read the file as text rather than run it. The refusal has the same shape: the detection is asserted a side (`PublicationIndexerTests.swift:165`, `PublicationIndexerTest.kt:192`), and the sentence a reader actually reads -- `RefusedFile.swift:31` and `RefusedFileDialog.kt:19` -- is asserted by nothing on either. **Remembering a handed-over file is complete on iOS.** `FolderBookmarks` tells a file from a folder by asking the filesystem, so a bookmark written by an older build lands in the right list; `LibraryRestore.swift:48` puts the remembered files at the head of the scan; `LibraryScanning.swift:112` adopts each under no source at all; and `FolderBookmarks.swift:81` sets the cap at twenty, which `:86` enforces oldest-first. The previous row cited `:36` for that cap, and `:36` is a blank line. Four cases in `FolderBookmarksTests.swift` and three in `LibraryRestoreTests.swift` cover the kind split, the limit, and a vanished file being forgotten silently rather than reported as a folder the reader could re-pick. Android keeps nothing, and this audit confirms it cannot: all three `takePersistableUriPermission` calls are `OpenDocumentTree` folder pickers, so no handed-over `Uri` is ever persisted. **Imported copies are built and tested on both**, nine mirrored cases a side in `ImportedCopiesTests.swift` and `ImportedCopiesTest.kt`: copied into the download store, indexed, the same file imported twice is one copy, never swept away by "remove downloads after finishing", and the copy outlives the original. The delete confirmation names the title, the space freed, and the original it does not touch -- `DownloadsDestination.swift:162` and `DownloadsParts.kt:375`. **The dead `importedBytes` accessor is now read on both, and it was untested as well as uncalled.** A repository-wide search on 2026-09-06 found `LibraryImports.swift:80` and `LibraryViewModel.kt:1191` and no other mention of the name in any Swift or Kotlin file, so the previous row's "tested" was wrong: nothing asserted it either. The figure reached a reader only inside the downloads total, which `SettingsGroup.swift:94` describes as everything StoryArc's own files weigh and every screen labels *downloads*. **It is shown rather than deleted, and `offline-downloads`' *Storage view* is the clause that decided it**: the total is to be "broken down by source", "On this device" is a source, and this is the only part of that breakdown either app can state today. `local-library`'s *Importing* scenario is the second clause -- "the app reports the space used". The storage group draws it under the downloads total when there is something imported and draws nothing when there is not: `DownloadsSettings.swift:69` and `DownloadsGroup.kt:97`, under `downloads.imported` and `downloads_imported` in en, fr, de and es. Four cases a side assert the number (`ImportedStorageTests.swift`, `ImportedBytesTest.kt`) and three a side assert the row (`ImportedStorageRowTests.swift`, `ImportedStorageRowTest.kt`); all four suites were watched failing by name against a mutated source or a mutated input. **A breakdown by every source is still unbuilt on both platforms** and belongs to `offline-downloads`, not here. **What has no test on either platform**: the file type registration for the six named formats (`project.yml:134`, `AndroidManifest.xml:46`), the open-in chain and its refusal sentence, both watchers (`FolderWatcher.swift:23` and `FolderWatcher.kt:28` are reached by no test on either side), the 10,000-item budget, which appears only in comments at `CoverLoader.swift:9`, `LibraryModel.swift:79` and `CoverLoader.kt:25` and has never been measured, and Android's persistable permission with its unavailable-folder notice. **Two of the six `built, asserted by nothing` verdicts are asserted in part, and the count alone does not say so.** *Access is revoked* is asserted on iOS at `LocalSourceStateTests.swift:68` and by nothing on Android, where `folderState` at `LibraryViewModel.kt:630` and `UnavailableFolders` at `LibraryEmptyStates.kt:85` are reached by no test. *Unsupported file* has its detection asserted a side (`PublicationIndexerTests.swift:165`, `PublicationIndexerTest.kt:192`) and the sentence a reader reads asserted on neither. The other four -- *Large library*, *Open-in from another app*, *File type registration* and *New file appears* -- have no test on either side, which is what the table's line for this bucket describes. Read the row for which is which. **No verdict here rests on ungated proof.** Every suite named above is in `StoryArcKit`, which `pnpm test:ios` runs, or in a `src/test` source set, which `pnpm test:android` runs. Nothing in this capability is proved only by `apps/ios/UITests`, by `apps/ios/Packages/StoryArcEpub`, or by an `src/androidTest` suite. **The `forgetVanished` defect that stood from the previous two rounds is fixed, and it was two defects in one comparison.** `LibraryScanning.swift:268` compared a publication's stored location, standardised to `/var/...`, against the folder path, which a resolved bookmark spells `/private/var/...`. **The direction was measured rather than assumed**: the comparison decides the publication is *outside* the folder, and `gone` selects what is *inside*, so nothing was removed -- a reader who deleted a book kept a row that opens on a file that is not there. It never forgot a book that was present. **The second defect did.** The prefix carried no trailing separator, so a walk of `.../Comics` read `.../Comics2` as being inside it and forgot a neighbouring library's books. Both sides are now resolved through `standardizedFileURL.resolvingSymlinksInPath()` and the separator is kept, which is the spelling `isAppStorage` and `isOnDevice` already use. `VanishedSpellingTests.swift` holds four cases; the two failing ones were watched failing by name before the fix -- `2 == 1` for the bookmarked spelling and `1 == 2` for the neighbour. **Android cannot hold this defect and was not changed.** `ScanReconciliation.vanished` compares no path at all: membership is decided by the source a row was attributed to (`LibraryViewModel.kt:779`), and Android locations are Storage Access Framework document tree URIs, which carry no `/private` alias. **The identity-link change is not in this worktree, and it would move nothing here.** `LibraryModel.linkScannedIdentities` does not exist at this commit; it landed in `839e4ae7`, which is not an ancestor of `main`. Its subject is a reading position surviving a rename, which is a `reading-progress` scenario -- no scenario in this spec names reading progress. **Nothing was driven.** No simulator and no emulator was used in this round. Every verdict above is read from the source and from the suites the gates run |
| `library-browsing` | partial | 29 scenarios: 25 built and tested on both, 4 built and asserted by nothing. **Re-audited on 2026-09-06, and only in part — the eleven scenarios added by `named-failures-and-quieter-chrome` were read against the source on both platforms, and the eighteen this row already counted were carried forward unread.** Everything below the next bold sentence describes the carried eighteen and was written before 2026-09-06. **The eleven re-audited: ten built and tested on both, one built on both and asserted on one side.** *The scope is stated, and can be narrowed* is one control on both faces of the search screen — iOS's `SearchScopeStatement.swift:24` is drawn by `SearchResultsView` and `SearchAtRest` alike, which `StatedAxisTests.swift:98` and `:107` pin as one type in two places, and the scope has a key of its own (`SearchScopeTests.swift:107`); Android reaches the same two lines through `LibraryControls.kt:123-131` over `LibraryViewModel.kt:170-172`, which reads `LibraryPreferences.searchScope`, with `SearchScopeTest.kt:49-172` and `ScopeChipsWrapTest` behind it. *Searching with every source unreachable* names the quiet libraries in one sentence rather than one row each: iOS's `SearchResultsView.named()` is asserted at `StatedAxisTests.swift:118`, `:135` and `:142`, Android's `SearchListing.kt:105` and `:238-239` at `SearchListingTest.kt:149`, `:158`, `:168` and `:177`; narrowing to the device asks nobody, which `SearchScopeTests.swift:136` and `SearchScopeTest.kt:118` each assert. The six skipped-notice scenarios are the closest mirror in this file: `SkippedPublications` is the same value on both (`SkippedPublications.swift:18-109`, `ScanReport.kt:40-134`) with the same four-case `Notice` and the same `settling`/`dismissing`, and ten test names match word for word across `SkippedPublicationsTests.swift:26-113` and `SkippedPublicationsTest.kt:31-126`. *The notice is not on a timer* has three cases on iOS (`SkippedNoticeTimerTests.swift:37-57`) against one on Android (`SkippedNoticeTest.kt:72`, "the notice outlives the six seconds the toast had"), and both notices are laid out in the shelf's own column rather than over it (`LibraryView.swift:250`, `LibraryScreen.kt:442`). *Announced without sight* is one merged element with a separately named way to the list on both (`SkippedNoticeAnnouncementTests.swift:111-194`, `SkippedNoticeTest.kt:98-110`); iOS additionally asserts that nothing in the notice asks for focus and Android does not. *The controls that change the view are grouped* and *A control that stands alone carries a name* are the same fold on both — availability, layout, sort and direction inside one named menu, Select standing alone because it changes mode (`LibraryToolbarTests.swift:104`, `:159`, `:172`, `:197`, `:249`, `:267`; `LibraryControlsAreNamedTest.kt:126`, `:140`, `:182`, `:194`, `:214`). **The one that is not `built and tested` is *An ordering says that it is an ordering*, and the asymmetry is in the assertions rather than in the code.** Android frames every ordering through `ListOrder.chipLabel()` at `ListOrder.kt:285-286`, pinned in all four languages by `SortChipNamesAnOrderingTest.kt:64-79`, which also holds the deliberate exception that the curated order is not dressed as a sort. iOS frames it too, by a different route: `ListOrderMenu`'s label is *Sort* with an up-down glyph and the field name is its `accessibilityValue` (`ListOrder.swift:149-158`), so the field name is never shown as a control's whole label. Nothing on iOS asserts that. `ListOrderTests.swift` has twelve cases and none of them is about naming, so a rewrite that put `order.titleKey` in the label would pass every test in the file. Scored `built, asserted by nothing` rather than `built and tested`, because a test on each side is what that verdict requires. **Unified library, search grouping by match kind, and per-scope layout are built on both platforms.** The shelf spans sources rather than folders: finished downloads join it, each attributed to the source its record names, so a chapter fetched from Kavita is in the library instead of being reachable only by browsing back to the server. Read that limit exactly as written -- the shelf holds scanned local folders **plus finished downloads**, and nothing else. A connected Kavita, OPDS or SMB source contributes nothing to it until a file has been downloaded from it, and SMB shares are browsed and never indexed. A scope selector narrows the whole view -- the shelf, the search, the filters and the continue row -- to one source; it persists until changed, and a scope naming a source that has since been removed opens the whole library rather than an empty one. **Built on both, and the iOS half was mounted and watched on 2026-08-30.** `ScopeMenu` was written, translated into four languages and mounted by nothing, so an iOS reader could not leave "all sources" at all. It is now first in `LibraryToolbar`, where Android puts its own, gated on the same rule: `ScopeMenu.offered(in:)` returns nothing below two sources, so the control is absent rather than a menu with a single answer, and six cases assert that gate against the `attributesPublications` Android gates on. **The crash the previous round reported does not reproduce.** That round recorded an `EXC_BAD_ACCESS` inside `ToolbarContentBuilder` on first layout and suspected the `Picker` tag -- `LibraryScope` being an enum that carries a `UUID` where the neighbouring `SortMenu`'s tag is a plain enum. The item was re-mounted verbatim as that comment described, built with `pnpm build:ios`, and run on a booted iPhone 17 Pro with two sources configured: the app launches, the menu opens, a source can be picked and the choice persists across a relaunch. The tag is not the mechanism -- `LibraryScope` is `Hashable` and hashes as its `UUID`, which is what a `Picker` tag needs -- so the `Picker` was kept rather than churned into a menu of buttons for a cause that was never established. Checked in both layouts, because the reported trace named `libraryColumn` and the stacked and split layouts share it: the split layout launches and draws the control on a booted iPad Pro 13-inch as well. What the earlier round could not verify, it was right to leave rather than ship. A publication carries its source's name only where more than one source is configured. Search results are grouped as series, titles, people and tags, the headings ordered by the best match rather than by a fixed list, and each publication appears once under the reason it ranked where it did. Grid or list is remembered per scope, falling back to the choice a reader made before the layout was per scope. The grouping, the scoping and the attribution are asserted against the same table in both suites; the download attribution is asserted in both persistence suites. **The iOS scope menu has since been watched** (see above). The group headings, the source line under a cover and the Android side of all three have still only been read -- no emulator was available in that round, and the Android per-scope layout is covered only by an instrumented test that was never run. **Two things this row used to list as outstanding are built and tested on both, and one is not what it looked like.** Date-added and file-size sorting are complete: both cases are in the enum, both are stamped by the scanners, both are ordered in `compare()`, both appear in the sort menus with translated strings, and both are asserted in both suites. **Recent searches are complete on both.** The value, its rules, its persistence and its tests were already on both platforms and Android already showed the list; on iOS `RecentSearchSuggestions` was dead code, because the library's `.searchable` attached no suggestions closure. It attaches one now, so an iOS reader who opens an empty search field is offered the terms they typed before, each running the search again, with a Clear action under them. **Watched on a booted simulator**: a term typed into the shelf's field, then cleared, came back under a "Recent searches" heading with its clock glyph and a destructive "Clear recent searches" beneath it -- once, not once per keystroke. That was one modifier, not a feature -- and this row previously listed the scenario as outstanding, which the code already contradicted. **The filter count was wrong in both halves.** The spec names ten facets, not eleven. Seven are built on both. Of the remaining three the spec's own Open Questions rule out two -- source is the scope selector, which is already built, and publication status has no source in any format this app reads -- which left **the "Downloaded" filter as the one real gap**. **It is built on both now**, and the answer to the question it raised is that the group and the availability axis are not the same control said twice. The axis keeps everything that opens with no network -- a folder a reader picked as much as a download the app fetched -- because it asks *will this open on a plane*. The group asks the narrower question `offline-downloads` owns: did this app fetch it and is it keeping it. A file a folder walk found answers yes to the first and no to the second, and the difference is the one the app already draws for the mark on a cover: the card can be pulled, the grant can lapse, the folder can be unmounted, and only the app's own copy carries the promise. So the group admits a subset of what the axis admits, which is what keeps *Filtering offline*'s clause -- only what reads without a network, whatever the source is doing -- true of it. The spec's own Open Question had said as much before either existed: what blocked download state was that "the library is assembled from a scan that never consults" the record of downloaded files, and the axis never needed that record. Three answers rather than two ticks, for the reason the decade group is a radio list -- a publication is downloaded or it is not, so ticking both is the same as ticking neither -- and it counts in the badge and goes when Clear filters goes, on both platforms. Ten cases a side assert the rule, including the one that would rot quietly: a chapter downloaded from a server that has since gone away stays on the shelf, because the rule never asks the registry. **Not driven on either platform**, and the two things worth watching are the menu at the largest text size and the empty state a reader reaches by asking for "Downloaded" with nothing downloaded. **The divergence it inherited is closed.** On iOS both the download group and the availability axis are `@AppStorage` and survive a cold launch; on Android both were `rememberSaveable`, so they came back after a rotation and reset on a relaunch. `LibraryPreferences` now holds a key for each, in the same preferences file the query and the per-scope layout already use -- which is the arrangement iOS reaches from the other side, its two `@AppStorage` keys landing in the `UserDefaults` the rest of the library's preferences live in. Neither is a field on `LibraryQuery` on either platform, and deliberately: the query is the value both encode, so a case added to it is a change to `:core:model` and to iOS's mirror of it. Nothing is migrated on either side. Stored as the enum's name and not its ordinal, because an ordinal is a position in a source file; a name the app cannot read widens the shelf rather than narrowing it, which is the direction that cannot strand a reader. Eleven cases assert it -- the round trip and the three keys' independence in `:core:persistence`, the name mapping and its fallback in `:feature:library`. **Not driven**: what a test cannot reach is the composition that reads the store on first draw, so the proof that a shelf left narrowed comes back narrowed is a relaunch on a device. **The widen-scope offer is now on both.** `NarrowedToNothing` was constructed on iOS with neither `scopeName` nor `widen`, so the "search all sources" button never drew and `LibraryModel.widenToAllSources` had no caller: an iOS reader scoped to one source who searched and found nothing was told "No publication matches the active filters" and offered only a Clear filters button. Both arguments are passed now, the same way Android passes them -- the offer present only when the view is scoped somewhere narrower than every source. Neither platform asserts it: the offer is a branch inside a view, Android's view-model cannot be built in a JVM unit test, and neither app has a UI test target. **Watched end to end on a booted iPhone 17 Pro, now that the scope can be changed**: an unscoped library that matches nothing offers Clear filters and no widen button, which is correct; narrowing to a source that holds nothing says "Nothing from Attic NAS is on this device yet" and puts a "Search all sources" button above Clear filters; tapping it returns the shelf to every source. Light and dark, at the default and the largest accessibility text size, where the menu wraps and stays legible. The branch itself is still asserted by neither platform, for the reasons above, but `LibraryModel.widenToAllSources` now has two cases of its own -- including that widening leaves the search and the filters where the reader put them, which is what keeps it a different offer from Clear filters. **The last missing scenario is built and tested on both, and this row had it wrong in a way worth recording.** *Mixed local and server search* was scored missing on the grounds that "nothing merges anything" and that "the library's own search never asks a server". Both halves of that were already false: `LibrarySearch` fans one question out to every Kavita and OPDS source, and `SearchAnswers` merged the answers append-only. What was genuinely absent is the other two words of the scenario -- **ranked**, and **each labelled with its source** -- and the labelling was absent *on purpose*, because the implementation followed the unarchived `one-library-three-destinations` delta, which modifies this very scenario to say "no result is labelled with the source that supplied it". **The main spec is the contract and the two disagree in writing; that conflict is now the one thing left here and it is a spec decision, not a defect.** What was built follows the main spec. **The unlabelled merge was also losing answers, and it was photographed doing it.** `SearchAnswers` folded a remote row into any local row of the same kind and title, across libraries. On 2026-08-31 a search for *Fine Print* with a catalogue and a folder configured produced `200 GET /opds/all?q=Fine%20Print` in the server's own log and not one catalogue row on the screen: its single match had the title the device already held, so it was folded out of existence with nothing on screen to say a catalogue had answered. `ios-search-remote-and-away-dark.png` is that screen. `SearchListing` replaces the merge in `LibraryFeature` and `:feature:library`: it keeps the append-only promise -- a late answer adds rows below and never moves one -- and folds a duplicate **only when the same library said it twice**, which still folds a server's copy of a chapter downloaded from that same server, because a download carries its library's identity. `SearchRank` is the ranking, applied to each answer as it lands including the device's own: title strength first -- is, begins with, begins a word, contains, elsewhere -- then held before away, then the shorter title, then the folded title. **A server's better match outranks a local worse one, and place only breaks a tie**, because the reader asked for a book and not for a place; the pair that answer equally are settled by the one that opens with no network. Both types are pure and mirrored, thirty-three cases a side, plus integration cases over the exact two documents `scripts/opds-server.mjs` serves -- Android's four drive them through a real server on the loopback interface and assert the app asks for `q=Fine%20Print`, iOS's three assert the same outcome from the same bytes through its own parser. Each row now carries a third line naming its library, reusing the shelf's own "From %@" and "On this device" in the four languages they already ship in. **The rule for when that line is drawn is not the shelf's**, and an independent review of the branch is what caught the difference: the shelf shows a source only when more than one source is *configured*, and one server plus files another app handed over is one source -- so the label was hidden in the commonest mixed search there is. It is drawn when more than one *place* could have answered, the places being the origins of what the device matched plus every library asked, decided once when the question is asked so it cannot appear on the rows already on screen when the second library replies. A remote row still leads to its server's own browser and never to the publication page, which resolves against the library's own set. **Not driven on either platform**, and the screen to capture is exactly the one that was photographed failing. **What the append-only rule does and does not promise is now stated exactly, because it was overstated.** `rows` only ever grows and no row is removed, replaced or reordered against another -- but `groups` partitions by kind, so a late row whose kind already has a heading lands inside it and pushes every later heading down. A row can move *down*; it can never move up, past another row, or onto a different heading. Appending late rows at the bottom under a fresh heading of their own would fix the position and was rejected: ranking runs across kinds within one answer, so the device's own answer would already shatter into repeated headings, and one heading per kind is what *Typing a query* asks for. The stronger clause the doc comments used to quote -- "the arrival of remote results never reorders or displaces a result the reader is already reaching for" -- **is not in the main spec either**; it is in the same unarchived delta as the labelling clause, and the two want settling together. A case a side renders the groups and pins both halves. **Four mirror divergences were found and fixed on the way**, which is what the mirrored-logic rule exists for: `String.folding(options:)` full-case-folds "Straße" to "strasse" where Kotlin's `lowercase` cannot, the length key counted grapheme clusters against UTF-16 units, the title tie-break ordered by scalar against UTF-16 unit, and the trim borrowed two platform defaults that disagree about the non-breaking spaces. All four are now spelled out identically on both sides and pinned by cases a side. `SearchAnswers`, its tests, `SearchResultGroup` and core's `SilentSource` are **deleted** -- the merge had to be written beside them while those directories belonged to other agents, and they were removed once that wave landed. What *also* exists, and is a different scenario belonging to `kavita-server`, is the offer beside the local matches to put the same query to a scoped Kavita server, which is built on both. **The two that used to sit beside it are built.** *Filtering offline* is the "Downloaded" filter above. *Overriding a curated order* -- a reading list had no sort control, no return-to-curated action and no label on the order it was in, so a reader could not tell that the order in front of them had been chosen for them, could not sort it their own way, and had nowhere to get back to; the third clause held only because nothing could violate it. It has all three now, on both platforms, built out of the library's own sort rather than a second one: the order the list is in is one of the answers rather than a separate idea, and the control carries the name of the order it is in, which is what labels the curated order as curated -- on the chip on Android, and in the line under the count on iOS, where a toolbar menu draws its glyph and not its title. While a sort is overriding the list a second control appears named for where it goes, which is the one tap the requirement asks for. The third clause is now enforced rather than merely unviolated: nothing is written back, sorting goes through `LibraryIndex.arrange` so a list collates the way the shelf does, an entry the library can no longer answer for keeps the tail in the list's own order, a row's number stays its place in the list rather than its place on screen, and **rearranging is offered only in the curated order** -- `EditButton` gives its place to the way back on iOS and the arrows leave the rows on Android -- because a move reports the position it landed in *as drawn*, and writing that into someone else's reading path is the failure the clause exists to prevent. Twelve cases a side. **Neither has been driven**: a reading list with entries has to be built by hand first, and no simulator or emulator was reached in this round. **A chosen order lasts as long as the screen does**, on both platforms, which is the shortest honest reading of "for that session" -- leaving the list lands back on the order that means something. **The cover grid now keeps both bounds on both.** Android used `GridCells.Adaptive(minSize = 108.dp)`, which takes no maximum, so the Adaptive-columns scenario's second clause -- cover size stays within the readable range -- held on iOS only, and the Kotlin comment claiming the same bounds was wrong. `BoundedAdaptive` replaces it: below the cap it computes exactly what `Adaptive` computes, remainder pixels included, and above it every column stops at 168 dp, which is what a narrow window needed -- one column was stretching to the whole width and a single cover filled the screen. Seven cases assert it, including a sweep over every window width from 1 to 2400 px; there is no iOS mirror because SwiftUI owns the same arithmetic inside `GridItem(.adaptive(minimum:maximum:))`, and the pair of bounds is what the two platforms share. Seen on the emulator at a width where the cap does not bite, which is every width a phone has -- the band it changes is 169 to 231 dp, which is a freeform or split window, and none was driven. The *catalogue* grid is a different grid and is still uncapped on both, matched at `Adaptive(140.dp)` against `.adaptive(minimum: StoryArcSpace.huge * 2)`; the scenario names the cover grid, so that is left as it is rather than changed on one side. **Two scenarios exist on both with no test**: progress on covers, and the volume/chapter ordering of an opened series -- for which no local-library series screen exists at all. The third used to be the curated default order of a reading list, which `ListOrderTests`/`ListOrderTest` now assert on both sides. **Two stale in-code comments to sweep when this area is next touched**: `LibraryIndex.swift:10-13` and `LibraryIndex.kt:14-16` still name grouping by match kind *and* the curated order of a reading list as absent, and both have been built since; and `LibraryQuery.swift:63-70` and `LibraryQuery.kt` still say download state "needs the library to know what has been downloaded", which it now does. All four sit in `StoryArcCore` and `:core:model`, which no agent this wave owned, so they were left rather than half-corrected on one side. **Two layout defects the 2026-08-31 emulator captures recorded are fixed on Android and neither has been re-photographed.** The filter chip row ran off the window at `font_scale 2.0` with *Filter* half out of it and nothing on screen saying the row scrolled -- it did scroll, and reachable through an interaction a reader has no reason to attempt is not reachable. It is a `FlowRow` now, so at the largest text size the four controls take two or three lines and all of them are simply on screen; each chip is boxed with the menu it opens so a popup that measures as nothing cannot take a slot of nothing and push the next chip onto a line it did not need. And the tablet's detail pane drew a back arrow while the shelf sat permanently beside it, with nowhere to go back to; Material's own `ListDetailPaneScaffold` hides that affordance when both panes are visible and `AppContent` composes its pair by hand, so `HostedScreen` carries the rule as `isBesideList` and the bar leaves its navigation slot empty. Back itself is untouched -- the gesture and the pane's own "this is gone" button still pop the page and return the pane to its one sentence. **Neither is asserted**: both are branches inside a composable, and neither Android module has a UI test target. iOS has no counterpart to the second -- its split layout pushes the page onto the column's own stack, where a back control is correct and the system draws it **`named-failures-and-quieter-chrome` synced on 2026-09-04 and was archived on 2026-09-05.** The shelf names what it could not open, in a durable notice the reader dismisses rather than a timed toast, with the reasons kept beside the count and reachable afterwards; the toolbar folds show and scope into existing menus and leaves select standalone; *Presentation* gained the grouping rule. The eleven scenarios that change added were re-audited on 2026-09-06 and are described at the head of this row. **For a day no cover on the iPhone shelf opened anything** (2026-09-05 13:05 to 2026-09-06): the iPad pane change left the shelf's value links without a destination they could see -- SwiftUI's log says such a link cannot be activated -- and every gate stayed green, because the tripwire pinned a line's position and no walk opened a page from the shelf in between. Fixed by driving the detail stack's path from the shelf column; `CurlWalkTests` is the phone's proof and `SweepIpadPanes` the iPad's. **Every Android cover surface is on the size ladder since 2026-09-06**: the three remote grids ask `rememberCoverColumns()`, and the five surfaces with a width of their own -- the catalogue groups row, the shelf cover picker, the series shelf, the shelves lattice and the list thumbnail -- step by 1.4 at the accessibility boundary, which `CoverLadderStepTest` pins by arithmetic and `design.md` §4's table records row by row; the pane-versus-window measurement is recorded as decided there too, with the 840 dp / 900 pt wide-tier threshold the one divergence left open. Frames in `android-cover-ladder-2026-09-06`. |
| `settings-and-about` | partial | **28 scenarios, every one re-read against the source on 2026-09-06: 15 built and tested on both, 13 built and asserted by nothing.** Nothing here is one-platform and nothing is missing. The previous row scored 14 and was written before the app-icon chooser and the what's-new screen were specified; those two requirements carry 13 of the 28, and its counts are replaced rather than added to. **One rule decides both platforms: a suite no gate runs asserts nothing.** `package.json:44` defines `check` as lint, `test:ios`, `build:ios:tests`, `test:android` and `build:android:tests`. `build:ios:tests` at `package.json:42` is `xcodebuild build-for-testing` and `build:android:tests` at `package.json:40` is `assembleAndroidTest`; both compile and neither executes. So `apps/ios/UITests` and every Android `src/androidTest` file are compiled and never run, and this row credits neither as an assertion. An earlier draft of this row credited `androidTest` and discounted `UITests`, and read 18 and 10 as a result. Correcting the asymmetry moves *Choosing appearance*, *Resetting* and *Structure* down to *asserted by nothing*. **Two defects the previous row records are fixed.** The diagnostic no longer reports `configured = 0` as a literal: `Diagnostic.swift:128` and `Diagnostic.kt:116` both count the registry the screen hands them, and seven matched cases a side pin it, including one named *the count is the real one, not a literal* at `DiagnosticSourcesTests.swift:63` and `DiagnosticSourcesTest.kt:78`. Its parenthetical that "`clearing()` and `clearCache` are called by no test at all" is also out of date -- `ClearCacheTests.swift:46` and `ClearCacheTest.kt:34` drive `clearCache` a side, and `FileProtectionTests.swift:64` calls `clearing()`. **The icon chooser's accessibility promise is proved in part on iOS, not left wholly unproved.** `swift test --filter AppIconChooserAnnouncement` in `apps/ios/Packages/StoryArcKit` reports `Test run with 6 tests in 1 suite failed after 0.018 seconds with 7 issues`. Swift Testing runs each case on its own, so the failing calibration case at `AppIconChooserAnnouncementTests.swift:141` does not take its neighbours down. Three cases pass: `:151` holds that every face is one combined element named for the face, `:199` that a platform offering no choice draws no rows, and `:208` that a refusal names the face still in use. Two fail because the calibration bits read `nil`: `:170`, the *in use* trait, and `:183`, the decorative tile. So the scenario's first clause -- announced by name and never as an unlabelled image -- has a green iOS assertion, and the *in use* clause and the tile clause have none. Android's half is green and in `src/test`, not in an instrumented directory: `AppIconRefusalNamesNoFaceTest.kt:126` and `:133` find each row by the face's name and assert which one is selected. The decorative-tile clause has a green assertion on neither side -- iOS's case fails and Android has only the `contentDescription = null` at `AppIconGroup.kt:222`. *Announced without sight* is therefore *asserted by nothing*, and a repair should target the *in use* half and the tile rather than all five cases. A repair is on a branch this audit could not see; the verdict describes this worktree. Across the whole settings tree, `swift test --filter SettingsFeatureTests` reports `Test run with 62 tests in 10 suites failed after 0.034 seconds with 7 issues`: nine of the ten suites pass, and all seven issues are this one. **The chooser is otherwise whole on both platforms, and a reader reaches it.** `StoryArcApp.swift:187` opens `SettingsView`, which draws `AppearanceSettings` at `SettingsView.swift:200`, which draws `AppIconSettings` at `AppearanceSettings.swift:99`; Android runs `SettingsHost.kt:33` into `SettingsScreen`, `AppearanceGroup` at `SettingsScreen.kt:395` and `AppIconGroup` at `AppearanceGroup.kt:176`. The platform is the only record on both, asserted by `AppIconChoiceIsNotRecordedTests.swift:105` and `AppIconChoiceIsNotRecordedTest.kt:116`. The refusal, the no-face-at-all case, the absence of a silent retry and the return to the default are pinned by `AppIconStoreTests.swift:119` and `:164` and by `AppIconAliasesTest.kt:120` and `:174`. The chooser is reachable by the settings search through `SettingsAnchor.swift:16` and `SettingsAnchor.kt:21`, and each platform's search test walks every anchor. **The spec carries two *At the largest text size* scenarios, at `docs/openspec/specs/settings-and-about/spec.md:75` and `:176`, and neither is measured by a running iOS test.** Android measures the what's-new sheet in three cases under `src/test`, which `pnpm test:android` runs: `WhatsNewLayoutTest.kt:121` holds the dismissing action on screen at 200 per cent, `:144` holds the icon column's width, and `:158` holds every entry's width. `node scripts/gradle.mjs :feature:settings:testDebugUnitTest --tests '*WhatsNewLayoutTest*'` reports `BUILD SUCCESSFUL in 5s`. iOS has captures instead -- `ScreenshotTests.swift:220` asserts the Continue button is hittable and `AppIconCapture.swift:64` photographs the chooser -- and both files sit in `apps/ios/UITests`, which the rule above excludes. Android measures nothing at all for the chooser: no Android test applies a font scale to `AppIconGroup`. So both largest-text scenarios are *asserted by nothing*. **Two scenarios are asserted on one side only.** *Following the system* is asserted on iOS by `PaletteTests.swift:99` and by nothing on Android: `Theme.kt:387` maps System to `isSystemInDarkTheme()`, and `AppearanceTest.kt:21` asserts the four entries with System first, `:30` and `:37` the OLED palette, without ever reading that line. *Reader theme is separate* is the mirror, and is scored once, here. Its opt-in clause is asserted on Android by `ReaderAppearanceTest.kt:74` over `ReaderAppearance.kt:57`, while iOS's `linkedPreset` at `LinkedPreset.swift:19`, called from `StoryArcApp.swift:276`, is named by no test in the repository. Its non-override clause is asserted on iOS by `SettingsStoreTests.swift:89` and on Android only by the instrumented `SettingsStoreTest.kt:79`. Each clause fails on one side, so the scenario is *asserted by nothing* and is counted there once. **Three more scenarios rested on Android files no gate runs, and they move down.** `core/persistence/src/androidTest/.../SettingsStoreTest.kt:47` is Android's only assertion that a chosen appearance persists, and `:95` its only assertion that a reset leaves sources, downloads and reading progress alone; the running `DynamicColourSettingTest.kt:29` beside it asserts what `AppSettings.Defaults` holds and nothing about the neighbouring stores. `feature/settings/src/androidTest/.../SettingsSemanticsTest.kt:201` is Android's only assertion that a group row carries its summary, against `SettingsGroup.kt:80`, which no `src/test` file names. `assembleDebugAndroidTest` still compiles all of it -- `node scripts/gradle.mjs :core:persistence:assembleDebugAndroidTest :feature:settings:assembleDebugAndroidTest` reports `BUILD SUCCESSFUL in 1s` -- so this is not the dead-suite case Android's instrumented `ProgressStoreTest` was. It is the weaker one: the code compiles, no gate runs it, and *Choosing appearance*, *Resetting* and *Structure* are therefore *asserted by nothing*. iOS runs its own halves, so each is one-sided rather than empty: `SettingsStoreTests.swift:36` for persistence, `:107` for the reset, and `StorageSummaryTests.swift:32` and `:45` for the downloads group's summary row. The group list itself is asserted on both, by `SettingsSearchTests.swift:17` and `SettingsSearchTest.kt:23`. **Four scenarios have no test anywhere on either platform, and they are the same four the earlier row named**: the privacy statement at `PrivacyGroup.kt:145` and `PrivacySettings.swift:51`, About's contents, the support link's exclusivity, and the acknowledgements list. A grep for `ko-fi` over both test trees returns nothing, and the only string the link is written in is `BuildInfo.swift:24` and `AboutGroup.kt:98`. *Clearing data* joins them for a different reason: the cache third now has a mirrored test a side, and the reading-history third, the downloads third and every space figure the scenario asks each row to state have none. **What changed in this version is the best-covered requirement here**: five of its six scenarios are built and tested on both, with the decision pinned by `WhatsNewTests.swift` and `WhatsNewTest.kt` and the wiring pinned by `WhatsNewWiringTests.swift:64` and `WhatsNewWiringTest.kt:46`, which read the shell's own source so that deleting the sheet cannot stay green. Reaching the log from About changes nothing about what is seen, and both platforms hold that structurally: `WhatsNew.onLaunch` is the only writer, and neither About screen hands the log a store. `AboutGroup.kt:45` passes `releases = WhatsNew.releases` and no store; `AboutSettings.swift:39` calls `WhatsNewHistory()` with no argument at all. `WhatsNewWiringTests.swift:110` and `WhatsNewWiringTest.kt:61` pin the load-bearing half by asserting that no other file in Settings reaches the store. **Not driven.** No screen in this capability was watched on a device or an emulator in this round; every verdict rests on the source, on the suites named above and on the two compile gates. |
| `localization` | partial | 12 scenarios: 1 built and tested on both, 10 built and asserted by nothing, 1 one-platform. **Re-audited scenario by scenario on 2026-09-06 against the merged tree, because the two audits before it read a commit that predates the four-language vocabulary work.** The row it replaces scored 4 built and tested, 5 asserted by nothing, 2 one-platform and 1 unclear. The `unclear` is retired. **The first take of this row was itself reviewed on 2026-09-06, and it was wrong in four places; this is the corrected take.** It scored eleven scenarios and claimed twelve, it took two of its aggregate counts with a pattern that matched more than it meant, it called a gated iOS test ungated, and it missed a live defect on the surface it was set to examine. The defect is fixed in this change, on both platforms, and the fix is described below. Nothing else here is a new measurement of the code: the citations were checked line by line in review and all of them hold. **None of this is a regression out of the vocabulary change.** The change fixed the defect it set out to fix and the fix holds; what it did not do was reach the second surface of the same defect on the other platform, and nothing had re-read the rest. **Following the system is built on both and asserted by neither, and the first take of this row scored it without reading it.** Each platform leaves the choice to its own resource loader when the reader has chosen nothing: `Locale.storyArc` answers `.autoupdatingCurrent` (`InterfaceLanguage.swift:41`), and `interfaceLocale` answers null, which `speaking()` turns into the context it was given unchanged (`InterfaceLanguage.kt:34`). Both loaders then walk the device's preferred-language order and fall back to the base catalogue, which is English on both. **No test on either platform asserts that order or that fallback.** Android's `InterfaceLanguageTest` asserts one clause of it — `no choice leaves the system language alone` — and iOS asserts nothing at all. A device listing Italian, then French, then German would have to get French, and nobody has watched it. **iOS's comic reader draws the raw Swift error to a reader, and Android's no longer does — that is half the answer to the question this round was set.** The other half is the EPUB reader, which lost the chosen language on both platforms and is repaired below. `feature/reader` *is* the comic reader, and it is fixed: `ReaderViewModel.kt:241` holds the failure as a string-resource id rather than a sentence, `:437` and `:495` set `R.string.reader_cannot_open`, and `ReaderScreen.kt:228` draws `stringResource(failure!!)`. `ReaderFailureSaysNothingInternalTest.kt` guards it by reading the view model's own source and failing any `_failure.value =` line that does not name a resource; its header records what the defect was and what the fix costs, which is that the cause of a refusal is now known to nobody. iOS never had that fix. `ReaderModel.swift:286` and `:325` both assign `failure = String(describing: error)`, `ReaderView.swift:266` hands it to `ReaderFailure`, and `ReaderPage.swift:80` draws it as `Text(message)` over a `String` — which does not localise. What a French reader meets when an archive will not open is a Swift value description. **There is no iOS key for it to have used**: Android's `reader_empty` and `reader_cannot_open` both exist in four languages (`feature/reader/.../values/strings.xml:40,51`), and iOS's catalogue defines `reader.empty` and nothing for the open failure. That is a broken mirror on the one sentence a reader sees when a book refuses to open, and no platform difference forces it. **`pnpm strings:drawn` cannot see it, by construction.** The leak reaches the view through a variable, which is the first limit its own header names. **The EPUB reader lost the chosen language on both platforms, and this change gives it back.** Neither platform drew an exception here; both resolved a correct key against the wrong locale, which no gate could see because every key exists in all four languages. On iOS, `EpubReaderOpening.swift:30,49,52,93` built the refusal with `String(localized:)` and handed it no `locale:`, so Foundation resolved it against the device. Seven more iOS call sites did the same — `PlayerCentre.swift`, `CoverCell.swift`, `CoverList.swift`, `CatalogueBrowser.swift`, `ThemeAxesSheet.swift`, `PageColourSection.swift` and `ThemePreview.swift` — and one of them put two languages inside one VoiceOver utterance, because `CoverCell` passed the locale for the page count and not for the progress. All eleven now pass `locale: .storyArc`. On Android, `EpubReaderViewModel` resolved its three refusals with `application.getString`. The override reaches an activity and not the `Application`: `speaking()` builds the context and `EpubReaderActivity.kt:315-316` hands it to `attachBaseContext`, while `StoryArcApplication` overrides nothing and no call to `Locale.setDefault` or `setApplicationLocales` exists in the tree. The view model now holds a string-resource id, as `:feature:reader` already did, and `EpubChrome` resolves it with `stringResource`. **Both halves are gated, and neither is watched.** `scripts/ios-strings.mjs` gained a fourth check, `UNLOCALISED`, which reports any `String(localized:)` handed no locale; it named all eleven sites before the fix and names none after. `EpubFailureSpeaksTheReadersLanguageTest` reads the Android view model's source and refuses an `application.getString` on a failure line, as `ReaderFailureSaysNothingInternalTest` does for the comic reader. Both are source guards. Neither proves a French reader saw French. **Two more sentences reach a reader from iOS source, and two paths on each platform put an unowned string inside a translated one.** `StorageUsage.swift:107-115` composes `"0 kB"`, `"1 kB"` and `"\(bytes / 1024) kB"` by hand and hard-codes the SI names for MB and GB; a French file manager writes `Mo`, and so does the app's *other* byte formatter, `DownloadStore.swift:201`, which goes through `.byteCount(style: .file)`. Two spellings of one number on one platform, and the wrong one is drawn on Privacy (`PrivacySettings.swift:58,86`), on the metered-download confirmation (`MeteredConfirmation.swift:70`) and in the SMB browser (`SmbBrowserView.swift:167`). Android has two entry points and both are the platform's: `Formatter.formatShortFileSize` at eight call sites and `Formatter.formatFileSize` at five, thirteen across eleven files, each taking the activity's own context. Both localise the unit, so Android is correct here; it is the count and the word "one" that the first take of this row got wrong. `StorageUsageTests.swift:26-28` pins `formattedBytes(0) == "0 kB"`, so a test holds the defect in place. The unowned-string paths mirror exactly: `CatalogueMessages.kt:40` and `CatalogueMessages.swift:52` fall through to `error.message` and `error.localizedDescription` for any transport failure that is not a known host or a timeout, and both `describe` functions interpolate a parser's own reason into `catalogue_error_malformed` / `catalogue.error.malformed`, where the reason is `"invalid XML"` or `"invalid JSON"` from `OpdsAtom` and `OpdsJson`. Both are reached from a reader's action — adding a catalogue, browsing one, and a download that fails (`CatalogueConnection.kt:163,283,285`, `CatalogueBrowser.kt:221,223`, `DownloadQueue.kt:543,546`). Foundation localises `localizedDescription` into the *system* language, so iOS is the milder of the two; Android's is English whatever the reader chose. **The language override changes words and not numbers, sizes or dates, and that claim needed re-taking rather than repeating.** The discarded audit said `Locale.storyArc` was read in one place. It is read at 128 lines across 47 shipped files after this change, counted with a word boundary. **The first take of this row said 149 across 57, and that figure counted four view modifiers whose names merely begin the same way** — `.storyArcGlassText`, `.storyArcGlass`, `.storyArcTheme` and `.storyArcFeedback` — so it overstated the reach by a quarter. The words are covered as far as a source gate can show: `speaking()` sets `environment(\.locale, .storyArc)` (`BackgroundDownloads.swift:30-33`) at five scenes (`StoryArcApp.swift:185,216,225,283,310`), and every `String(localized:)` is now handed `locale: .storyArc`, which `strings:ios` now refuses in `pnpm lint`. Formatting is where it stops. Of 32 `.formatted(` call sites in shipped iOS source, two carry `.locale(.storyArc)` — `HomeFinished.swift:53` and `SearchResultsView.swift:257` — one pins `.locale(.current)` on purpose (`StorageUsage.swift:120`), and the remaining 29 take `Locale.current`, which is the device's. A reader who sets the app to French on a German phone gets French words around German dates and German byte sizes. Android does not have this shape **in its formatting**: `attachBaseContext` (`MainActivity.kt:111-113`, `EpubReaderActivity.kt:315-316`) overrides the context, and `Formatter`, `DateUtils.formatDateTime` and `DateFormat.getDateInstance(MEDIUM, LocalConfiguration.current.locales[0])` all read it. Two Android statics escape it for the same reason iOS's do — `DateUtils.getRelativeTimeSpanString` (`LibraryStates.kt:50`) and `NumberFormat.getInstance()` (`ThemeSheetParts.kt:183`, `PageColourSection.kt:330`) — so *Dates and times* is a mirrored miss and not a one-sided one. Nothing on either platform asserts a drawn date at all: no test in either tree names `DateFormatter`, `formatted(date:`, `DateUtils` or `getDateInstance`. **iOS still says "Failed after 1 attempts", and a fix is in flight on a branch this worktree cannot see.** `App/Resources/Localizable.xcstrings:75` stores `downloads.failed` as a flat string in all four languages, and `DownloadQueueSection.swift:204` draws it with a live count; `offline-downloads` caps attempts at three, so one is a case a reader reaches. Android had the same row and does it properly: `R.plurals.downloads_failed` at `DownloadsParts.kt:296`, whose English entry carries the comment explaining exactly this. The platforms hold 17 and 25 plural entries; the eight Android has that iOS does not are in `:app` and `:core:playback`, against zero plural keys in iOS's `App`, `PlayerFeature`, `Playback` and `EpubReaderFeature`. Only Android asserts a plural form — `BulkSelectionChromeTest.kt:93,102` renders 0, 1 and 5 through `pluralStringResource`, while iOS's namesake only checks that the source mentions the key. `feature/settings/.../values/strings.xml:155` still declares a flat `downloads_failed` saying "%2$d attempts" that nothing calls; it is dead, and it is the wording the plural replaced. **Alphabetical ordering ignores the chosen interface language on both platforms, and nobody is fixing it.** `LibraryIndex.swift:47` defaults `locale` to `.current` and `LibraryIndex.kt:52` defaults it to `Locale.getDefault()`; the four call sites that matter pass nothing — `LibraryModel.swift:337-338`, `ListOrder.swift:93`, `LibraryViewModel.kt:1329-1330`, `ListOrder.kt:121`. The ordering is therefore correct until a reader overrides the language and silently wrong afterwards, on both. The collation and the article rules underneath are sound and mirrored: the article tables are identical set for set, and both suites assert English, Spanish and French cases (`LibraryIndexTests.swift:67,72`, `LibraryIndexTest.kt:59-72`). Every one of those tests passes the locale explicitly, which is why none of them can see the call site. **Long translations lose the `unclear` and become the capability's one `built and tested on both`, though the two halves are not equally proved.** Android now has seven Robolectric suites that redraw a surface under `de-rDE`, `es-rES` and `fr-rFR` at 320 dp: `DownloadLimitWrapTest`, `ListOrderChipsWrapTest`, `ScopeChipsWrapTest`, `SourceRemovalFooterTest`, `SourceProgressNoteTest`, `SkipReasonWordsTest` and `KavitaCardFactsLocaleTest`. They assert bounds, not presence — `DownloadLimitWrapTest.kt:131-149` rejects a label measured to no width, one starting inside the gutter, one ending past the window and one drawn taller than it is wide — and they run in `pnpm test:android` and in CI. iOS's half is one UI test: `SweepDownloads.swift:103-113` launches in German at `AccessibilityXXXL`, asserts that *Download entfernen* is hittable, and commits the frame at `docs/designs/screenshots/downloads-retry-2026-09-05/ios-downloads-failed-ax5-de.png`. **The first take of this row called that walk ungated, and it is gated.** `project.yml:213-217` puts every file under `UITests/` in the `StoryArcUITests` target, and `ios.yml:104-111` runs `xcodebuild test … -only-testing:StoryArcUITests` on every push to main and on every pull request touching `apps/ios/**`. The workflow names no `-skip-testing` and `apps/ios` holds no test plan, so the German walk runs there, and `scrollToFailedRow` ends in an `XCTAssertNotNil` on a hittable *Download entfernen*. Seven gated Android wrap suites against one gated iOS hittability assertion at AX5 in German is the only `built and tested on both` this capability has. It is also the thinnest one: iOS asserts that one control survives one size in one language, where Android measures four bounds on seven surfaces in three. **The pseudo-locale walk reaches two of its ten routes and runs in no gate at all.** `scripts/pseudo-locale.mjs:174-184` lists Home, Library and eight Settings screens; its own KNOWN GAP note, dated 2026-08-31, records that `gear` taps a clickable node and the overflow menu never opens, so every Settings route fails one hop in. The row this replaces said nine routes pass, and the script says otherwise. It is Android-only, no route is a reader, and `pseudo:android` appears in neither `pnpm lint` nor `pnpm check` nor any workflow. **`strings:drawn` changed the gate picture on 2026-09-06 and `strings:ios` did not.** `strings:drawn` runs in `pnpm lint` (`package.json:34`) *and* in CI, with its own self-test beside it (`contract.yml:92,96`). `strings:ios` still runs only in `pnpm lint`, and `strings:ios:check` runs nowhere. Both are green here: every key resolves in en, fr, de and es, and no bare sentence sits in a drawing position under `apps/`. Android's completeness is the platform's: `MissingTranslation` under `./gradlew lint`, which CI runs (`android.yml:34`) and which `:app` hardens with `warningsAsErrors` and `abortOnError` (`app/build.gradle.kts:51-55`). Six Android modules carry strings now rather than five, and de, es and fr define every key all six declare, with none left over. The catalogues are 821 keys on iOS against 830 on Android. **The app has never run under a right-to-left system language.** `scripts/pseudo-locale.mjs:40` sets `PSEUDO` to `en-XA` and names `ar-XB` nowhere, and the walk is Android-only. What exists is a declaration on one side and a default on the other: `AndroidManifest.xml:20` sets `supportsRtl="true"` and `InterfaceLanguage.kt:65` notes that `setLocale` carries the layout direction, while iOS holds no `layoutDirection` line anywhere in `App` or `Packages` and relies on SwiftUI mirroring by itself. The scenario's second half is the sound one: reading direction comes from the publication and never from the interface (`ReadingDirection.inferred`), asserted on both (`IdentityAndSourceTests.swift:170-171`, `ProgressMergeTest.kt:308-309`). **Content language is right on both and asserted on one.** `displayTitle` is a stored string drawn verbatim — `DetailHero.swift:108` uses `Text(_: String)`, which does not look anything up, and `DetailHero.kt:205` passes it straight to `Text` — and no machine-translation call exists in either tree. Server labels behave the same way; only Android proves it, at `KavitaCardFactsLocaleTest.kt:58-73`, which redraws a Kavita card in four locales and asserts that the frame changes language while the age rating keeps ComicInfo's own vocabulary. iOS's `KavitaCardFactsTests` measures heights and never reads a word. **What this round did not do**: it opened no emulator and no simulator, so the reader in French, the recreated activity and the right-to-left interface are all credited to source and to nobody's eyes. The EPUB repair is in that class too — a source gate on each platform proves the locale is handed over and the id is resolved in the composition, and nobody has watched a French reader open a damaged book. **What the review of the first take changed**: one verdict up (*Long translations*), one verdict added (*Following the system*), two aggregate counts corrected, and a product defect found and fixed on both platforms |
| `native-experience` | partial | 44 scenarios: 18 built and tested on both, 15 built and asserted by nothing, 8 one-platform, 3 missing on both. **Re-audited on 2026-09-06, and only in part — the sixteen scenarios that had never been audited were read against the source on both platforms, and the twenty-eight this row already counted were carried forward unread.** Everything from the next bold sentence to the end of this row describes the carried twenty-eight and was written before 2026-09-06. **The sixteen re-audited: eleven built and tested on both, five one-platform.** *Corrected on 2026-09-06.* The first version of this sentence said twelve and four. The evidence below names eleven built-and-tested scenarios and five one-platform scenarios. No twelfth built-and-tested scenario is named anywhere in this row. The count follows the evidence, and the header table follows the count. *The five app-icon scenarios.* `The platform is the only record` is the tightest mirror in this file: `AppIconChoiceIsNotRecordedTests.swift:94-157` and `AppIconChoiceIsNotRecordedTest.kt:97-162` are six cases each, matched name for name, and each walks the trees for a store, a preference key, a backup entry, a log line and a diagnostic field that could hold a second record. `Surviving a reinstall or a restore` reads the platform rather than a memory on both — `AppIconStoreTests.swift:56` and `:66`, `AppIconSwitcherTest.kt:49` and `:54`, `AppIconAliasesTest.kt:183`. `The platform refuses the change` names the face still in use and asks once (`AppIconStoreTests.swift:104` and `:118`, `AppIconChooserAnnouncementTests.swift:207`; `AppIconRefusalNamesNoFaceTest.kt:101`, `:123` and `:138`, `AppIconSwitcherTest.kt:87`, `:97` and `:120`). **Two of the five are one-platform, in opposite directions, and both are honest divergences rather than oversights.** `The platform withdraws the ability` is iOS's: `AppIconStore.swift:62` reads `supportsAlternateIcons` once at init (`:116` says why once), the chooser draws no rows and says why (`AppIconChooserAnnouncementTests.swift:198`, `AppIconStoreTests.swift:186` and `:201`). Android's chooser has no such state — no branch in `AppIconGroup.kt`, and `strings.xml:197-206` holds five face names, a note, and two refusal strings with nothing for *this device offers no choice*. Whether an Android device can lose the ability at all is not settled from source, so this is recorded as one-platform rather than as a gap on Android. `The system draws its own tinted icon` is Android's, and it is the sharper of the two: every face's adaptive icon carries `<monochrome android:drawable="@drawable/ic_launcher_monochrome" />` (`ic_launcher.xml:8` and its four siblings), asserted face by face at `AppIconManifestTest.kt:184-194` with the reason stated — "a gradient tinted flat loses the" divisions. On iOS **no appiconset carries a tinted or dark appearance at all**: all five `AppIcon*.appiconset/Contents.json` hold exactly one image with an empty `appearances` list, so what iOS 18 tints is the full-colour art and the clause about shipping real single-colour art is unmet there. *The six reader-chrome and theme-sheet scenarios.* Two of them, `iOS reader chrome` and `Android reader chrome`, name one platform in the scenario itself and are one-platform by construction, the way `local-library` splits its folder scenarios — iOS's Liquid Glass grouping is asserted at `ReaderChromeTests.swift:174-204`, Android's tonal capsule at `ReaderChromeColoursTest.kt:55`, `:78` and `:94`. `The preset grid on both platforms` is three columns on both at ordinary text sizes — iOS `ThemeSheet.swift:143-147` with `ThemePresetGridTests.swift:43-57` asserting three at every ordinary size and one at every accessibility size, Android `ThemeSheetParts.kt:164` (`COLUMNS = 3`) drawn at `ThemeSheet.kt:164-168`. Android does not assert its column count; it asserts that both levels survive the largest text size (`ThemeSheetTest.kt:300`). `Opening the sheet` and `Applying a change with the sheet open` are built and asserted on both (`ThemeSheetTests.swift:53-272`, `ThemeSheetTest.kt:56-300`), and the page behind the sheet updating live is the half the `reading-themes` row already records as built. **The fifth one-platform is `Theme sheet on a large screen`, and iOS is the side that has it.** `EpubReaderView.swift:146-163` is a `.popover` with `.presentationCompactAdaptation(.sheet)` and a comment quoting the requirement; the platform collapses it to a sheet on a phone, which is the scenario in one modifier. Android's theme sheet is a `ModalBottomSheet` at `ThemeSheet.kt:216-223` and nothing in `feature/epubreader` reads a window width class — no `WindowWidthSizeClass`, no width branch anywhere in the module — so a tablet gets the same full-width sheet a phone does. *The five selection-chrome scenarios are the strongest group of the sixteen: all five built and tested on both.* `The mode replaces its surface` is the tab bar down for exactly the duration of the chrome on iOS (`BulkSelectionChromeTests.swift:57`, `:176`) and one top bar rather than two on Android (`BulkSelectionChromeTest.kt:490`, `:577`). `How many are chosen, and how to leave` puts the count in the title and the way out beside it, never in the actions (`BulkSelectionChromeTests.swift:111` and `:135`, `LibrarySelectionTests.swift:26` and `:56`; `BulkSelectionChromeTest.kt:92` and `:437`). `Nothing chosen yet` has the same test name on both sides — *the actions are inert at nought picked and live above it* — at `BulkSelectionChromeTests.swift:293` and `BulkSelectionChromeTest.kt:388`, with the drawn-inert half at `:326` and `:146`. `Each platform's own form` is met in the second clause too, which asks each platform's source to say which convention it follows: `BulkActionBar.swift:25-31` names Photos, Files and Mail on iOS 26 and lists the three parts, `LibrarySelectionTopBar.kt:36-47` names Material 3 Expressive's contextual top app bar. `Every action names itself` is `BulkSelectionNamesTests.swift:60-85` against `BulkSelectionChromeTest.kt:263` and `:308`, the latter holding the clause about an action that cannot be a glyph moving into a named overflow. **None of the sixteen was watched running.** Every verdict above is source and unit tests; the icon chooser in particular is a screen whose whole subject is what a launcher draws, and no launcher was looked at. **Three things this row used to list as missing are built.** Haptics are wired on both, with a deliberately two-word vocabulary -- completion and refusal -- through `SensoryFeedback` on iOS and `HapticFeedbackConstants.CONFIRM`/`REJECT` on Android, called from both comic readers. Increase Contrast is built and asserted on both, including a live contrast listener on Android. The cover-derived accent is derived, contrast-floor-adjusted and asserted on both. It reached exactly one screen -- the end-of-publication screen in each reader -- for as long as the publication's page was unreachable. **On iOS it now reaches two**: the page sets `Theme.coverAccent` from its own wash, and the page has callers as of 2026-08-31. On Android it is still the one screen. **A wide window gets the platform's own split navigation on both**, and the iOS half of that sentence was wrong until 2026-09-05: there was no `NavigationSplitView` anywhere in the app, and deliberately so. `AppShell` is a `TabView` with `.tabViewStyle(.sidebarAdaptable)`, which the platform draws as a tab bar on a phone and as a sidebar on an iPad from one destination set -- `LibraryView` records why a split view inside one of those destinations would be a second, disagreeing navigation. The consequence, found while settling `publication-detail` task 4.3, was that iOS had **no second pane**, so the delta's "one sentence before a publication is chosen" had nowhere to be shown and `PublicationDetailPlaceholder` was deleted rather than wired. **It has one now, and the shell is still the tab bar.** The Library destination -- and only that destination -- is composed as a two-column `NavigationSplitView` inside its tab (`LibraryPanes.swift`), which is what the requirement asks of a page rather than of a destination set: this row's own *The two platforms reach the same destinations differently* says iOS presents the set "as a tab bar that adapts into a sidebar in a wide window", so the split had to go inside a destination and not around them. The placeholder and its `detail.empty` string are restored with the pane, word for word what Android's `detail_pane_empty` says in all four locales. Home, Downloads, Search and the sidebar's own rows still push; that inconsistency is real and named on `publication-detail` task 4.1. **Not watched**: there was no simulator in that pass, and the phone half -- a two-column split collapsing into one stack -- is the part most likely to be wrong in a way only a device shows. So: a sidebar on iOS, a Material 3 navigation rail on Android, holding the library, every catalogue, server and share, the collections and settings -- the same places a narrow window keeps in a strip and two toolbar icons. The choice is a window width and nothing else: one number, 600, mirrored on both platforms and unit-tested there, so a Split View drag, a rotation, an Android multi-window resize and a fold are one event with one answer. That is also the whole of the foldable requirement bar the hinge: the activity already declares the fold a configuration change it handles, so nothing is recreated and nothing is lost, and the test exists precisely to keep a posture branch from creeping in. **The long-press menu reaches every cover**: it was on the grid and missing from the list layout on both platforms, and from Android's Continue reading row, so marking a publication read was a thing you could only do in one layout. **Scroll edge effects are declared where the app floats its own chrome**: iOS carries the catalogue strip and the library's notices as `safeAreaBar` with a soft edge and puts the strip on Liquid Glass with the Reduce Transparency fallback it had been missing; Android's top bar takes its scrolled container colour as the shelf passes under it, and the stretch at the end of a scroll was already Compose's own. **Quick actions and continuity are built on both.** The menu is three entries and the list is decided once, in `QuickActions`, mirrored case for case and pinned by ten assertions a side: the publication in progress named first because it is why anyone holds an icon down, the library always because it is the one destination a fresh install has, and downloads only once there is something in them -- an entry that opens onto an empty screen is a promise the app cannot keep. A publication with a whitespace-only title is refused, which `offline-downloads` has already met once. The entries survive the app being killed because the *system* stores them, which is also why the entry carries a stable identifier rather than a path: a menu can outlive the scan that last placed the file, and on Android a `content://` grant does not outlive the process it was given to. Both platforms then have to wait -- a quick action lands on a cold start with an empty shelf -- so both poll the library for five seconds and then give up and land on the library, rather than throwing the reader into a book minutes after they asked. Continuity diverges on purpose, because the platforms do: iOS publishes one `NSUserActivity` doing two jobs, handoff and Spotlight, because they describe the same fact and two would drift, and it carries an identifier so the receiving device looks in its own library and a book it does not have is a book it does not open; Android has no Handoff and there is no backend to invent one with, so the mirror is `pushDynamicShortcut` when a publication is opened -- published *and* reported as used, which is what lets the launcher rank it and the Assistant answer for it. Neither carries a reading position across devices. iOS needs a scene delegate installed through `configurationForConnecting`, because SwiftUI surfaces neither the cold-launch shortcut nor `performActionFor`. **None of the last three paragraphs has been watched working.** There was no simulator and no emulator in those rounds, so the sidebar, the rail, the glass strip, the edge effects, the quick-action menu, its icons, the Spotlight entry and the handoff banner have been compiled and reasoned about and not seen -- and the scene-delegate route in particular is the half most likely to be wrong in a way only a device would show. **Three things were missing on both, and only the first was on this row before; one of the three, the in-app predictive-back preview, has since been built on Android, and the counts for the carried twenty-eight are still left where they are — no longer because the header table cannot move, since it is now re-derived from the rows on 2026-09-06, but because re-scoring that scenario means re-reading it against the code, and the 2026-09-06 pass read only the sixteen that had never been audited.** A fourth, the Material You opt-out, was built on 2026-08-31 and is described below. *Widgets*, deferred deliberately by [ADR-0011](../decisions/0011-home-screen-widgets.md), which names the two blockers: a shared reading snapshot a second process can read, and an iOS signing team to provision the App Group that would hold it. *The in-app predictive-back preview.* ~~Missing.~~ **Built on 2026-08-31.** The manifest opt-in was already set, so the *system* drew the app-level preview, and `BackPreview`/`backPreview()` were implemented and unit-tested; `PredictiveBackHost`, the only thing that applies the shrink-and-round transform, **had zero call sites anywhere in `apps/android`**. It now has two. The first wraps `SettingsScreen`, which is where the app's only two `PredictiveBack` handlers already were and where, with no host above them, the callback fired and nothing was drawn -- the KDoc at `PredictiveBack.kt` says exactly that. The second wraps the shell's navigation area, where the app's one consolidated back rule lives: what was a plain `androidx.activity.compose.BackHandler` at `AppShell.kt:83`, with no progress and no preview, is a `PredictiveBack` handler inside a host. Two hosts and not one, because nesting is the arrangement rather than a conflict -- the composition local is static, so Settings' inner host owns the gesture through its three levels and the outer one owns every other in-app destination, and each applies its transform exactly once. The host sits inside the navigation area rather than around the whole shell: back pops a screen off the current destination's path or returns to the start destination, so the screen is what leaves and the bar or rail is what stays. `PredictiveBack.kt`'s KDoc still says "installed once so far, around `SettingsScreen`" -- it is in `:core:designsystem`, which the slice that added the second host did not own, and it is one sentence to sweep. This has **not been watched on a device**: the transform is unit-tested as a value and the composition that applies it is reachable by no test in this repository, so what a reader sees is unproven. **The Material You opt-out is built, on Android only, and counted as one platform rather than as missing.** The scenario requires "a setting to use the StoryArc palette instead"; `useDynamicColor` was a hardcoded `true` at both production call sites with no `AppSettings` field and no row in `AppearanceGroup`, so the only route back to StoryArc's own palette was OLED Dark -- a different setting meaning a different thing. There is now a field defaulting to true, a switch in the Appearance group in all four languages, and both `MainActivity` and `EpubReaderActivity` read it. The switch agrees with OLED Dark rather than fighting it: `StoryArcTheme` has always let true black win over the wallpaper, so under OLED Dark the switch is disabled and its note says why, and the reader's own answer is kept for when they leave it. One platform rather than a gap, because iOS has no dynamic colour to opt out of -- hence no `SettingsAnchor` counterpart there either. **Not yet photographed**: the palette this restores has not been seen on an emulator. *The library's list fallback at accessibility text sizes.* **The navigation bar's own half of that requirement is settled and the library's is not.** At `font_scale 2.0` "Downloads" filled its third of a 360 dp window edge to edge and sat against the display boundary -- `after-2026-08-31/android-shelf-caption-scale2-light.png` is the evidence -- and French's "Téléchargements" is six characters longer again. Material's own navigation bar does not scale its labels with the system font size at all (`NavigationBarItemView` removes it; `labelFontScalingEnabled` defaults to off), which is why every stock Material app draws small navigation labels at a large font scale. That rule is now ported into `AdaptiveNavigation`, because the Compose `ShortNavigationBarItem` has no equivalent -- the label is the caller's composable -- and scoped to the bar and the collapsed rail, which are the two controls that ration a fixed width between a fixed number of destinations. The expanded rail keeps the reader's text size, because it has room and is the only control that draws the secondary entries. The three ways out are all closed by somebody: Material says a label does "not truncate or wrap" and must not shrink to fit, and this capability says a destination is never an unlabelled icon. It is a deliberate exception to `design.md`'s "all scale with the Android font scale", scoped to the eight words in the navigation control, and TalkBack is unaffected because the label is still the item's name. **The Downloads shelf was a third instance of the same defect and is fixed.** Its `coverMinimum()` held `design.md` §4's three tiers and ignored the font scale entirely, so at an accessibility text size it kept 104 dp columns while the library shelf widened to 146 -- two shelves in one app laying out differently. iOS had the identical divergence in `OnDeviceShelf`, where Apple's audit reported it as five clipped labels. **The rule now has three implementations and that is the standing finding**: `:feature:library` owns `coverMinimumWidth`, `coverMaximumWidth` and `BoundedAdaptive` and all three are `internal` to it, so `:app` cannot call them; they belong in `:core:designsystem`, which both modules already depend on. Three test files now assert the same table so the copies cannot drift silently. **The filter chip row above the shelf is the same defect unfixed**: at the same font scale it runs off the window with no affordance that it scrolls, and it belongs to `:feature:library`. The library's own grid-to-list fallback is still absent on both platforms: Nothing on either platform reads the text size to choose a layout -- grid-versus-list is a stored reader preference, and a grep for `dynamicTypeSize`, `isAccessibilitySize` and `fontScale` across both trees returns four hits, none of them in the library. The one accessibility-size adaptation that does exist is the reader's fit picker becoming a menu on iOS, with no Android counterpart. **Three more clauses are absent on both and belong on this list**: a page's focal area may still fall across a foldable's hinge, and nothing reads a hinge rectangle -- no `androidx.window` dependency, no `WindowInfoTracker`, no `FoldingFeature`, only two prose comments; no key toggles the reader chrome, though page turns are bound on both; and `accessibilityAction`/`CustomAccessibilityAction` appear nowhere in either tree, so a VoiceOver or TalkBack user gets only the pager's own scroll. **Every performance number in this capability is unmeasured.** There is no benchmark harness of any kind in this repository -- no launch budget, no memory budget, nothing that would fail if either were blown. **The visual-proof requirement is the sharpest gap, because this file is the evidence for it.** "The project's device matrix" is named nowhere in the repository; `AGENTS.md` specifies light and dark at the default and the largest text size and no devices. There is no reference to compare a capture against and no comparison harness: no baseline directory, no image-diff script, nothing in `package.json`. Thirty-five PNGs are committed under `docs/designs/screenshots` and thirty-two of them are Android settings screens. Two device-driven Android scripts exist because a `@Preview` once lied -- `smoke-android.mjs` walks every route on a connected device and asks logcat whether the process died, and `a11y-scan.mjs` reads the real accessibility tree at real density -- and iOS has one simulator-bound UI test, scoped to the library and currently `XCTExpectFailure`d, and nothing that walks routes. Neither platform *gates* anything: no check refuses a change whose only evidence is a preview. The both-appearances matrix is Android's alone: eight settings screens at the full light/dark x default/largest grid, against three iOS screenshots in total, none at the largest text size -- and `AccessibilityAuditTests.launch(contentSize:)` exists precisely to force one and no test in the file passes it **The brand accent is one colour on both platforms, and the icon a reader chose survives a reinstall** (`brand-identity-and-app-icons`, archived 2026-09-05): `brand.accent` is the only chrome accent, the arc stops are identity only and guarded from chrome on both sides, and the themed Android icon collapses the five faces by design. `named-failures-and-quieter-chrome` (archived the same day) added the selection chrome rule: a mode replaces its surface and shows its actions inert before a first pick. The main spec holds 44 scenarios; the sixteen this sentence used to exclude were re-audited on 2026-09-06 and are described at the head of this row. **Two more findings of the 2026-09-05 design review closed on 2026-09-06.** The navigation bar's selected label follows the accent on every scheme (it was the pink pole over the violet pill: `accentedItemColours` in `AdaptiveNavigation.kt`, pinned by `AccentReachesTheControlsTest`, frames in `android-nav-label-2026-09-06`), and the library's icon buttons read the scheme's primary instead of a hand-tinted brand accent, so under Material You they take the wallpaper's colour beside the text buttons that already did (`BulkSelectionChromeTest`, frames in `android-library-chrome-2026-09-06`). |
| `sources` | partial | 16 scenarios: 13 built and tested on both, 2 built and asserted by nothing, 1 one-platform. **Re-audited against the source on 2026-09-06, and the row it replaces was wrong in four places.** It also scored "12 built on both", which is not one of the four verdicts this page uses; the counts above are in that vocabulary. **The retry policy is complete on both platforms.** *Retry policy* grants an immediate retry when the device regains connectivity and when the app returns to the foreground. The old row said neither platform did either, "because there is no reachability observer wired to the probe on either side". Both are wired. `SourceRetryTriggers.swift:29-67` collects `SourceReachability.triggers` and watches `scenePhase`, and `LibraryView.swift:325` applies it; `SourceRetryTriggers.kt:37-64` collects the same flow and answers `ON_RESUME`, and `AppDestinations.kt:85` calls it. `SourceReachability` decides whether an occasion earns a probe, and sixteen mirrored cases assert it on each side (`SourceReachabilityTests.swift`, `SourceReachabilityTest.kt`). The backoff beside it is still five seconds doubling to a five-minute cap (`SourceProbeTests.swift:14-33`, `SourceProbeTest.kt:19-36`). iOS landed in `7a0ced4b` on 2026-09-03 and Android in `c1a484d5` on 2026-09-05, both after the old row was written. A reader who is reading is still left alone, which *Automatic recovery* requires. **Both test gaps the old row named are closed.** Android asserts the source-store round trip now: `SourceStoreTest.kt` holds nine cases and arrived in `8ed5a941` on 2026-09-02, where the row said "there is no `SourceStoreTest` at all". The cached notice, the incremental refresh and the disappearance removal are asserted in *both* library feature modules, eight mirrored cases each, from `66bd12b8` on 2026-09-05 -- `LibraryShelfLifecycleTests.swift:84,113,139,248,284` and `ShelfLifecycleTest.kt:112,132,152,233,264`. **The empty state is the one scenario that is half unbuilt, and the four explanations it wants are dead code on both platforms.** *Adding the first source* asks for an empty state "naming the four source types with a one-line explanation of each". The explanations exist and are translated into four languages, and nothing draws them: `SourceKind.explanationKey` (`SourcePresentation.swift:31-38`) and `SourceKind.explanationRes` (`SourcePresentation.kt:39-45`) have no caller anywhere. Nor do `titleKey`/`titleRes`, `symbolName`/`icon` or `statusKey`/`statusRes` beside them -- each settings screen carries its own copy of the same maps instead (`SourcesSettings.swift:186`, `SourcesGroup.kt:318-330`). What a reader meets is a title, a sentence and two actions (`LibraryStates.swift:62-102`, `LibraryStates.kt:86-128`), and `AddSourceMenu` names the four kinds by short title alone (`AddSourceMenu.swift:25-29`, `AddSourceMenu.kt:55-94`). The second clause -- open a file or a folder while configuring nothing -- is built on both, and asserted by neither. No test on either platform composes the empty state: `EmptyLibraryView` and `EmptyLibrary` have one caller each, both in production (`LibraryContent.swift:176`, `LibraryScreen.kt:555`). `SearchAtRestTest.kt:86-102` composes `SearchSuggestions`, which is the navigation-shell page and shares only its string resources with this one (`SearchAtRest.kt:203`). iOS's only proof is `apps/ios/UITests/SweepEmpty.swift:27`, **which no gate runs**. **A pull to refresh re-asks the servers on iOS and only re-walks the folders on Android.** `LibraryView.swift:337-339` calls `resolveSources` and then `rescan`. `LibraryScreen.kt:494` passes `onRefresh = { viewModel?.rescan() }` and nothing else, so an Android reader who pulls on a populated shelf gets a folder walk and no catalogue fetch. Android does ask its sources on entry (`LibraryScreen.kt:278`) and from the away screen (`:578`), which is why the gap reads as covered. That is the one-platform verdict, and it is a defect rather than a decision. **The second scenario asserted by nothing is the offline notice.** The screen that says a server cannot be reached is drawn on both -- `OfflineSource.swift:23`, reached from `SourceBrowser.swift:40`, and `OfflineSourceScreen.kt`, reached from `AppShell.kt:211` through `AppScreens.kt:112` -- and no test on either platform names it, and no frame photographs it. `apps/ios/UITests/SweepSources.swift:91` shoots `source-unreachable-detail`, which is the settings source-detail page (`SourceDetail`), a different screen from this one. **Still true from before, and re-checked line by line**: removal deletes the stored secret first and unconditionally (`LibrarySources.swift:186-215`) and the downloads before the registry entry (`StoryArcAppActions.swift:327`, `SettingsHost.kt:39-45`); the confirmation states what it frees through `SourceRemovalWording`, asserted on both (`SourceDetailSizeTests.swift:184,199`, `SourceRemovalDialogTest.kt`, `SourceRemovalFooterTest.kt`); a source's detail screen shows the state, the last successful sync, the last error in plain words, the item count and the bytes, and offers the five actions (`SourceDetail.swift:34`, `SourceDetailScreen.kt:142-175`); a diagnostic is redacted with fifteen mirrored cases per platform; the metadata cache, the cover cache and the thirty-day progress tombstone all hold; and reordering is a drag on iOS and two buttons on Android, which divergence 4 records. **Two proofs live in a suite no gate runs.** Android's `CredentialStoreTest.kt` and `CoverCacheInstrumentedTest.kt` are instrumented, so *Storing a secret*, *Reading a secret* and *Cover caching* rest on iOS's gated suites plus an Android suite that only compiles. It does compile: `pnpm build:android:tests` reported `BUILD SUCCESSFUL in 11s` on 2026-09-06. **Nothing in this row was watched happening.** |
| `offline-downloads` | partial | 19 scenarios: 7 built and tested on both, 10 built and asserted by nothing, 1 one-platform, 1 missing on both. *Wi-Fi only* moved into the first group on 2026-09-06: both its clauses are asserted on both platforms and the sentence it requires is drawn. Six of those eleven are partials -- built on both with a clause of their own scenario absent on both -- and they are named below, because "untested" flatters them. **A download is written where a removal looks for it, on both platforms.** The store named a file after the publication and the Settings screen deleted the one named after the identifier, so removing a downloaded publication dropped the record, left the bytes, and the storage total on that same screen never went down. The optional name a caller could leave out is gone: the store takes the record, or the three things that determine the path, and there is no spelling that omits the title. A removal deletes the download's whole directory rather than one file, so bytes written under any earlier naming go with it -- a delete that does not depend on the stem cannot disagree with the write about it. Two workarounds went with the split: `KeepOffline` named by identity on purpose to survive it, and `keepForOffline` recorded every copy as `application/octet-stream` while naming the file from the URL. Five assertions a side, including one that writes a file the old way and proves the new removal still takes it. Found on iOS while mirroring: a whitespace-only title survived `safe` and produced `  .cbz`, which Android already treated as no title at all. **Background transfer is built on both. Android is verified end to end**: a foreground service holds the process, a download started on the screen finishes with the app off it, and the service stops itself when the queue empties. **iOS is verified only in the foreground**: the transfer goes through a background `URLSession`, which is what lets the system carry it, and a download completes and lands through that path -- but the Simulator's transfer daemon never delivers a completion that lands while the app is suspended, and keeps reporting the task as live, so the suspended case needs a real device to confirm. A completion that arrives with nobody waiting is adopted, and a download nothing is carrying goes back in the queue. **Four scenarios are built and asserted on both**: a re-fetch of something already downloaded is suppressed, the failure/retry/backoff loop, automatic cleanup with a real ten-second undo (the bytes are moved aside, not deleted), and backup exclusion. Automatic cleanup has not been seen working on a device. **The largest gap is the one nobody had written down: a download shows no size and no progress, ever.** `advancing` is called exactly once per download, at completion, on both platforms -- `DownloadQueue.swift:337` and `DownloadQueue.kt:278`. iOS's `BackgroundTransfers` implements no `didWriteData` delegate, Android reads the whole body into a `ByteArray`, and `OpdsAcquisition` parses no `length`. So the spec's first scenario -- "its size is shown, and progress is visible" -- is served by an indeterminate spinner from tap to finish, and the 400 MB comic the spec's purpose names is held entirely in memory on Android. **Two of the three scenarios that had no implementation now have one, mirrored and asserted; the third is still missing and the reason is outside this capability.** *Low device storage* is built and tested on both. `StorageHeadroom` is the shared rule -- a 256 MB floor checked against the volume, and against the size about to be written where one is known -- and each download store asks its own volume, which is the only genuinely platform-shaped part. A shortage pauses every queued and running download as `Download.Pause.outOfSpace`, which is the state and the four translated sentences that had been unreachable since the queue was written, evicts the cover cache once and before any publication, and deletes no download: the bytes stay and the transfer resumes from them when there is room. A download the reader paused keeps their reason rather than being resumed by the shortage lifting, and a volume that declines to report its free space leaves the queue running -- AGENTS.md §2 forbids turning a normal condition into a failure a reader has to dismiss. Six assertions a side on the rule and six more on the two library operations. *Overriding a metered connection once* is built and tested on both. `MeteredDownload` answers two questions: whether to ask, which turns on the connection alone and not on the Wi-Fi-only setting, and whether a queued download may start, which is where "for that item only" lives. The grant is held against the publication's id in memory, so it dies with the app instead of becoming a standing permission; the Wi-Fi hold is now per download rather than global, so one granted publication runs while the rest of the queue waits, and `held` stops claiming the queue is stopped while bytes are arriving. The confirmation is one modifier on iOS and one composable on Android, reached from both the catalogue shelf and the publication's format rows, reusing the title, the confirm label and Cancel already translated for the share that asks this question before streaming. **The size is stated where the app has one and its absence is stated where it does not** -- an OPDS acquisition link carries no `length`, so before a first download the honest answer is usually a sentence rather than a number, and `Download.expectedBytes` supplies the figure on a retry. Seven assertions a side. **The one part of it that cannot be settled at a desk** is whether the link is really cellular: `NWPathMonitor` and `ConnectivityManager` both report Wi-Fi to a simulator and an emulator whatever the host is on, so the branch that decides *to ask at all* wants a device with a SIM or a tethered hotspot. *Reading while downloading* is **still missing**, and only its second clause was ever reachable from here. The wait before the first page is shorter: `DownloadLibrary.promoting` moves the download a reader is waiting on to the head of the queued run -- order, not priority, so nothing is cancelled or preempted -- which on a metered link, where the bound is one, was the difference between a five-megabyte comic and a four-hundred-megabyte wait. Five assertions a side. But the scenario's first clause is "opens immediately by streaming", and nothing in the app can stream an HTTP acquisition. The architecture's extension point exists and is used -- `ComicArchiveOpener.register` and `PublicationAccess.register` hand `PublicationIndexer` a `RandomAccessSource` for `smb://` today -- but **no source over HTTP range requests is registered, no OPDS acquisition link carries a `length`, and `scripts/opds-server.mjs` serves no `Accept-Ranges`**, so there is nothing to stream from and nothing in the corpus to prove it against. Those three live in `Catalogue`, `core:catalogue` and `scripts/`. **Two claims that used to sit in this row are false and are worth naming exactly.** *"Resumes by itself" is built and asserted on both platforms (2026-09-06), and the queue still says nothing about why it is waiting.* `reconsider()` has two callers a side: the connection report and a removal that frees room. Android registers a default `NetworkCallback` through `NetworkCost.onWifi` and the queue collects it; iOS's `NWPathMonitor` handler reports through `NetworkCost.onChange`, which the queue observes weakly. Both report only a connection that differs from the last one, because each report costs a main-thread filesystem stat. **The settings reach the queue as of the same day**, and this is the part that had voided the rest: `settings` defaulted to the app defaults and the one production caller on each platform left it out, so the shipped queue read Wi-Fi-only as off and no storage limit, was never held, and had nothing to resume. The parameter has no default now, so a caller that omits it does not compile. Pinned by `DownloadQueueWakingTests` / `DownloadQueueWakingTest`, four assertions a side; the storm assertion counts the credential rather than `running`, which is keyed by download id and so cannot count a second start of the same download. **Both of those gaps are closed (2026-09-06), and one limit under them is not.** *Wi-Fi only* is now built and tested on both platforms in both directions. `pump` calls `holdForConnection` on every pass: a queued or running download the connection no longer permits becomes `Download.Pause.waitingForWiFi` and its transfer is cancelled, and one the connection permits again goes back in the queue. **On iOS the cancellation reaches the system's transfer as of 2026-09-06, and did not before.** `BackgroundTransfers.download(_:named:)` awaited a bare `withCheckedThrowingContinuation`, which carries no cancellation, so the `URLSessionDownloadTask` ran on: the whole file arrived over cellular and `land` then marked the row finished, and each return of Wi-Fi started another copy of the same transfer while the previous continuation was never resumed. It is wrapped in `withTaskCancellationHandler` now, one assertion pins that the caller is told `CancellationError`, and a second pins that a transfer unwinding late does not take the slot of the one that replaced it. A cancelled transfer no longer records itself as a failure, which would have overwritten the pause reason and deleted the fetched bytes. **Android needs no equivalent and still spends the bytes in flight**: a cancelled coroutine throws out of `attempt`, so nothing is recorded and nothing lands, but `HttpUrlConnection` reads the body to the end on its own thread before the cancellation is observed. `DownloadLibrary.reconsideringWifi(permits:)` is the shared rule and decides both directions in **one pass**, which is what makes a flapping connection cheap -- ten transitions in ten seconds move each row exactly ten times, and a pass that alters no row returns the same value so the store is not written at all. `MeteredDownload.mayStart` is the predicate it asks, so a download carrying a metered grant is not paused and the grant releases no other row. Six assertions a side on the rule, eight on iOS and six on Android on the queue. **A device that fills up while the queue waits for Wi-Fi now says the room rather than the Wi-Fi.** `pausingForSpace` converts a `waitingForWiFi` row as well as a queued or running one, on both platforms: the shortage outranks the connection, `hold` names it first, and `pump` returns at the shortage without examining the connection -- so a row left saying "waiting for Wi-Fi" kept saying it while the reader was *on* Wi-Fi, naming a remedy that had already happened. `resumingAfterSpace` puts the row back in the queue and the same pump holds it for Wi-Fi again if the connection still forbids it. Two assertions a side. **The bytes are not discarded and the app cannot resume from them.** The record and its `downloadedBytes` survive the pause -- pausing is not cancelling, and that is asserted a side -- but there is still no Range header anywhere in either tree, so a resumed transfer begins at zero and five returns of Wi-Fi are five whole downloads. **The pause reason is persisted on both platforms (2026-09-06), and it had to be for the screen below to draw anything.** `StoredDownload` carried `isFinished`, `failure` and `attempts` and no pause, so every paused record came back queued -- and the settings screen reads a store snapshot, so `waitingForWifi` and `outOfSpace` could never reach it in the running app. The download row on each platform -- `DownloadQueueSection.swift:208` and `DownloadsParts.kt:305` -- draws `Download.Pause` from that same snapshot, so it never rendered a pause reason either, on any row, since the queue was written. The reason is a field now, written from the enum's own case name on each side and tolerated as absent, so a record from an earlier build still comes back queued rather than being lost. Two assertions a side. The queue still asks the connection and the volume on its first pump, which is what keeps the reason true rather than merely remembered: a device relaunched on Wi-Fi puts the row back in the queue and shows no hold at all. **The records have a reader-visible consumer on both platforms; `DownloadQueue.held` still does not.** `DownloadsSettings` / `DownloadsGroup` ask `DownloadLibrary.hold(limit:)` of the store snapshot they are handed, and draw two sentences: the state, then the remedy. `DownloadQueue.held` asks that same rule of the live library and has no caller outside the test suites on either platform, so a screen showing a *live* hold is still unbuilt. Waiting for Wi-Fi and a full device say the queue starts again by itself, which is the part a reader needs and could not see; the reader's own limit names both ways out, worded so the spec's "offers to free room" is still available to build. The two pause sentences are reused rather than written a second time. One new sentence and three remedies, in four languages, **not reviewed by a speaker**. Six assertions a side, each building the real surface, and each proved able to fail by commenting the row out. **Not photographed**: no simulator or emulator capture was taken, so the four routes and the largest text size are owed. *"Resumes after the app is killed" is a restart, not a resume.* There is no Range header, no `bytes=`, no resume data and no `downloadTask(withResumeData:)` anywhere in either tree, and the scenario asks the app to state which of the two happened -- no string on either platform does. **Three more partials to cost honestly**: per-item and global pause, resume and cancel are absent from the downloads view on both, and the in-file comments at `DownloadsSettings.swift:198` and `DownloadsGroup.kt:269` admit it; the storage view has no cover-cache size and no per-source subtotal; and the storage limit stops the queue and never offers to free room. Downloading a collection shows no "n of m complete". **Verifying a download no longer crashes Android.** Both platforms verify by indexing the file before the record is marked finished, so a truncated archive is caught at landing rather than at the first page turn. iOS caught `PublicationIndexer.IndexError` and reported it in plain language as non-retryable; Android's `attempt` caught only `OpdsError` and `IOException`, and `IndexException` extends `Exception`, not `IOException` -- so a download whose bytes will not index threw out of `attempt`, out of `transfer`, and out of a coroutine launched on `Dispatchers.Main.immediate` in a `SupervisorJob` scope with no handler, and took the app down. Android now has the branch iOS had, naming the format for an unsupported container and saying the bytes could not be read otherwise, in all four languages, marked non-retryable the same way. One assertion a side pins the fact the crash depended on: an index failure is its own type and is not an I/O failure. **Not driven**: neither platform's failure path has been watched on a device, because it needs a server that serves bytes that will not open. **The scenario's "a failed verification re-queues it once" is now built and asserted on both.** The two ways indexing can fail are not the same event and no longer get the same answer: an unsupported format is not a failed verification -- the bytes are what the server holds, re-fetching produces the same format, and it stays terminal -- while unreadable bytes are, the likeliest cause is the transfer, and they go back in the queue exactly once. `Download.verificationFailures` counts separately from the three transfer attempts and is persisted on both platforms, so a download killed between its two chances does not come back with both; a re-queued download no longer tells a reader waiting to open it that it failed, because it has not. Seven assertions a side. Still not driven against a server that serves bytes which will not open **A failed transfer's queue row offers Retry and Remove on both platforms** (Android and iOS, 2026-09-05): a transfer that has already stopped is not offered a Stop it cannot perform, clearing it is a fourth confirmation that says nothing reached the device rather than describing a copy and a position that do not exist, and on iOS the retry reaches whichever live queue holds the record -- and `DownloadQueue.reconsider()`, which had no caller on either platform when that landed, now has one for the Wi-Fi-returns case and one for a removal. Pinned by `DownloadQueueRemovalTests` / `DownloadQueueRemovalTest` and `DownloadQueueRetryTests`; photographed in `docs/designs/screenshots/downloads-retry-2026-09-05/`. **One test in this capability was flaky, and was fixed on 2026-09-06.** `BackgroundTransferCancellationTests` — *Cancelling the caller cancels the transfer instead of letting it land* — read `told` as nil where it expects `cancelled`, once in four local runs and on every GitHub `macos-26` runner. It was not a defect in the transfer: the test polled for a bounded **half second** for the box the cancelled task writes, which is ample on an idle machine and not on a loaded one. The budget is five seconds now. A passing run still costs milliseconds, so the change is invisible except on a slow host. Four consecutive full-suite runs passed afterwards. |
| `opds-catalog` | partial | 13 scenarios: 12 built and tested on both, 1 built and asserted by nothing. **Re-audited against the source on 2026-09-06, and re-scored the same day: the certificate path moved from "asserted by nothing" to "built and tested on both", so the two totals went from 11 and 2 to 12 and 1. The table above moved with them.** The parsing layer is an exact mirror at 86 tests a side -- iOS across `Tests/CatalogueTests`, Android across `core/catalogue/src/test`, the same cases under the same names. **Adding a catalogue, naming what a non-feed actually was, HTTP Basic and Bearer under the origin rules, navigation sections with their counts, OPDS 2.0 groups, facets, search, the publication screen, the format ranking and the four refusals are built and tested on both platforms.** Facets reach a screen on both, which is worth stating because a parsed field that nothing draws is this repository's commonest defect: `CatalogueBrowserView.swift:163` builds the menu on iOS and `CatalogueBrowserScreen.kt:145` builds it on Android, out of the `facets` that `OpdsParsingTests.swift:103` and `OpdsParsingTest.kt:113` assert. A publication's own screen is reached on both, from a row a reader presses: `CatalogueDetailView.swift:23` from `CatalogueGroups.swift:183`, and `CatalogueDetailScreen.kt:76` from `AppScreens.kt:383`. **One behaviour rests on no assertion. The certificate path was the other, and it is asserted now.** Pagination is `CatalogueBrowser.swift:108`, called from `CatalogueBrowserView.swift:231`, and `CatalogueBrowser.kt:117`, called from `CatalogueBrowserScreen.kt:228`; `loadMore` and `hasMore` appear in no test directory on either platform. **Naming `CertificatePins` in a test is not asserting trust.** It appears in five Android test files. Four of them -- `RemoteSearchOpdsTest.kt`, `SearchScopeTest.kt`, `DownloadQueueConnectionTest.kt` and `DownloadQueueWakingTest.kt` -- pass it as an empty constructor, as a parameter the test does not look at. Only `OpdsTrustTest.kt` asserts the rule. Count the assertions, not the occurrences. **`scripts/opds-server.mjs --self-test` passes 23 checks on every `pnpm lint`, and it tests the mock rather than the app.** It drives the fixture with node's own `fetch`, so no Swift and no Kotlin runs in it. Twenty of the 23 checks are range and byte-serving checks that belong to `offline-downloads`; two reach this capability, asserting that an Atom link states its `length` and an OPDS 2.0 link its `size`. It proves the corpus is worth testing against. It proves nothing about either client, and **nothing in this row has run against a live catalogue** **The certificate path is asserted on both platforms as of 2026-09-06, and the counts above move with it.** The path is `OpdsTrust.swift:16-137`, with the sheet at `CatalogueSheet.swift:230` and the pin at `CatalogueConnection.swift:158`, mirrored by `OpdsTrust.kt:18-82`, `CatalogueSheet.kt:245` and `CatalogueConnection.kt:194`. `OpdsTrustTest.kt` adds twelve cases and `OpdsTrustTests.swift` seventeen, over the same two certificates carried in both files as the same DER -- one self-signed, and one an issuer signed -- so the fingerprint each platform shows a reader is asserted to be the same string. **Android drives the whole decision.** `OpdsTrust.PinAwareTrustManager.checkServerTrusted` is called with a stub platform trust manager, and the cases assert that an untrusted certificate is refused and described, that a matching pin is accepted, that a pin belongs to one host and to one certificate, that the fingerprint shown is the leaf's and not its issuer's, that a server offering no certificate is refused, and that a certificate the system trusts is accepted whatever is pinned. **iOS drives the same decision.** `OpdsTrustDelegate.decision(for:host:)` is called with a `SecTrust` the suite builds, and the cases assert that an untrusted certificate is refused and described, that a matching pin is accepted, that a pin on another host does not open this one, that a pin on another certificate does not open this one either, that the fingerprint shown is the leaf's and not its issuer's, and that a certificate the system trusts is accepted whatever is pinned. The last of those anchors the certificate to itself, which is how a test gets a positive answer from `SecTrustEvaluateWithError` without a network. iOS also asserts the parts the description is built from: the leaf, the fingerprint, the subject, and every rule `CertificatePins` applies. **One method stays out of reach on iOS, and it decides nothing.** `urlSession(_:didReceive:)` reads the certificate from `URLProtectionSpace.serverTrust`, which is read-only and set by the system, so no test can build the challenge; after the split that method holds the guard and the two reads that call `decision(for:host:)`. An empty certificate chain is out of reach as well, because `SecTrustCreateWithCertificates` answers `errSecParam` for an empty array and writes no trust; Android asserts that branch over a null chain, which Java's `X509TrustManager` really can be handed. **The pin is an exception a reader adds, not a restriction they impose, and that is now asserted rather than assumed.** The scenario offers the pin only when the system does not trust the certificate, so a system-trusted certificate is accepted whether or not a pin exists. A reader who does proceed anyway proceeds for one host and one fingerprint: `CatalogueConnection.trustCertificate` pins what it was shown and nothing wider, and cases on both platforms assert that a pin does not travel to another host or to another certificate, at the decision and inside `CertificatePins` alike. **Both suites were proved able to fail.** Bypassing the pin check made three Android cases fail by name; dropping the host from the lookup made three more fail on Android and two on iOS; dropping the colons from the fingerprint made two fail on iOS. Bypassing the pin check inside the iOS decision failed `anUntrustedCertificateIsRefusedAndDescribed` and `aPinOnAnotherHostDoesNotOpenThisOne`, which no test caught before 2026-09-06; dropping the system check failed `aSystemTrustedCertificateIsAcceptedWhateverIsPinned`. **Two more mutations on 2026-09-06, after the review found the suites blind to them.** Widening the iOS pin check from one fingerprint to the whole host failed `aPinOnAnotherCertificateDoesNotOpenThisOne` by name, and passed before that case existed. Taking the last certificate of the chain instead of the first failed `theFingerprintIsTheLeafAndNotItsIssuer` on iOS and on Android, and passed on both before those cases existed. **The seams cost four `private` keywords and one method split in two, and no behaviour.** `PinAwareTrustManager` is `internal`, and `leaf`, `fingerprint` and `subject` on iOS are internal to `Catalogue`. The iOS rule moved out of the delegate method into `decision(for:host:)` beside it, which the delegate method calls. No public API moved and no check changed. **`scripts/opds-server.mjs` was not changed.** It serves plain HTTP, node's standard library cannot generate a certificate, and serving TLS would mean a private key in the repository. Its self-test still passes 23 checks. |
| `kavita-server` | partial | 19 scenarios: 11 built and tested on both, 8 built and asserted by nothing. **Re-audited against the source on 2026-09-06. The old row said "12 built on both", which is not this table's vocabulary; read against the source it resolves one lower, and the eight are named at the end of this row.** **A third agent is rebuilding the Kavita layer's server identity for a chapter as this is written, so this row scores commit `ce9dbc44` and may move again today.** **Built and tested on both platforms: extracting a base and a key from a pasted OPDS URL** (`KavitaAddress.swift:45` and `KavitaAddress.kt:67`, eight matched tests a side plus the `CatalogueTarget` pair), **the one-shot token renewal** (`KavitaClientTests.swift:66`, `KavitaTest.kt:285`), **libraries, series, volumes and loose chapters** (`KavitaLibraryTests.swift:24,31,47` against `KavitaTest.kt:187,192,197`), **the progress merge** (`KavitaExchange`, 17 matched tests a side), **the server's metadata laid over the file's** (`KavitaFind`, 15 a side; reached at `LibraryDownloads.swift:73` and `LibraryViewModel.kt:848`), **keeping a chapter and reading it offline** (`KavitaCardStore` 7 a side, `KavitaKeepCard` 3 a side, `KavitaCardFacts` 4 a side, drawn at `DetailMainColumn.swift:50` and `PublicationDetailScreen.kt:382`), **marking a chapter read**, and **searching a server, in both its shapes** (`KavitaFind` again, and the field carried down all three levels -- three views on iOS through `kavitaSearchable`, one screen and a `KavitaLevel` on Android). **Two scenarios drop out of that list, and Android is why.** *Adding a server* names a version floor: iOS asserts the refusal at `KavitaClientTests.swift:36`, naming both versions, against the throw at `KavitaClient.swift:88`; Android throws the same at `KavitaClient.kt:64`, and `KavitaTest.kt:85` asserts only that `0.7.14` sorts below `0.8.0`. Nothing asserts that `connect()` refuses. *API key revoked* is worse: iOS has `KavitaClientTests.swift:92`, and `KeyRejected` appears in no Android test at all, though the throw is at `KavitaClient.kt:315,322` and the sentence at `KavitaMessage.kt:25`. **The row's `LibraryScreen.kt:481` citation no longer resolves.** Android's offer to put a library search to the server is `KavitaSearchOffer`, declared at `LibraryScreen.kt:753` and drawn at `LibraryScreen.kt:445`; commit `2cfc5a5a` moved it. iOS keeps `KavitaServerSearchOffer.swift:21`, drawn at `LibraryContent.swift:111`. Neither platform asserts it. **`KavitaSync` is still asserted by nothing, and the old row's grep for it is now a trap.** `ProgressCapabilityTests.swift:16` names a test function `onlyKavitaSyncs()`, added by `c18caa2c`, so the grep the row invited now answers -- on a test of `SourceKind.syncsReadingProgress`, which never touches the type. No test on either platform builds `KavitaSync`, `KavitaProgressStore` or `KavitaUnsent`. **Every one of its entry points does reach a reader, which was checked by hand**: `report` from `App/StoryArcAppActions.swift:72` and `ReaderHost.kt:62`, `pull` from `KavitaChapterList.swift:115` and `KavitaChapters.kt:112`, `mark` from `LibrarySources.swift:362` and `LibraryViewModel.kt:295`, `append` from `KavitaChapterList.swift:290` and `KavitaChapters.kt:200`, `flush` from `KavitaBrowser.swift:89` and `KavitaBrowserScreen.kt:132`. **A seventh piece of tested, uncalled code is here.** `KavitaClient.search` answers with series alone -- `KavitaLibrary.swift:229` and `KavitaClient.kt:237` -- and both platforms test it, at `KavitaLibraryTests.swift:79` and `KavitaTest.kt:205`. Nothing calls it. `find` at `KavitaLibrary.swift:244` and `KavitaClient.kt:251` replaced it and is what the search field asks. It costs no scenario, because `find` serves them all; it is two tests that prove nothing a reader can reach, and they should go or the method should. **`scripts/kavita-server.mjs --self-test` passes 22 checks on every `pnpm lint`, and it tests the mock rather than the app.** It drives the fixture with node's own `fetch`, so no Swift and no Kotlin runs in it. It asserts the fixture's progress arithmetic, its continue point, its read and unread marks, and that its corpus states more than one age rating and more than one publication status. That is worth having, because a corpus of constant zeros hides a client that drops the field. It is not a client test, and **nothing here has run against a live server**. **The eight asserted by nothing**: the version floor, the revoked key, the continue point (`KavitaChapterList.swift:110` and `KavitaChapters.kt:107`, tested nowhere on either side), the server's own collections (`KavitaShelfViews.swift:43` and `ShelvesScreen.kt:119`), the append to a reading list, the refusal to mix sources in one server list (`LibrarySourceHealth.swift:116`, `AddToShelfMenu.swift:198`, `AddToShelfSheet.kt:249`), the push's retry on the next connection, and the library's offer to search the server. What stands behind the two writes underneath them is `KavitaClient.report` and `KavitaClient.mark`, asserted on both sides -- against a loopback server on Android and a stubbed transport on iOS. **Verification is compilation and unit tests, not use.** `pnpm lint` is green and both suites pass. **Nothing in this row has been seen on a screen**, and that has not changed since the last audit **The *Metadata* requirement's seven fields all reach a screen on both platforms, which is what closed the last one-platform scenario on 2026-08-31.** `ageRating` and `publicationStatus` cross the wire as bare integers; both tables are copied from Kavita's own `AgeRating.cs` and `PublicationStatus.cs` and asserted position by position, six tests on iOS at `KavitaRatingsTests.swift` and nine on Android at `KavitaRatingsTest.kt`. Two rules keep a line off the screen on both sides: a number outside the table is not a rating, and Kavita's own `Unknown` and `Not Applicable` are not ratings either, because drawing them would tell a parent this book had been assessed when nobody has. The wire type holds the status optionally, because Kavita's zero there is `OnGoing`, a state a curator chose. **Two limits stand.** The Kavita browser still offers no way to remove one kept chapter from inside itself; Settings and the library both do it. A cached search can only match what has been kept, because the card is the one Kavita answer written to disk. **`KavitaClient.search` was examined on 2026-09-06 and deliberately left in place.** The capability's three search scenarios all require matches across series, chapters, people, genres and tags. That is `find`. `search` answers with the series alone, so no scenario can be served by it, and wiring it to a screen would build a narrower search than the spec asks for. It should be deleted, together with the two tests that are its only callers -- `KavitaLibraryTests.swift:79` and `KavitaTest.kt:208`. The deletion was not made in this pass because `KavitaLibrary.swift`, `KavitaClient.kt` and those two test files sit outside the files this run was given. The change is four deletions and no replacement: `find` already covers every caller, and a fresh grep on 2026-09-06 found no other reference on either platform. |

**What is asserted and what is not, precisely, because the difference matters here.** The keep, the card's round trip through storage, the two enums' whole tables and the offline block are each pinned by tests that fail when the fix is reverted — the keep's two lines were mutated to "the server said nothing" and a test caught it. **The live series screen's two lines are asserted by nothing on either platform**: deleting the status line from `KavitaChapterList.swift` or from `KavitaChapters.kt` fails no test. Both are large list views with no extraction seam, and giving them one is its own change. Nothing on either platform has been photographed. **One limit remains, named rather than hidden.** And the downloaded-offline path still shows five of seven: `KavitaCard` in `:core:model` has no field for either, so *Reading a downloaded Kavita title offline* falls back to the file's `ComicInfo.xml` for exactly those two -- two fields on the card and two lines in `KavitaKeep` close it, on both platforms, and `KavitaKeep`'s own KDoc says so. **Proven against the mock and not on a screen.** `scripts/kavita-server.mjs` served `ageRating: 0` for every series, so the whole stated-rating path was exercised by nothing; it now cycles Kavita's own numbers, and seven new self-test checks hold the corpus to Kavita's ranges, to at least one unrated series and at least one rated one, and to more than one of each. Driven over HTTP against the running mock on 2026-08-31: eleven series, five distinct ratings, three distinct statuses, three of the eleven correctly drawing no rating line. The Compose block that renders them has been seen by no emulator |
| `network-share` | partial | 12 scenarios: **4 built and tested on both, 7 built and asserted by nothing, 1 built, drawn and asserted on iOS, with no Android half for the platform to have.** Re-audited against the source on 2026-09-06, and both gaps that audit left were worked the same day. **The one-platform verdict is gone, and it was a category error rather than a change in the code.** Every scenario that has code has it on both platforms; what differs is which side asserts it, and this page calls that *built, asserted by nothing*. Reopen-on-next-read is the case: `SmbClient.swift:218-236` and `SmbClient.kt:225-245` are the same method, and only Android proves the branch (`SmbClientTest.kt:118`), because `SmbReconnectTests.swift:6-12` says in its own header that reaching it from iOS would widen a private surface for a test alone. Discovery is the mirror image -- built on both (`SmbDiscovery.swift:37-60`, `SmbDiscovery.kt:33-75`), wired into both sheets (`SmbSheet.swift:24`, `SmbSheet.kt:91`), and asserted by neither. The refusal half of it now is on iOS; see below. **iOS does state the transport, and the old row said it stated neither.** `SmbSheet.swift:180-188` writes `smb.encrypted %@` or `smb.notEncrypted %@` with the dialect, under the folder list. Android writes the same pair and composes signing around it (`SmbSheet.kt:211-222`: `smb_encrypted`/`smb_not_encrypted` inside `smb_signed`/`smb_not_signed`). So both sheets state encryption, and **signing is the half iOS never states**, per [ADR-0016](../decisions/0016-ios-smb-response-signing.md), which found that SMBClient 0.3.1 verifies no response at all. **The source detail screen was the half neither platform had, and it now has it.** *Encrypted transport* asks for the sentence there, and there is where a reader looks after the sheet is gone. `SourceDetail.swift` and `SourceDetailScreen.kt` each draw one sentence for a network share and for no other kind -- *StoryArc reads this share over SMB. The connection is not encrypted.* -- in en, fr, de and es. `SourceTransportNoteTests` (11 cases) and `SourceTransportNoteTest` (14 cases) assert that a share draws it, that the other three kinds do not, and that a refused credential or an unreachable host does not move it. **The sentence names encryption and never signing on either platform**, which is ADR-0016 rather than an omission: iOS cannot answer the signing question, the ADR refuses the line, and mirroring Android sheet wording onto this screen would have made the two platforms disagree about a security claim. Both suites match the four shipped sentences against eight signing words and fail on any of them. **They also pin the negation, which they did not at first.** Each asked only whether its sentence carried the word for *encrypted*, so *The connection is encrypted.* satisfied both -- the one word that carries the security claim was the one word neither held. Each now matches the negated phrase in its own language, and flipping the English value fails `everyLanguageDeniesEncryption` and `every locale states the connection is not encrypted, and none claims signing` by name. This matters because the sentence is a constant: `SmbClient` hardcodes `isEncrypted = false` on both platforms, and neither `Source` nor `SourceDiagnosis` carries what a session negotiated, so the screen cannot read it off the connection the way [ADR-0010](../decisions/0010-smb-clients.md) describes. **[ADR-0016](../decisions/0016-ios-smb-response-signing.md) now records why a sentence on a screen is not the disclosure it refuses**, under *What "nothing is added" covers, and what it does not*: the encryption line is `network-share`'s own clause and predates that ADR on both sheets, while the signing line it rejects is still stated nowhere on iOS. **A third piece of declared, unreachable code is here.** iOS's `SmbError.encryptionRequired` is declared at `SmbAddress.swift:85`, translated and displayed at `SmbConnection.swift:193-194`, and thrown nowhere -- exactly the shape of the `protocolUnsupported` defect this row records as fixed. `SmbClient.swift:157-161` states why: a share with `smb encrypt = required` answers `ACCESS_DENIED`, a refused password answers the same, and this client cannot tell them apart, so the commoner reading wins. Android reads jcifs' own sentence instead (`SmbClient.kt:200`) and asserts it (`EncryptionRequiredTest.kt:23`). The iOS case should be reached or removed. Today it is a translated string no reader can see. **SMB 3 encryption cannot be settled at a desk, and it is not scored absent for want of trying.** [ADR-0010](../decisions/0010-smb-clients.md) records that neither client encrypts -- jcifs-ng carries the negotiate context and no cipher, SMBClient offers SMB 2 only -- and [ADR-0016](../decisions/0016-ios-smb-response-signing.md) surveyed the replacements and decided that **nothing ships**: "No line on the connection sheet, no badge, no toggle, no prompt." Proving a negotiation needs a server with `smb encrypt = required`, which `scripts/smb-server.sh --encrypted` serves and which only the Android refusal has been driven against. So *Encrypted transport* now has its second clause built and asserted on both, and its first is still unverifiable here. **What the line will say when encryption is on is recorded and not shipped**: the sheets already carry *encrypted* in four languages (`smb.encrypted %@`, `smb_encrypted`), and the detail sentence would end *The connection is encrypted*. It is in neither catalogue, because `SmbClient` hardcodes `isEncrypted = false` on both platforms and a second sentence nothing can reach is the `SmbError.encryptionRequired` shape this row already reports. **No test covers the encrypted case and none pretends to**: proving a negotiation needs a server with `smb encrypt = required`, and nothing has been driven against one for this clause. **A denied local-network permission is detected on iOS, and the sheet now draws what it says.** `SmbDiscovery.swift` sets a `stateUpdateHandler`, reads a refusal off `NWError` -- `kDNSServiceErr_PolicyDenied`, `kDNSServiceErr_NoAuth` and POSIX `EPERM`, and nothing else -- and produces one sentence on the first refusal and none on a second. The sentence is in `Sources/Smb/Resources/Localizable.xcstrings` in four languages. **It shipped once with no reader able to see it**: `SmbSheet.swift` read `discovery.hosts` and never `discovery.advice`, `LibraryFeature/` was outside the slice that wrote the producer, and nine tests passed around a sentence shown to nobody. The sheet draws it now, where the host list would have been, and `SmbSheetAdviceTests` (3 cases) walks the sheet's own body for it: present after a refusal, absent without one, and no host section either way. **The wiring is asserted too, and it was not.** `SmbDiscoveryRefusalTests` called `isRefusal(_:)` and `noteRefusal()` directly, so deleting the whole `stateUpdateHandler` block -- the only production behaviour the commit added -- left every test green. Three of its nine tests now read the handler back off the browser `start()` built and hand it a state, and all three fail by name if that block goes. The two deliberate behaviours are untouched and asserted: the host section is drawn only when the list is not empty, so discovery still hides itself, and the form sits beside the list rather than behind it. **Android has no such permission to deny**, and that is a difference the platform forces rather than one to mirror: `NsdManager.discoverServices` needs no runtime grant, the manifest declares only `INTERNET` and `ACCESS_NETWORK_STATE`, and no code in the app requests a permission at all. There is nothing to detect and nothing to tell a reader to turn back on, so no Android sentence was written. **An SMB 1 server is still refused by name on both**, off NT statuses on iOS (`SmbProtocolRefusalTests.swift`, four cases) and off jcifs' three sentences on Android (`SmbProtocolRefusalTest.kt`, four cases), and neither has been driven against a real SMB 1 server because `scripts/smb-server.sh` has no `--smb1` mode. **Everything else is built on both and mostly unasserted**: the browse-and-pick-a-root step (`SmbSheet.swift:170-178`, `SmbSheet.kt:238-250`); the two-second indicator and the sixty-second offer to download and leave (`NetworkNotice.swift:28,31`, `NetworkNotice.kt:113,116`), both wired into the reader and named by no test on either side; and the metered confirmation before streaming (`SmbBrowserView.swift:92`, `SmbBrowserScreen.kt:190`), both reading `NetworkCost.isCareful` and neither asserted -- `MeteredDownloadTests`/`MeteredDownloadTest` assert `offline-downloads`' queue rule, which is a different question. The sixty-second clock itself is asserted on iOS only: `SmbReconnectTests.swift:38-47` pins `SmbReachability`, and Android has no `SmbReachabilityTest` at all. **Both live suites still skip themselves when no server is listening on 4445, and iOS's skips silently** -- `SmbClientTests.swift:86-92` catches the failed connect and returns, so the test passes having asserted nothing, where Android's `assumeTrue` reports a skip. **"Nothing written to the device" is true of CBZ, CBT and EPUB and false of PDF and CBR**: both platforms copy the whole archive into the cache first (`SmbBrowserView.swift:236-258`, `SmbBrowserScreen.kt:363`), because PDFKit, `PdfRenderer` and libarchive all want a real file. **Driven against a real Samba server with `server signing = mandatory`, authenticated**, and nothing has been watched on a device since. |
| `collections-and-reading-lists` | partial | 15 scenarios, scored as 18 clauses because three of them carry two independent halves: 12 built and tested on both, 4 built and asserted by nothing, 2 missing on both. Nothing here is one-platform -- the mirroring across Swift and Kotlin is unusually exact, down to matching test names. **A reader can create, rename by re-creating, delete, and populate local collections and ordered reading lists on both platforms, reorder a locally-defined list, see their position in it, add any publication to any number of them, and be offered the next entry in list order rather than the next in series when they finish one.** A collection with no cover of its own is drawn from the artwork it holds: the first four member covers by identity ascending, as quadrants, or one cover across the frame below four. The library has a selection mode in both the grid and the list, with the count visible and one action out of it, and what is picked can be added to any collection or reading list, kept on the device, or marked read -- through the same paths one publication already takes, with one undo for the whole set for ten seconds. A whole collection or reading list can be downloaded or marked read from its own screen, the item count and total size stated before anything is copied. Downloading a selection copies its files into the app's own download store rather than fetching them, because everything the library holds is already a file on the device; a publication read off a share is still kept offline from the reader instead. **A local reading list can be copied onto a Kavita server**, so it syncs and other Kavita clients can see it. Only a server that has just answered when asked for its reading lists is offered -- reachable and able to hold one, which an empty answer cannot tell apart from no answer, so the two are kept apart. Before anything is sent the reader is shown, per server, how many of the entries that server already holds and how many it does not; the ones it does not are named, with the reason: this app has no backend and never uploads a file, so a publication the server has never seen cannot join one of its lists. Those entries are left out and the local list keeps them -- the list is copied, not moved. The copy is undoable for ten seconds through the same bar every other bulk action uses, and the undo asks the server to drop the list again. When no server can take one the action is disabled with the reason beside it, rather than failing after the reader has confirmed it. Built against the documented `ReadingList/create` and `ReadingList` DELETE endpoints and the mock in `scripts/kavita-server.mjs`, never against a live server, and the sheet has not been seen on a screen. **An edit made to a server-backed reading list while the server is away is kept rather than lost, on both platforms.** Adding a publication to one writes the edit down before anything is sent, in a durable queue of its own that survives the app being killed -- which is the point, because the reader who edits on a train has closed the app long before the server is back. The reader sees it twice over: a line under the shelf's name saying how many edits are waiting, and the entry itself on the list, in place, marked as waiting for the server -- grey rather than red, because `sources` makes offline a normal state. The shelves screen reconciles every list it can reach each time it appears: what the server has taken since is forgotten silently, because an edit that arrived is not a change worth a sentence; what is still owed is sent through the same queue that already carries writes to a server, so nothing is pushed twice; and a list that moved underneath a waiting edit keeps its own version for membership and order. The discarded edit leaves the transport queue as well, so a later flush cannot resurrect what the server has already overruled, and the reader is told once, naming the list and the entries that were not added. Once is durable rather than incidental: the notice is written down, shown, and deleted when it is dismissed, so a refresh cannot repeat it and a launch cannot lose it. The rule itself is a five-row table in `ShelfMerge`/`ShelfPull`, mirrored line for line on both platforms with seventeen matching tests each, the way `ProgressMerge`/`ProgressPull` hold ADR-0006's -- including the row that says a list this device has never seen is pushed to rather than second-guessed, because without a baseline "did the server change" is unanswerable and losing the edit is the one outcome offline must never produce. **Neither offline scenario has been seen on a screen.** Driving them needs a Kavita server, a publication in the library that carries a Kavita origin, and the server taken away between two refreshes; the simulator booted, built and ran the app in that round, but the tooling that drives its interface stopped responding before the shelves screen was reached, and no emulator was driven at all. **Deleting a collection asks first, on both platforms, and says what stays.** The row said for a week that neither half existed; both landed in `63917c7f` on 31 August and the row was not brought forward, so read the code before you cost this one. A `ShelfDeletion` is the gap between the question and the answer: while one exists nothing has been written, and `apply` -- the only thing that writes -- is reached from the confirming button alone. The dialogue names the shelf, says that the titles stay in the library, and names which of the two shelves goes, because *the collection* and *the reading list* are different words to a reader. It states no count: the spec asks for the confirmation and the sentence, and a counted sentence was proposed and reverted on 2026-09-06 because it reads "0 titles stay in your library" for an empty shelf. **A confirmation rather than an undo, deliberately.** Every other bulk action here offers ten seconds because its result is visible and reversible; this one's result is an absence, and a bar offering to undo a shelf that has already gone answers the reader's fear too late. **The wording is asserted, not assumed.** A `confirmationDialog` never evaluates its message while nothing is presented, so iOS asks `ShelfDeletion.message` directly and Android composes the dialogue under Robolectric and reads the sentence back in its own locale -- the gap that let a reverted source removal body pass every test on 2026-09-05. **A reader can choose a collection's cover.** `ShelfCoverChoice` offers the composite first and every member after it, and answers what is showing from `CompositeCover`'s own premise rather than from a flag of its own, so the tick in the picker and the artwork on the shelf cannot disagree. The offer is on the collection's own screen, where what is inside it is being looked at, and a collection holding nothing does not ask. `ShelfCoverMenuTests` on iOS and `ShelfCoverMenuTest` on Android walk the screen rather than the function, because the function was already tested for weeks while nothing called it: iOS walks the built value tree of `CollectionDetail`, Android composes `CollectionDetailScreen` under Robolectric and asks its semantics tree for the control. Each one fails when the control is taken off its own platform's screen, in both directions -- gone where a collection holds something, present where it holds nothing. `ShelfCoverChoiceTests` and `ShelfCoverChoiceTest` still walk the function, which is a different claim. **Two clauses still have no code on either platform.** *A collection can only be created locally.* Nothing lets a reader choose a server at creation, and no server-collection write exists in either client. *A server-backed reading list cannot be reordered at all*, online or off, on either platform, so the "sent to the server" half of the ordering scenario has nothing to send -- even though the mock already serves `/api/ReadingList/update-by-multiple`. A Kavita list opens read-only on both. **Two divergences worth recording for an estimate**: Android reorders a local list with up and down buttons rather than the drag the spec names, which is a defensible accessibility choice and is still a divergence; and a bulk download writes entries into the download store as *completed* records rather than queueing them, so they later appear in the downloads view with no queue position, no progress and no pause, and an image-folder or `smb://` publication is skipped without saying so. **Two limits remain in the offline half.** The push waits for the shelves screen to be looked at, because the reconnect loop it should hang off was being rewritten in the same round; and the only edit that can be queued is adding an entry, because appending is the one reading-list write `KavitaClient` exposes |
| `reading-themes` | partial | 18 scenarios, scored as 19 clauses because *Choosing a background* carries two independent halves: 14 built and tested on both, 4 built and asserted by nothing, 1 missing on both. Audited scenario by scenario for the first time on 2026-09-06, and two of that first pass's own verdicts were corrected the same day, downward. The row this replaces scored these scenarios under their old home in `ebook-reader`, and two of its outstanding items are now stale. **The live preview is inside both levels of both sheets** -- `ThemeSheet.swift:39` and `ThemeAxesSheet.swift:50` on iOS, `ThemeSheet.kt:126` and `ThemeAxesScreen.kt:158` on Android -- and **custom backgrounds shipped**. **The six presets, the axis model and the named reset are built and mirrored.** `ReadingTheme.swift:78` and `ReadingTheme.kt:105` hold the same pure type: a preset, the set of axes deviated from, and an optional palette. Twenty unit tests a side assert the same table, in `ReadingThemeTests.swift` and `ReadingThemeTest.kt`; `ThemeResetTests.swift` and `ThemeResetTest.kt` assert that a reset restores the preset, keeps the custom colour slot, and reports an unmodified preset as needing no reset. The chain from a reader's tap resolves on both sides: `ThemeSheet.swift:156` and `ThemeSheet.kt:144` call `adopt`, and `EpubReaderModel.swift:371` and `EpubReaderActivity.kt:700` submit the preferences to Readium and store the choice against the shelf. **The shipped axis set is not the one this spec names, and the first pass wrote "the ten axes" as though it were.** The spec's table defines eleven axes and ends with Background colour and Brightness. `ThemeAxis` holds ten cases -- `ReadingTheme.swift:43-52` and `ReadingTheme.kt:61-70` -- which drop those two and add hyphenation. Dropping them is bookkeeping: both are modelled elsewhere and both reach a control, the brightness slider and `PageColourSection`. Adding one is not. Hyphenation ships as a reader-facing toggle at `ThemeAxesSheet.swift:202` and `ThemeAxesScreen.kt:381`, and `ebook-reader`'s `spec.md:79` requires "the axes offered are exactly those in `reading-themes`, with none added and none dropped". `reading-themes` never names hyphenation; `ebook-reader`'s `spec.md:40` does, so the two specs disagree and the app follows the looser one. No scenario verdict moves on this, because the axis set is a requirement rather than a scenario, but the divergence is real and both suites entrench the code's count under the name `ten axes`, at `ReadingThemeTests.swift:26` and `ReadingThemeTest.kt:32`. **One behaviour is missing on both platforms. A second was, and was built the same day.** *Resetting an axis* asked for a long press or a double tap on a slider to return that axis to its preset value, and neither sheet carried the gesture. **Built on 2026-09-06, on both platforms.** iOS puts a long press on the slider itself with a travel tolerance, so a drag is not read as a press (`ThemeAxisSliders.swift:108`); Android puts it on the axis block, because a Compose `Slider` runs its own press detector inside its node and consumes the down event. Both carry an accessibility action named `theme.axis.reset` / `theme_axis_reset` in four languages, and on Android that action is the only path reaching the slider itself. The reset runs through the path a drag takes, so the preview, the page, the stored theme and the reading position see it identically. Task 3.5 of `reader-theming-and-page-transitions` was ticked for this before it existed; it is `[~]` now and names the frames it still owes. *The background pairing is applied before it is previewed*: the spec asks for the background and the derived text colour to be "shown in the preview before being applied", and both readers apply first. The swatch calls `adopt` at `PageColourSection.swift:82` and `PageColourSection.kt:93`, `adopt` hands the candidate straight to `onAdopt` at `PageColourSection.swift:181` and `PageColourSection.kt:79-81`, and the sample below then draws the palette already in force. That is the second half of *Choosing a background*. Its first half -- the 7:1 derivation, the reader's override, and the refusal below 4.5:1 with the measured ratio stated -- is built and tested on both, in `ReaderPaletteTests.swift` and `ReadingThemeTest.kt`. Scoring the scenario once hid an absent behaviour behind a passing one, so it is scored as two clauses here, the way `collections-and-reading-lists` scores its three double scenarios. **Four scenarios have the code on both sides and the assertion on one side or on neither.** *Presets follow the appearance polarity* was scored built and tested by the first pass and is not: Android asserts the guard at `ReaderAppearanceTest.kt:64`, `no linked preset until the reader asks for one`, and iOS asserts nothing. No iOS test names `linkedPreset`. Delete `LinkedPreset.swift:20` and every iOS test still passes; delete `ReaderAppearance.kt:57` and `ReaderAppearanceTest` fails. The nearest iOS test, `SettingsStoreTests.swift:70`, asserts that the opt-in flag defaults to off, which is the setting rather than the behaviour the scenario names. *Reset does not disturb the reading position* takes the same path a type-size change takes -- `EpubReaderModel.swift:371` and `EpubReaderActivity.kt:713` read the locator, submit, then go back to it, because Readium lands on the progression rather than the paragraph -- and only iOS guards that path, at `ThemeSheetTests.swift:230`. *Brightness is reader-local* reverts on leaving by code on iOS (`EpubReaderView.swift:295`) and by the window attribute on Android (`EpubReaderActivity.kt:418`); nothing on either side asserts the revert. *Fixed-layout and image content* paints the reader's own colour behind the page and draws the artwork over it untouched (`ReaderModel.swift:163`, `ReaderScreen.kt:215`), and no test on either platform touches the matte. **The appearance link still resolves once, when the reader opens.** `LinkedPreset.swift:20` and `ReaderAppearance.kt:57` answer only while the opt-in setting is on, and the answer reaches the reader at construction, so a switch to dark mid-book changes nothing until the book is reopened. The scenario asks only that the reading theme does not follow the appearance unless the reader links them, and it does not. **Two weaknesses in the evidence, recorded because the verdicts do not show them.** The sheet-level scenarios lean on tests that read the source file and assert that a substring is present -- `ThemeSheetTests.swift:159` concatenates two source files and greps them, and `ThemeSheetTest.kt:156` does the same. They are real tripwires, but they prove that a call site exists rather than that a control renders and states a value, and they are asymmetric: `ThemeSheetTest.kt:138` checks that `PageColourSection(` is on level two, and the iOS list at `ThemeSheetTests.swift:131-140` has no such entry, so the iOS sheet could lose its background control and that test would still pass. The two reset suites are also not the mirror they look like: iOS asserts the scenario's "other five presets are unchanged" clause at `ThemeResetTests.swift:79`, and Android's `ThemeResetTest.kt` has four tests and no counterpart. **No verdict above rests on a behaviour anyone watched.** Android carries one instrumented sheet test, `ThemeSheetSemanticsTest.kt`, and iOS carries theme steps inside `UITests/SweepEpubReader.swift`; this audit ran neither, and read the source on both platforms instead. One question stays open and is not this audit's to settle: whether `theme.pageColour.ratio` keeps *Contrast %@ to 1* or becomes three plain bands -- see `one-vocabulary-in-four-languages` |
| `page-transitions` | partial | 17 scenarios: 6 built and tested on both, 7 built and asserted by nothing, 3 one-platform, 1 missing on both. **This is the first audit of the capability.** The row it replaces recorded only that the spec was split out of `comic-reader` and `ebook-reader` on 2026-08-30. Every mode reaches a reader on both platforms: the comic picker at `ReaderMenuSettings.swift:55-105` and `ReaderControls.kt:116-178` writes through `ReaderModel.choose` (`ReaderModel.swift:168`) and `ReaderViewModel.choose` (`ReaderViewModel.kt:181`), the ebook picker at `PageTurnSection.swift:21-66` and `ThemeAxesScreen.kt:230` writes through `EpubReaderModel.swift:320` and `EpubReaderViewModel.kt:268`, and the container switches on `choices.effective` at `ReaderPages.swift:33-39` and `ReaderScreen.kt:872-931`. `TransitionChoices` is a mirrored pure type (`TransitionChoices.swift:72-172`, `TransitionChoices.kt:143-237`) and both suites assert it case for case (`ShelfMemoryTests.swift:119-268`, `ReadingThemeTest.kt:393-549`). **iOS never withholds Curl, so its absent-Curl path has no production caller.** `ReaderModel.swift:99` defaults `canCurl` to `true`, and a grep over `apps/ios` finds `false` passed only in `ShelfMemoryTests.swift:137`. Android's gate is real in the comic reader -- `ReaderViewModel.kt:89` reads `Build.VERSION.SDK_INT >= TIRAMISU` -- while its ebook reader hardcodes `canCurl = true` (`EpubReaderViewModel.kt:258`), as iOS does (`EpubReaderModel.swift:310`). So the device answer ships in one reader on one platform. **Two scenarios turn on that single fact, so both carry the same verdict.** *Curl is not offered where it cannot be honest* and *Every other mode stays available* both open on "Curl is absent on a device", and one mirrored case asserts each of them: `ShelfMemoryTests.swift:134-147` and `ReadingThemeTest.kt:417-432` assert the absent row, the Slide fallback, Fast fade still offered, and the stored choice left alone. This repository counts code that is declared and never reached as absent, so **both scenarios are one platform only**. An earlier draft of this row scored the second one built and tested, and so counted one more scenario as shipping on both readers than the code does. The iOS argument is written down and is defensible: iOS 26 is the floor and every device it admits runs the shader. The clause's other half, a device that "cannot render it at the display's refresh rate", is implemented by neither. **Reduce Motion turned off mid-session restores the mode on iOS and does not on Android.** iOS reads `@Environment(\.accessibilityReduceMotion)` (`ReaderView.swift:21`), which SwiftUI re-renders on. Android reads `Settings.Global.ANIMATOR_DURATION_SCALE` inside a getter (`ReaderViewModel.kt:128-133`, `EpubReaderViewModel.kt:280-285`) and registers no observer, so `viewModel.transitions(settings)` (`ReaderScreen.kt:304`) recomputes only when something unrelated recomposes. The fix pattern is already in the codebase: `HighContrast.kt:99-109` registers a `ContentObserver` on a settings key. **No frame rate has been measured on either platform and *Frame budget* is still missing on both, but the instrument that would measure it now exists on both.** `FrameRun` counts the frames a turn delivered and the frames it dropped, and is mirrored (`FrameRun.swift`, `FrameRun.kt`) with eleven cases a side over the arithmetic (`FrameRunTests.swift`, `FrameRunTest.kt`). The frames come from the platform's own clock -- `CADisplayLink` at `FrameProbe.swift:76`, `Choreographer` at `FrameProbe.kt:96` -- and the interval each frame is judged against is read off the display rather than named in code, so no source and no test states 60 or 120. Both curls drive it from the first drag to the end of the settle (`CurledPages.swift:117,151`, `CurledPages.kt:105,133`). **It is off in ordinary use, twice over on Android.** iOS arms on the launch argument `-StoryArcFrameProbe YES`, which reaches `UserDefaults` through the argument domain and which no launch from the Home screen writes; Android arms on a debuggable build *and* the global setting `storyarc_frame_probe`, which only `adb` can write. Unarmed, neither platform creates a frame callback. `node scripts/measure-turn.mjs --ios` and `--android` arm it, print what the device reported and put the switch back, following `capture-android.mjs` in restoring the device on the way out. **The scenario stays missing because the number is still untaken.** A simulator draws at its Mac's refresh rate and an emulator at its host's, so a rate measured on a build machine is a fact about the build machine; nothing asserts a rate anywhere and nothing may. Spike tasks 0.3 and 0.4 both close with "no number, and it needs a device", and that is still true -- what changed is that taking it is now one command against a phone. **One thing the first iOS run is expected to find:** `CADisableMinimumFrameDurationOnPhone` is absent from `apps/ios/project.yml`, and without that key iOS caps an app at 60 frames a second on a ProMotion iPhone whatever the panel can do. So the first number off an iPhone 17 Pro Max is likely to describe that key's absence rather than the curl, and the plist is the fix rather than the shader. **The curl's drawing is proved by frames rather than by an assertion, and only on iOS.** `docs/designs/screenshots/ios-curl-2026-09-05/` holds `ios-curl-fold-84.png`, `-77`, `-70`, `-62` and `ios-curl-settled.png` -- the fold at four consecutive positions, which is what separates a fold that tracks the finger from one painted at a fixed fraction. **Android has no committed frame at all**; its emulator run lives in `tasks.md` §4.3 and nowhere else. A frame proves a moment, not a transition, so *The page follows the finger* is scored asserted by nothing. What both suites do assert is the arithmetic: `CurlTurn` is mirrored and carries twelve cases a side (`CurlTurnTests.swift:20-97`, `CurlTurnTest.kt:25-97`) over the finger offset, the mid-settle base, the halfway rule and the flick override. The two shaders are twins down to their constants -- `crease` 0.06, `shadow` 0.05, `back` 0.55, which each platform holds beside its shader rather than inside it (`CurledPages.swift:156-158`, `PageCurl.kt:61-67`) -- and nothing asserts they stay twins. **The two flick detectors are different measurements.** iOS thresholds SwiftUI's `predictedEndTranslation` at 40 points (`CurledPages.swift:128-133`, the 40 itself at `:164`); Android thresholds the last move event's dx at 12 px (`CurledPages.kt:117`, the 12 itself at `:160`). Both feed the same asserted rule, so `CurlTurn.settles` is right on both and the gesture that reaches it is not the same gesture. **The boundary rubber-band belongs to the container, so the two container-less modes have none.** Slide and Scroll take overscroll from `TabView` and `ScrollView` (`ReaderContainers.swift:36,66`) and from `HorizontalPager` and `LazyColumn` (`ReaderScreen.kt:887,910`). In Curl, `CurlTurn.progress` clamps to 0 and 1 on both sides, which is a hard stop and not a resist-and-return; Fast fade has no drag to resist. Neither platform asserts the boundary, so *Turning at a boundary* is asserted by nothing. **A curl over a page that has not decoded shows the outgoing page again, not a placeholder.** `CurledPages.swift:97` and `PageCurl.kt:150` both reuse the leaving page when there is no page beneath. The 2:3 placeholder is real and lives only in a stitched scroll (`ReaderPage.swift:222-259`, `ReaderScreen.kt:781-793`); task 4.8 argues a paged mode needs none, which holds for Slide and Fast fade and does not hold for the curl's reveal. Neither side asserts the ratio, so *The next page is not ready* is asserted by nothing. **The horizontal scroll axis is never read from a publication.** `ScrollAxis.implied` takes `declaresHorizontal`, and both readers hardcode it to `true` (`ReaderModel.swift:149`, `ReaderViewModel.kt:160`), so production reaches two of the table's four rows. The table itself is asserted on both (`ShelfMemoryTests.swift:169`, `ReadingThemeTest.kt:455`), and the tallness input is measured and wired (`ReaderModel.swift:220`, `ReaderViewModel.kt:739`), so the observable outcome still matches the scenario. **The reflowable turn has a test on Android and none on iOS.** `TurnDragTest.kt` holds six cases over `TurnDrag.direction` and the fade's two phases; iOS keeps the same threshold inside a `UIPanGestureRecognizer` handler (`ReflowableTurn.swift:104`), and `apps/ios/Packages/StoryArcEpub/Tests` holds no transition case at all. The durations do agree -- 240 ms over reflowable text on both (`EpubReaderOpening.swift:222`, `ReflowableTurn.kt:185`) and 140 ms in the comic readers (`ReaderContainers.swift:199`, `ReaderScreen.kt:1723`). **Tap zones and Hardware input are pinned by source-text tripwires, not by assertions, so both are asserted by nothing.** `ReaderGestureTests.swift:96-115` and `ReaderGestureTest.kt:58-71` check that a spelling is still in a file. Android's list does not name the keyboard, and neither names the volume path. Volume keys ship on Android only -- `MainActivity.kt:73-82` consumes them when the setting is on and `ReaderScreen.kt:712-722` supplies the handler -- and iOS states the reason in place of a switch (`AppearanceSettings.swift:132`), which is what the scenario asks for. **One stale test name on iOS**: `ShelfMemoryTests.swift:199` is titled "refuses the two modes that need a picture of a page" while its own line 212 asserts Fast fade is available. The title predates `canFade`; Android's mirror at `ReadingThemeTest.kt:473` is titled correctly. **Six scenarios are built and tested on both, and these are their names**: *Scroll axis*, *A mode is unavailable for the content*, *Release behaviour*, *The curl is interruptible*, *Curl direction respects reading direction* and *Reduce Motion is on*. The release rule is asserted at `CurlTurnTests.swift:76-91` and `CurlTurnTest.kt:76-91`, and the interruption rule at `CurlTurnTests.swift:41-72` and `CurlTurnTest.kt:44-72`. **Two scenarios that read like members of that list are not members of it.** *Choosing a mode* has the mode table and the per-series persistence asserted (`ShelfMemoryTests.swift:270-279`, `ReadingThemeTest.kt:551-565`), and no case in any of the four test trees asserts its remaining clause, that a new mode applies "without losing the reading position", so the scenario is scored built and asserted by nothing. *Memory during a curl* has the prefetch window narrowing under pressure asserted (`PrefetchWindowTests.swift`, `PrefetchWindowTest.kt`, `TrimPressureTest.kt`, `ReaderModelTests.swift:107-120`), and its other half -- that a curl holds no raster of its own -- is structurally true, because both curls take two already-decoded pages by reference, and is asserted by nothing. **Outstanding, and unchanged since the row was split**: the curl over reflowable content (task 4.3b, both readers refuse it and say why) and a double-page spread curling as one surface. |

## In flight: `audiobooks-and-playback`; archived on 2026-09-05: `brand-identity-and-app-icons`
## and `named-failures-and-quieter-chrome`; and what the wave of 2026-09-01 landed

`quiet-shell-and-search` **archived** on 2026-09-01 as `2026-09-01-quiet-shell-and-search`, and
`quiet-reader` as `2026-09-01-quiet-reader`. Their sections below are kept because the
capability rows have not been re-audited against them.

Recorded here rather than as capability rows, because two of the capabilities involved —
`navigation-shell` and `home-screen` — **have no main spec at all**. They are new
capabilities that `one-library-three-destinations` introduces and has not synced, so there
is nothing for a row to be a status *of* yet. When either change syncs, this becomes rows.

Last updated **2026-09-04**. The per-capability rows below predate the day's
merges and are therefore **understated** for `comic-reader`, `ebook-reader`, `reading-themes`
and `settings-and-about`. They have not been re-audited scenario by scenario, and this section
says what landed rather than pretending they have. `library-browsing` and `native-experience`
were on that list until 2026-09-06. Both were re-audited in part on that day, and each row
names the half it read and the half it carried.

**One row is not merely understated but describes a shape the app has left.** The
`library-browsing` row still says recent searches are reached through the shelf's own
`.searchable` field. That was true before `quiet-shell-and-search` §1; search is a destination
with a screen of its own now. Left rather than patched, because patching one sentence inside a
row nobody has re-audited produces a row that is half-current and reads as wholly current —
which is the failure mode this whole section exists to avoid.

### `quiet-shell-and-search` — sections 1, 2 and 3 complete on both platforms

Search is a fourth destination rather than a role. iOS drew it as `Tab(role: .search)`,
which morphs the tab into a text field in place, so the bar changed shape under the
reader's thumb and there was nowhere to land; Android had no search in its navigation at
all. Both now draw four destinations, photographed before and after in
[`before-2026-08-31c/`](../designs/screenshots/before-2026-08-31c/README.md) and
[`after-2026-08-31c/`](../designs/screenshots/after-2026-08-31c/README.md).

The Android bar stays edge-to-edge and iOS keeps its floating capsule; that divergence is
**structural rather than stylistic** — `ShortNavigationBar` exposes no `shape` parameter at
all, so a capsule is not discouraged there, it is inexpressible.

| | iOS | Android |
| --- | --- | --- |
| The search bar on its own page | yes | yes |
| Scope stated and narrowable, persisted | yes | yes |
| Narrowing stops the fan-out, not just the rows | yes | yes |
| Suggestions before a query | yes | yes |
| Nothing-to-suggest state | yes | yes |
| Recent searches persist, clear, and record nothing on arrival | yes | yes |
| What's new, once per version | yes | yes |

Android's suggestions are its **own** type over `LibraryIndex`, not a port: iOS's composes
over its own home shelves and has no Android counterpart. Android's search captures are in
[`after-2026-08-31e/`](../designs/screenshots/after-2026-08-31e/README.md).

Two things are a **product choice, not a Material or HIG pattern**, and neither codebase
claims a guideline for them: the suggestions themselves (Material knows only historical
suggestions before typing) and the nothing-to-suggest state.

### `quiet-reader` — sections 1 to 5 complete on both platforms

Revealed chrome is two controls: a way out and a way in. iOS went from seven over the page
to two; Android from nine and a page slider to two in one `HorizontalFloatingToolbar`.
A reflowable publication states its position in words and offers no slider; a comic keeps
its slider, in the menu. The theme surface opens on the six presets with the axes behind
one action, and a modified preset is restored **by name**.

Captures in [`after-2026-08-31d/`](../designs/screenshots/after-2026-08-31d/README.md), and
they earned their place — **the pair found two defects no source-level test could see**:

- **The reflowable reader never withdrew its chrome.** `comic-reader` has required a
  four-second fade since it was written; the comic reader did it and `EpubReaderView` only
  ever *toggled*. In the pre-change tree the arrival frame and a frame taken six seconds
  later untouched have the **same SHA-256**. Fixed, and pinned on both readers.
- **`openTheEpubReader` had been silently skipping.** Its landmark was the themes control
  that moved into the menu. A skip passes, so `pnpm check` stayed green while the
  theme-sheet capture quietly photographed nothing.

One requirement was corrected rather than the code: *Entering the reader* said "chrome is
hidden", and **no reader has ever done that**. The delta now describes showing the controls
once and withdrawing them, which is the better half and what Apple Books does.

A real defect was also found by the tests: `ReadingTheme.restored()` was spelled as
`adopting(_:)`'s body on **both** platforms, so a reader who made a custom palette, chose
Calm and nudged line spacing lost the palette by putting the line spacing back.

Still outstanding: the largest-text-size captures on both platforms.

### `audiobooks-and-playback` — a player on both platforms, and not a finished one

**What works.** Audiobooks index as publications and play. One session object with two
implementations on each platform, and the surfaces cannot tell which is behind them —
`Playback` in `StoryArcKit`, `:core:playback` on Android. A compact bar and a full player on
both. `FormatSniffer` reads MP4, MP3, FLAC and Ogg from their bytes; an `aax `/`aaxc` brand
is its own container case, so the refusal is structural rather than a message a caller picks.
`FolderKind` decides comic-or-audiobook by majority. Seven byte-deterministic fixtures.

iOS holds the audio session as `.spokenAudio` with the interruption and route rules
read-aloud already had. Android runs a real media3 `MediaLibraryService` — `mediaPlayback`
foreground type, media3's own notification, `automotive_app_desc.xml` — asserted of the
**installed package** through `PackageManager` rather than of a file, and 5/5 on a device.

**Both readers' read-aloud session table moved into the shared model rather than being
copied**, so "one session type" is a fact about the build.

**Five defects found on a device that no unit test would have caught**, each now fixed: one
stray `.m4b` beside a library's folders made the *whole library* one audiobook; iOS's
`listen` did not hold the file's security scope, so tapping an audiobook did nothing at all;
Android's `LibraryScanner` had gates the indexer never saw, so **every `.aax` was dropped in
silence** — directly contradicting "refused by name"; the Android player's title sat under
the status-bar clock; and Material ships `Replay5`, `Replay10`, `Replay30` and **no
`Replay15`**, so a numbered glyph would have drawn "10" on a control that moves fifteen.

**Four plan claims were wrong and are corrected in `design.md`.** The chapter call takes the
asset's own locales, not `["en"]` — the fixtures declare `und`, as most real audiobooks do,
and asking for English returns zero groups, which is indistinguishable from an unchaptered
file. A truncated single file cannot be caught at index time, because `+faststart` leaves its
`moov` intact. Android already had a `mediaPlayback` foreground service for read-aloud. And
media3 1.10.0 **does** parse ID3 chapters — only MP4 atoms need the 1.11.0 bump.

**What does not work yet**, and none of it is silent:

- **Position is never written.** Closing an audiobook loses the place on both platforms.
  `ReadingPosition` needed a third case; its shape is now decided in `design.md` and the work
  is in flight.
- **The interruption rule is not connected on Android's audiobook path.** media3 handles
  focus itself and reports a loss as `onIsPlayingChanged(false)`, which that code reads as
  the listener pausing — so "a pause the listener made is never undone" is unenforced there.
  Read-aloud does connect it.
- **Read-aloud does not drive the player yet.** It was reported blocked on Readium having no
  speech rate; that is true of the configuration and **not** of the library — Readium injects
  the engine and its delegate exists to customise the utterance. Decided in `design.md`,
  in flight.
- `onPlaybackResumption` is written and never exercised; Android Auto has no browse tree; the
  compact bar sits over the rail on a tablet; the sleep timer, the remembered speed and the
  configurable intervals are not built; nothing has been **heard** on either platform.

**13 of 45 tasks done, 14 partial.** The partials are labelled per platform rather than
rounded up.

### `brand-identity-and-app-icons` — the palette moved and the icons shipped

**§1 is complete and §2 was already.** The accent is now a violet from the middle of the app's
own mark — `#8A4DF0`, **one value on every appearance**, measuring 4.06:1 on the dark canvas and
4.43:1 on the light against a 3:1 floor. The mark's pink reaches 2.48:1 on paper, so it could
not be the accent without a second token existing to cover for it; it takes the secondary role
instead, which is where a two-pole identity wants it. `ember`, `emberStrong`, `emberMuted` and
`ink` are gone. `arcMid`, `arcLate`, `arcEnd` and `iconPlate` are identity-only, and a
source-level guard on **each** platform fails if one reaches a chrome accent — mirrored rather
than written once, because `pnpm test:ios` cannot fail for a Kotlin violation.

**Two defects came out of the move, and neither was in the plan.**

- **iOS's global tint disagreed with its accent token.** `AccentColor.colorset` is
  `ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME`, so it is the tint every unstyled control
  draws itself in. It is generated, and the generator's own constants were named
  `accent`/`accentStrong` for the two *pink* values — so after the rename they meant the
  opposite of the token names and the app shipped the pink. No compiler reads an asset
  catalogue, which is why a rename could not find it.
- **A primary button's label was under its contrast floor.** The gate checked an accent
  *against* a canvas and never what may be drawn *on* it. A near-black label measured 6.91:1 on
  the old amber and 4.06:1 on the violet, under WCAG's 4.5 for normal-size text. Pure white is
  the only value in the set that clears it. The gate now checks both halves of every pair, and
  Android gained the brand-scheme test it had never had.

**§1.7 closed the part a reader would notice.** The accent now reaches tab bars, chips,
sliders and progress ticks on both platforms, pinned by `AccentReachesTheControlsTests` on iOS
and `AccentReachesTheControlsTest` plus `BrandSchemeTest` on Android. Three of the four control
kinds were already accented on iOS by one line — `ThemeResolver`'s `.tint` reaches every
unstyled control — and the fourth was not: a cover's progress rail drew its rail as
`.black.opacity(0.35)`, a scrim rather than a colour, so it read as mid-grey on a pale cover
and vanished on a dark one. It is `accentMuted` now, which is the role `design.md` had always
given that token and which nothing in the app had been doing. iOS's segmented control is left
neutral **deliberately**, and the reason is recorded at §1.7 rather than left as an omission.

**§3, §4 and §5 are built on both platforms.** Five `.appiconset`s and five `.imageset` tiles
on iOS, five `<activity-alias>`es on Android, and a chooser per platform reached from
Appearance — searchable by six identical terms a side, announced by name and by whether it is
in use, and holding at the largest accessibility text size. The generator emits twenty-four
assets from one SVG. The refusal path is built on both sides, which is the scenario of this
kind most often specified and not built: the platform declining the change names the icon
still in use, and the refusal is asked once rather than on every visit.

**Two device findings changed the design rather than the code.** Making the default *be* the
manifest's own activity bricks the app — an `<activity-alias>` whose target is disabled stops
resolving, so the launcher goes on drawing an icon that does nothing and a reader reinstalls.
So there are five aliases, `MainActivityInk` among them, and no face may name the target
activity. And the platform is the *whole* store on both sides — `alternateIconName`,
the component enabled states — so nothing about the choice is written to preferences, a
backup, a log or a diagnostic. A delta scenario asking for a stored preference to be "kept
rather than erased" was rewritten to match that decision rather than implemented against it.

**§6.3's gap closed, and the premise is now photographed rather than argued.** A themed icon
tints the monochrome layer, and shipping real single-colour art for it is the whole reason that
layer exists. Turning themed icons on had been recorded as un-automatable; it was one launcher
restart away — the flags were already right and the launcher had simply not re-read them. The
frames in `docs/designs/screenshots/android-themed-icon-2026-09-04/` show all five faces tinted
flat with every cut, notch and negative-space gap intact, beside the same five in their own
colours. The wiring was already asserted without a device: `AppIconManifestTest` matches the
exact `<monochrome>` element per mipmap, so a face re-pointed at the gradient foreground fails.

**What the frames also settled is a limit worth publishing rather than burying.** All five
adaptive icons declare the same monochrome drawable, so themed mode collapses the five faces
into one — Arc and Paper draw identically once tinted. Correct for a themed icon, whose point
is to take the wallpaper's colour rather than its own, and it does mean the delta could not
promise that "each option is shown as the icon it actually is" without a qualifier. It now
carries one, on both the requirement and the scenario.

**36 of 36 tasks done; synced on 2026-09-04 (`8ee74e46`), verified and archived on 2026-09-05.** This paragraph read "35 of 36, 1 open — verify then sync" until the archive; before that it
said "7 of 36 done, 1 partial" and the four above it described §1.7, §3, §4 and §5 as unbuilt, for three days and
about thirty commits. `CLAUDE.md` tells every agent to read this file **before** claiming a
capability is missing, so an agent reading the old version would have built the chooser a
second time. Corrected 2026-09-04, on the evidence of a verification pass rather than a
re-read of the task list.

### `named-failures-and-quieter-chrome` — §1, §2, §3 and §3b complete on both platforms

**A failure names its publication, and the notice is a state rather than an event.** The
indexer's reasons are kept instead of counted, so one failure names its publication and its
reason and several state the count and lead to a list with a reason per row. The six-second
`dwell` is deleted: the notice sits inline above the shelf, takes its own space rather than
floating over covers, and is dismissed by the reader. One decision carries most of the
requirement — the scan **replaces** the list rather than accumulating, and prunes
acknowledgements to the surviving names — which is what makes a publication that later opens
leave the list without being dismissed, a deleted file leave the same way, the same set not
re-announce itself, and a fixed-then-broken-again publication be news. Four states rather
than a bool and a count, so *nothing failed* is distinguishable from *failures you have seen*.

**The library toolbar went from six unlabelled glyphs to two controls and two named menus.**
Six was a measurement, not the review's five, and it is corroborated by the revamp document
independently. The show and scope choices folded into the menus already there; *Select* stayed
on its own, because entering a mode is not the same kind of act as picking a sort, and the
test says so. Android needed no change to the same controls — it already used menus for three
of the six — and its sort chip now reads *Sort: Title* rather than *Title*, which was the one
place a value was passing for an ordering.

**The selection chrome was rebuilt, and the two platforms diverge on purpose.** iOS follows
Photos: the tab bar goes down, the actions are a glass capsule in its place, the count moves
to the navigation title and *Select* becomes *Done*. Android follows Material's contextual top
app bar: close at the start, the plural count as the title, and the navigation bar left alone,
because the foot of an Android window is the navigation bar's territory. Each platform's source
names the convention it follows and cites ADR-0001 as the licence. On both, the actions are
inert-but-shown at nothing selected and the exit is never disabled.

**§3b.7 is closed, and the pictures found two defects that every test in the repository had
passed.** iOS has eleven frames across six walks, Android eight across three routes. The open
question — whether the automatic button style colours its own labels over the ancestor
`.storyArcGlassText(.primary)` — is answered: the ancestor wins, the glyphs measuring
saturation 0.000 beside a *Done* at 0.679, which is right, because it leaves the accent marking
only what is picked and the way out.

The two defects were both invisible to source-text guards. The capsule drew **three bare glyphs
at the default text size**, not merely at the accessibility sizes — breaking a clause this
change had already written, since a `ViewThatFits` fallback is room-saving by construction. And
the **inert state was drawn identically to the live one** on both platforms, from mirror-image
causes: an explicit `foregroundStyle` on iOS and an explicit `Icon` tint on Android, each
defeating the dimming the platform would otherwise apply. Both are fixed, both now have checks
that measure rendered pixels rather than read source, and a third collision came out of the
same reading — Android's mark-as-read was drawn with the identical vector and tint that marks a
picked cover in the same frame, so it moved into the overflow where its name is drawn.

**A merge hazard this change surfaced, recorded because it is not about this change.** Two
open changes held `MODIFIED` deltas on `library-browsing` → *Presentation* with disjoint
scenario sets, so whichever synced second would have deleted the other's. `pnpm delta:drop`
gained a check for exactly that on 2026-09-04 and found **four** live instances. All four are
resolved and the sync order of three is recorded in `.delta-drops.json`.

### The wave of 2026-09-01 — landed, gated, and four defects out of it

Four changes merged in parallel. All gates green on the merged result: 1720 iOS tests in 216
suites, SwiftLint 0 violations in 611 files, both platforms building.

**The iOS sleep timer fires.** It was shipped and inert — `SleepTimer`, `SleepCountdown`,
`setSleepTimer` and `sleepTimerElapsed()` all existed, the sheet offered the durations, and
**nothing in the app ever ticked the countdown or called `sleepTimerElapsed()`**; the only
caller in the tree was a test. So "Sleep in 30 minutes" never stopped anything and the
remaining time never moved. `SleepTimer.swift` now mirrors Android case for case: one remaining
time for both kinds, end-of-chapter **re-read** from where the audio reached, a straight 30 s
ramp on the *player's* volume, and a rewind of the same 30 s. 32 tests, mutation-checked.
**Three numbers were iOS inventions against a decision already recorded for Android** — a 5 s
fade, a 10 s rewind and a sixth duration offering 10 minutes — and are now pinned by test.
`onRecallSpeed`/`onRememberSpeed` had **no caller anywhere**, so every book started at 1×.

**A failure names its publication.** Both scanners already emitted `ScanEvent.skipped(path,
reason)` with the sentence `publication-formats` words; iOS matched `case .skipped: break` and
Android `is ScanEvent.Skipped -> Unit`. **That one line each was the whole defect.** The
notice is state rather than an event now — mirrored `SkippedPublications` with a four-case
`Notice`, settled at the end of a whole scan rather than accumulated, which is what makes a
publication that later opens leave the list without being dismissed. The six-second `dwell` is
deleted, and it no longer floats over covers.

**Android's retired segmented buttons are connected button groups**, at **two** call sites,
with a third file cleared of dead imports — and the change's design named neither call site
correctly. Three *files* were touched, which is where the "three call sites" this paragraph
used to claim came from; `ThemeSheet.kt` imported the retired API and called nothing. The
guard the change installed encodes the real number (`callers.size >= 2`), and a sweep of all
354 Android main sources finds exactly the two.

**The player's presentation moved to a host its own transport cannot destroy.** Pressing any
transport control inside the full player dismissed it: `PlayerDock` attached the `.sheet` inside
`if let bar = centre.compact`, and `compact` is computed, so pressing play or crossing a chapter
rebuilt the presentation's own host. Pre-existing — the identical walk fails against the commit
before the Close pill was removed.

#### Four defects the wave produced rather than fixed, each worth carrying forward

- **A crash that passes every gate.** `MPMediaItemArtwork`'s handler inherited `@MainActor` and
  died with `EXC_BREAKPOINT` on `MPNowPlayingInfoCenter`'s own queue. `pnpm check` and
  `pnpm build:ios` both exit 0 on it; only the simulator found it. **Second instance of the
  trap `audiobooks-and-playback`'s design.md already records for Readium's `EngineFactory`.**
- **The refusal sentences are not translated.** The change's task list said they were "already
  worded and already translated". They are worded — as **English literals** in each platform's
  format layer — so the notice now puts English reason text into a four-language app. The agent
  correctly refused to invent localised variants, because the same task forbids a second
  wording for one condition. **Its own change, and outstanding.**
- **`CoverlessWell` is in the wrong module.** `audio-playback` §4.4b asks the player to draw the
  same coverless treatment the library draws, and the module graph forbids it: `CoverlessWell`
  lives in `LibraryFeature` and `Package.swift` records "no feature depends on another". Its
  right home is `DesignSystem`, which `LibraryFeature`, `PlayerFeature` and the app target all
  already depend on — so the move needs no new module edge. **Until it happens, two views draw
  one treatment**, which is the drift that file's own comment warns about.
- **`rar4-solid.cbr` never reaches a skip**, so a task naming it as one of two differently
  failing files was wrong; a solid RAR4 is *found* and marked unopenable on purpose.
  `password-protected.cbz` and a new `Locked Vault.cbz` are the two that do.

### Two artifact problems fixed, because both would have deleted a requirement

Neither is a capability's status, but both change what the specs *say*, which is what this file
is a status of.

- **`quiet-shell-and-search` can now be archived.** It carried a `## MODIFIED` delta for
  *Reaching search* against `navigation-shell`, a capability with **no main spec** — the one
  `one-library-three-destinations` creates. The plan of record was to wait for that change to
  sync. Re-reading found the wait was not merely slow: this change's own proposal said the two
  must be reconciled *if* they touched the same requirement, and they always had. The rewritten
  statement moved to the change that creates the capability.
- **A gate now refuses the class of defect that caused it.** `pnpm delta:drop`, inside
  `pnpm lint`, fails when a MODIFIED delta would drop a scenario or a normative clause the main
  spec already holds. `openspec validate` does not check this — it checks the delta's own shape,
  never what the delta would displace, so only `openspec archive` notices, by which point the
  change directory has moved. On its first real run it found a live one: archiving
  `reader-theming-and-page-transitions` would have deleted the two-level theme surface
  `quiet-reader` synced, **including the clause that a reader wanting a preset must not pass an
  axis to reach it**.

## What blocks what

`sources` is the keystone. Five capabilities wait on it, and so do the three open tasks
in `format-scope-and-libraries`:

```
sources (registry, credentials, cache, health)
  ├── opds-catalog        built on both except catalogue search, which is iOS-only
  ├── kavita-server ──── reading-progress: synchronisation pulls, and drops what it
  │                       would push; the silent-adopt rule is unreachable
  ├── network-share
  └── offline-downloads ─ comic-reader: "offer to delete the download"   built on both,
                          as a gated sweep with a ten-second undo rather than an end-screen offer
                        └ settings-and-about: clearable downloads
                        └ format-scope-and-libraries 5.2, 5.3, 6.5
```

`collections-and-reading-lists` depends on `sources` only for its server-backed half. Its
local half — a collection a reader makes themselves — is built on both platforms, with two
holes in it that belong to no server: a collection is deleted without confirmation, and its
cover cannot be chosen.

## Where the two platforms differ

They are near line-for-line equal wherever code exists. **Nineteen** scenarios are not
equal, and every one of them is named in its own row above. These are the divergences that
are deliberate — the rest are gaps, not decisions.

Twenty until 2026-08-31, when Kavita's publication status and age rating became built on
both. The count moves in both directions, and the day's other attempt at moving it is a
caution rather than a data point: an iOS audit of the EPUB reader was written, reported zero
findings, and had in fact measured the publication page — the action element it tapped was not
hittable. It now proves it arrived and skips instead, so iOS's EPUB reader is **unaudited**
and Android's unnamed WebView on that screen stands unmatched. A number that moves because a
check measured the wrong screen has not moved.

1. **Volume-button page turns are Android-only.** Deliberate: iOS cannot capture the
   volume buttons within App Store rules, and the app says so rather than shipping a dead
   switch. `AppSettings.turnPagesWithVolumeButtons` nonetheless exists on iOS and drives
   nothing.
2. **A PDF's document outline is iOS-only.** `PDFKit` reads one and
   `android.graphics.pdf` exposes none, so the find sheet has a third tab on one platform
   and not the other -- absent, never empty. Selection, copy, marking and in-publication
   search are on both, over each platform's own PDF library.
   [ADR-0012](../decisions/0012-pdf-text-on-android.md).
3. **Android has a manual library refresh; iOS has none.** Both watch, by completely
   different means: iOS opens a kernel event source on each directory, because a vnode
   event says nothing about what happened a level down, and caps how many it opens;
   Android registers one content observer per picked folder, which already carries
   descendants. Neither has been watched working, and on iOS the foreground reconcile
   that would back them up is applied to no view.
4. **Reordering sources is a drag on iOS and two buttons on Android.** `List.onMove`
   gives iOS the drag the spec describes, and gives VoiceOver its own reorder actions with
   it. Compose has no drag-to-reorder; hand-rolling one is a long-press gesture, an
   auto-scroll and a set of semantics actions that a screen reader would still need spelled
   out — so Android mirrors the download queue in the same app, which chose two buttons for
   the same reason. Both persist the same order. A reading list reorders the same way on
   each platform, for the same reason.
5. **Predictive back is drawn by the system on iOS and, inside the app, by the app on
   Android.** iOS pushes onto a `NavigationStack`, which animates the interactive pop
   itself. Android's in-app navigation is the app's own state, so the app has to draw the
   preview of a screen leaving. **The divergence in mechanism stands; the gap in behaviour
   is closed.** `PredictiveBackHost` had no call sites at all until 2026-08-31. It wraps
   `SettingsScreen`, and it now also wraps the shell's navigation area, where the app's one
   consolidated back rule lives — that rule was a plain `BackHandler` and is a
   `PredictiveBack` handler. Two hosts and not one: the composition local is static, so
   Settings' inner host owns the gesture while a reader is inside its three levels and the
   outer one owns every other in-app destination, each applying its transform once. Around
   the navigation area rather than the whole shell, because back never takes the navigation
   control away — the screen leaves and the bar or rail stays. **The wiring has not been
   watched on a device**: the transform is unit-tested as a value (`BackPreview`,
   `backPreview()`), the composition that applies it is reachable by no test in this
   repository, and what a reader sees is a frame rather than a value.
6. **The reader's selection menu diverges on purpose.** iOS refuses the system edit menu
   and anchors a popover of five swatches to the words; Android puts named colours into
   the `ActionMode` the platform already raises, which is where an Android reader looks
   and is also what a screen reader can read out. Both offer the four things the spec names.
7. **The cleartext exception is wider on Android.** A self-hosted catalogue usually answers
   over plain HTTP on a `.local` name or a private address. iOS says exactly that with
   `NSAllowsLocalNetworking`; Android's network security config can name a host or an IP
   literal but not a range, so the exception there covers every host. Both platforms
   complete a typed address with no scheme to `https`, so reaching a server over cleartext
   takes a reader typing `http://`.
8. **Device-driven verification is Android's.** `pnpm smoke:android` walks every route on
   a connected device and asks logcat whether the process died; `pnpm a11y:android` reads
   the real accessibility tree at real density; `pnpm pseudo:android` walks nine routes
   under `en-XA`. iOS has one simulator-bound UI test, scoped to the library and currently
   `XCTExpectFailure`d. This one is not deliberate — it is where the tooling happened to
   get built.

## What the checks cover

| Check | Covers |
| --- | --- |
| `pnpm lint` | specs *and changes*, tokens, generated notices, fixtures, the libarchive pin, the iOS lockfile, the corpus, `strings:ios`, the brand assets' committed bytes, the 800-line ratchet, and `delta:drop` |
| `pnpm delta:drop` | a `## MODIFIED` delta that would delete a scenario or a normative clause the main spec already holds. `openspec validate` checks the delta's own shape and never what it would displace, so without this only `openspec archive` notices — after the change directory has moved. 15 self-test cases; `pnpm delta:drop:selftest` |
| `pnpm clean:builds` | not a check: reclaims orphaned build data, reports the shared caches' size and the volume's free space, and warns when a wave of agents will not fit |
| `pnpm clean:swift` | not a check either: removes the Swift packages' own build directories. A **stale** build presents as a `SIGSEGV` with no message and no failing assertion — an enum layout baked into an incrementally-rebuilt target. It does **not** explain a write-shaped failure: that one was a data-protection class, twice misdiagnosed. AGENTS.md §6 |
| `pnpm check` | `pnpm lint`, then both apps' lint and unit tests |
| `pnpm smoke:android` | sixteen routes open without crashing — the three destinations, the publication page, both readers and nine settings screens. Needs a device |
| `pnpm a11y:android` | unnamed controls, raw values as names, targets under 48dp. Needs a device |
| `pnpm strings:ios` | every key a Swift source asks for exists, and is translated in all four languages. Needs no device |
| `pnpm pseudo:android` | nine routes under `en-XA`: text that leaves its row, controls off the display, targets under 48dp. Needs a device |
| `AccessibilityAuditTests` | Apple's own audit, on the library only, and never at a forced content size |
| `pnpm corpus:check` | the generated test library is well-formed in every format |
| `pnpm opds` | not a check: a real OPDS server for the walkthrough a unit test cannot do |
| `pnpm kavita` | not a check either: a mock Kavita, because nobody here has a real one |
| `pnpm smb` | not a check: a real Samba server, including `--encrypted` |

**CI is narrower than that table looks.** `.github/workflows/contract.yml` runs
`spec:validate`, `tokens:check`, `tokens:verify`, `notices:check`, `libarchive:pin`,
`lockfile:ios`, `fixtures:check` and two self-tests — it does **not** run `pnpm lint`, so
`strings:ios`, `corpus:check`, `brand:check`, `lines:check` and `delta:drop` are local-only
gates today. **`delta:drop` being local-only is the one that matters most**, because the defect
it catches is silent, is only detectable before an archive, and has already occurred three
times. The iOS and Android workflows
run each platform's own lint, unit tests and build. Nothing device-driven runs in CI, which
is why `smoke:android`, `a11y:android` and `pseudo:android` are walks somebody remembers
rather than gates.

Nothing anywhere compares a screenshot against a reference, which `native-experience` asks
for by name. There is no device matrix written down, no baseline directory and no image-diff
script; the thirty-five committed captures are ad hoc, and thirty-two of them are Android
settings screens. The pseudo-locale walk and the accessibility sweep both stop at the two
readers, because a reader needs a publication on the device and the emulator has none.

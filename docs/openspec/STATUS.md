# Implementation status

What is built against what is specified, capability by capability. Written because the
change task lists track three changes and the specs describe fifteen capabilities, so
"how much of StoryArc exists" had no answer anywhere.

Audited 2026-08-27 by reading every `specs/*/spec.md` against the app source on both
platforms, and kept current as things land. A capability is **built** when every requirement has code on both platforms,
**partial** when some do, **absent** when none do.

Keep this current. A status document that lags is worse than none, because it is believed.

| Capability | State | What is missing |
| --- | --- | --- |
| `publication-formats` | partial | CB7 decoder (declared open question); EPUB publisher, description, series, series index; EPUB spine-render cover fallback; re-decode on zoom; codec name in the placeholder; spread pairing; skipped-page count in the UI |
| `comic-reader` | partial | **Image adjustments are built on both, the whole requirement**: brightness, contrast, inversion and greyscale, live and per series; sharpness is on iOS and on Android from API 33, where a runtime shader exists. Border trimming detects a uniform white or black margin per page and can be turned off for a page it gets wrong; the detection is covered by eight tests a side, and it refuses a gradient, a mid-grey band and a blank page. The sliders and switches were driven end to end on both, on a simulator and on an emulator; trimming itself has only been run against synthetic pages, because the fixture corpus is gradients and has no margins to trim. **Reading direction can be overruled and the reader's orientation locked, on both platforms.** A direction chosen while reading outranks the publication's metadata for that series alone, is remembered beside the theme and the transition, and applies without losing the page: the run of pages reverses and the reader stays on the one they were reading. The lock holds the app at the way up the reader is already at and hands the rest of the app back to the device on the way out; it is not remembered, because the spec scopes it to the reader. Neither has been driven on a device. The direction mapping and the per-series store are unit-tested on both platforms, and both controls were seen rendered in the reader on a simulator, but nothing here can rotate a simulator or an emulator, so the lock has never been watched holding. **A page opened on a cold launch straight into the reader arrives fitted.** It did not before: iOS sized the page against a scroll view UIKit had not laid out yet, recorded that fit as applied, and never tried again, so the page sat at its own pixel size in a corner until something else changed the page, the mode or the size. A fit is now recorded only once there is a laid-out view to apply it to, and the scroll view's own layout applies the one still owed. Driven both ways on the Simulator from the same cold launch -- unfitted before the change, fitted after it, and the settled screenshots byte-identical, so nothing else moved. Android needed no change and got none: its fit is keyed on the size `onSizeChanged` reports, which is only ever a size the layout really has, so a fit taken before the first measurement is discarded rather than recorded. That rule is pinned by a unit test on both sides. chapter navigation; slider thumbnails; spread pairing; scroll separator; memory-pressure prefetch shrink |
| `ebook-reader` | partial | Highlights, notes; reading aloud (whole requirement); PDF text layer, search and outline are iOS APIs with no UI. **Bookmarks are built on both**: the record and its store are on both -- a fraction through the publication plus the opaque locator, because ADR-0006 says a reflowable page number is not stable -- and the reader screen is now on both. The control is one toggle, since a position is either marked or it is not, and the marks sit in a second tab of the contents sheet because the spec puts them "alongside the table of contents" and both answer the same question. The excerpt had no obvious source: Readium reports text on a locator that came from a search or a selection and none on one that came from a page turn, which is every locator a bookmark is made from, so the resource is read and cut at the fraction the locator names. The head is stripped with the scripts, or every excerpt opened by quoting the chapter's own title back -- seen on an emulator. Driven on an emulator end to end: marked, listed with its chapter and its words, drifted away, tapped the row and landed back on the same page. **The iOS screen has not been seen.** It compiles, lints and its logic is pinned by the same sixteen assertions the Android half is -- nine on the excerpt rules, seven on the store -- but the Simulator control this repo uses is down, and the fixture EPUB dropped into the app container did not reach the shelf, so no bookmark has been watched being made there. Two things did come out of trying: iOS reads its resource off the main actor the way Android reads its on `Dispatchers.IO`, which needed the same `nonisolated(unsafe)` note `SmbClient` already carries for a library written before strict concurrency; and a filter left behind by another run was still narrowing the shelf on the next launch, which is `library-browsing`'s filter persistence working. **Hyphenation is built on both**, the tenth theme axis: a switch beside bold rather than a slider, since a word either may break or it may not, and nil rather than false when the reader has not asked for it -- passing false would be StoryArc turning off a publisher's own hyphenation on every book that wanted it. It sits with the axes publisher CSS overrides, so Original cannot reach it, which the axis tests now assert at ten rather than nine. **Footnotes and the return control are built on both, and neither has been driven.** Readium already tells a note from a link on both platforms -- Android hands over a `FootnoteContext`, iOS asks `shouldNavigateToNoteAt` -- so each reader only had to decide what the two mean: a note is refused and shown in place, which keeps the reader on their page and their place in the sentence, and anything else writes down where they were first. iOS shows the note through the same popover-with-a-compact-adaptation the theme and contents sheets use, at a smaller detent, because a half-screen sheet for one sentence reads as a departure rather than an aside. The return control is offered on *every* long jump -- a contents entry, a search hit, a bookmark, a link -- rather than only on a link as the spec's letter says: they are the same act from the reader's side, and a control that appeared after one kind of jump and not another would look like a bug rather than a rule. It is taken once and goes away, or it would bounce a reader between two pages for ever. What stopped the driving: the shared emulator was reset mid-run by another build, and a hand-built EPUB carrying a noteref never reached the shelf, so the popover has been seen by no one. The control's absence before any jump *was* observed, which is the only half that got that far. **In-book search is built on both**, in the same sheet, which now has three tabs -- contents, bookmarks and search all answer the same question, so a reader who opened the wrong one would have had to close it to ask again. Readium walks the resources and reports a page of locators at a time, and results are published as they arrive rather than at the end: a reader looking for a word they know is in chapter two should not wait for chapter forty. A query typed over a running one takes over, and the overtaken walk stops publishing rather than filing its rows under the new question. `SearchSnippet` is the rule worth mirroring: the *match* survives whatever the budget is, even when it alone exceeds it, and what one side of the context does not spend the other may -- a hit near the start of a paragraph has almost nothing before it and plenty after, and taking the same number of characters from each side would waste the budget on one and cut the other. Nine assertions a side. Driven on an emulator: searched, saw the hits listed under their chapters with the word emboldened in place, tapped the one in chapter two and landed on the page that holds it. **The iOS search screen has not been seen**, for the reason the iOS bookmark screen has not. **Table of contents built on both. Fast fade over reflowable text is now built on both, and the two platforms take the turn over from Readium differently on purpose.** iOS has to disable Readium's paginated scroll -- that scroll view is what animates a Slide -- and then put back the swipe it just took away, which costs it an edge tap it would not otherwise install. Android's pager only turns once a horizontal drag passes the touch slop, and a parent view can intercept exactly that and nothing else, so taps never leave the web view and links, text selection and Readium's own input listener go on working. The fade itself differs for the same kind of reason: iOS rasterises the outgoing page and moves underneath it, because its `goForward` is async and overlapping the two is what keeps the turn feeling immediate; Android moves at the peak of the dip, where the page colour is already opaque, so there is nothing to snapshot. Driven on an emulator and recorded: the outgoing page fades to the page colour, the swap happens unseen, the arriving page fades in, and the two are never on screen together -- which is the whole reason this is a dip and not a cross-fade. Choosing Pages hands the turn back, and the swipe still works. The iOS half was also drawing its dip *under* the opaque still, so half of every turn there was invisible and the visible half started with a cut; fixed while reading it for the mirror. Curl is still refused on both, and still for the honest reason: it needs the incoming page as a second texture before it is on screen |
| `reading-progress` | partial | Synchronisation (whole requirement); conflict-resolution rules are written and unreachable. **The finished lifecycle is built on both**: a completion timestamp that is stamped once and never restamped — reopening a finished publication writes a new position, not a new completion — a finished publication that reopens at the beginning while keeping the record, and "Start from the beginning", confirmed, offered only where there is a position to clear. Four tests a side assert the timestamp table; the restart was driven on an emulator. Room went to version 2 with a written migration, never a destructive fallback. "Manual mark-read" was listed here and is in fact built — `AddToShelfMenu` and `AddToShelfSheet` have carried it for both local publications and Kavita chapters |
| `local-library` | built | Every requirement has code on both platforms, and none of the three newest has been seen running on a device or a simulator -- they are covered by unit tests and by nothing else. **Imported copies are built on both**: a publication picked from the file picker is copied into the download store, which already owns copied bytes and keeps them out of backups, and is listed under a fixed "On this device" source; the same file imported twice is one copy; an import is never swept away by "remove downloads after finishing", because nothing can be asked for it again; deleting one names the title, the space freed, and says the original elsewhere is untouched. **Watched changes are built on both**, and only their arithmetic mirrors: `FolderSnapshot` is the same type on both platforms and carries the two guards -- a walk that finds nothing removes nothing and does not overwrite a good snapshot -- while being *told* about a change is a dispatch source per directory on iOS, capped at 96, and one content observer per picked folder on Android. Whether either mechanism actually fires on iCloud Drive or on a given `DocumentsProvider` is exactly what has not been tried; returning to the foreground reconciles as well, which is the other scenario the spec names and does not depend on being notified. **The scan is resumable on both**: it writes down what it has indexed every two dozen publications, and the next scan of that folder restores them and opens none of them again. The journal is cleared when a scan finishes, so it is not the metadata cache `sources` still wants. **Open-in from another app works on both**, including a refusal that names the detected format |
| `library-browsing` | partial | Unified library across sources; search grouping by match kind; recent searches; date-added and file-size sorting; per-scope layout. **Filters: seven of the ten the spec names are built on both platforms** — read state, format, language, publisher, genre, tag and year range. They combine with AND, the badge counts each group once, one action clears them all, and every one of them survives a relaunch. The previous row said "8 of 11 filters" missing and was wrong twice: the spec names ten, and language was in the query and in the badge with no control on either platform, so a reader could not reach it. Genre and tag are new metadata, not just new controls: `ComicInfo.xml` defines `<Genre>` and `<Tags>` and neither parser read them. The year range is arbitrary in the query and the control offers the decades the library spans, because a menu cannot ask for two numbers. The remaining three are open questions in the spec rather than work items: publication status is in no file format this app reads, download state needs the library to know what `offline-downloads` holds, and source is the scope selector *Unified library* already asks for. EPUB `dc:subject` is still unread, so an EPUB carries no genre |
| `settings-and-about` | built | Nothing outstanding. **Search points at a single setting rather than only at its group: ten anchors a side, mirrored case for case, and choosing a match navigates, scrolls the row into view and tints it for two seconds before it fades. Privacy clears downloads beside the cache and the reading history, each row stating its own size — that row used to say nothing downloads yet, which stopped being true when `offline-downloads` landed.** The two search indexes had already drifted, "cache" reaching Downloads on iOS and Privacy on Android; they are realigned term for term and a test a side now asserts that every anchor is reachable. Driven on an emulator in light and dark: searched, navigated, scrolled and watched the tint fade |
| `localization` | built | The reader walk still only covers the library and the seven settings screens: the two readers need a publication on the device, which the emulator has none of. **Both gaps are closed.** `pnpm strings:ios` is the missing-key gate iOS never had — a `LocalizedStringKey` that matches nothing is not an error, not a warning and not a crash, it renders the key, and four shipped strings were doing exactly that: `about.version`, `theme.fontSize.position` and `theme.pageColour.refused` stored their keys positionally (`%1$@`), which SwiftUI never derives, and `catalogue.acquire.other` declared no placeholder for the argument its own translation spends. The About screen had been reading `about.version 0.1.0 1` on a simulator. The gate also fails on any key untranslated in any of the four languages, which is the clause the spec words as a build failure. `pnpm pseudo:android` is the pseudo-locale test: the debug build now sets `isPseudoLocalesEnabled`, without which `en-XA` is not compiled in and the walk would pass because nothing changed. It navigates by position, never by label — under a pseudo-locale there is no label to match — and reports text that leaves its row, controls off the display, and targets under 48dp. Nine routes pass with every string accented and half again as long. **Language override is built on both and switches in place**: Android carries the choice into the composition, iOS into the environment locale and into every `String(localized:)`, so neither recreates anything. Plurals and locale-correct byte formatting are done |
| `native-experience` | partial | Context menus; quick actions; widgets; handoff; tablet sidebar; foldables; scroll edge effect; launch and memory budgets. **Predictive back is built on Android**: the manifest opt-in was already there, and every screen the app can be inside now reports how far the gesture has travelled rather than only that it finished -- one host around the navigation area shrinks the leaving screen away from the finger, rounds its corners, and settles it back when the reader changes their mind. iOS needed nothing: its interactive back swipe is the `NavigationStack`'s own and works because nothing pushed onto the stack installs a competing horizontal drag; the reader is a full-screen cover on purpose, because a swipe that dismissed it would fight the page turn. **Haptics are built on both**, and deliberately two events wide: reaching the end of a publication, and a page turn the book refuses at its first page. A page turn, the chrome and the page slider get none -- a comic read at speed is two hundred turns, and a buzz on each is a defect. Both platforms use their own vocabulary, `SensoryFeedback` and `HapticFeedbackConstants`, never a raw vibration. **Cover-derived accent is built on both**, as a mirrored pair asserted case for case: a 32x32 colour census of the cover's thumbnail with paper, ink and grey abstaining, and the winner lightened or darkened until it clears 3:1 against a wash that itself clears 4.5:1 against the white text on it. A black-and-white cover derives nothing and the brand accent stands, which is the fallback the spec asks for. It reaches one screen -- the end-of-publication screen -- because that is the only surface in the reader with publication context and no artwork under it, and the non-negotiable is that chrome over a page never tints; the publication detail screen the design calls for does not exist yet. **Increase Contrast is built on both**: the tokens respond rather than each view deciding, so the subtle border becomes the strong one and the tertiary text tier steps up to secondary, and on iOS the glass chrome takes the same opaque fallback Reduce Transparency gets. Driven on a booted simulator in light and dark with the setting on and off, where the strengthened borders are visible; the end screen and the back gesture were **not** driven on a device, and the shared Android emulator was left alone |
| `sources` | built | Nothing outstanding, with one honest limit: the "cached" notice leaves when a walk completes, even a walk that saw nothing, because the scanner does not report whether it could read a folder — the shelf survives such a walk, but stops saying it is cached. **The metadata cache is built.** The catalogue is written to disk when a walk finishes and restored before the next one starts, so the library is on screen in a file read rather than a folder walk; covers likewise, keyed by publication *and* pixel size. Both live in the caches directory, which is what makes them "evictable independently of downloaded publications" without a line of code for it. A refresh updates incrementally — Android used to empty the shelf and rebuild it — and a publication a walk no longer finds is removed while its progress stays, because progress lives in its own store keyed by identity. Neither the removal nor the write fires on a walk that saw *nothing at all*: that is far more likely to be a folder it could not read than a reader who deleted every book, and emptying the shelf then is the opposite of what the capability promises. Driven on an emulator: with the file moved away, the shelf and its cover are still there. **Connection state is built on both**, with the retry loop on `SourceProbe`'s backoff. **Registry, credential storage, folder-as-source, OPDS-catalogue-as-source, renaming, Settings › Sources, removal and reordering are built.** |
| `offline-downloads` | partial | Background transfer is built on both. **Android is verified end to end**: a foreground service holds the process, a download started on the screen finishes with the app off it, and the service stops itself when the queue empties. **iOS is verified only in the foreground**: the transfer goes through a background `URLSession`, which is what lets the system carry it, and a download completes and lands through that path -- but the Simulator's transfer daemon never delivers a completion that lands while the app is suspended, and keeps reporting the task as live, so the suspended case needs a real device to confirm. A completion that arrives with nobody waiting is adopted, and a download nothing is carrying goes back in the queue. Automatic cleanup is built, unit-tested and wired on both platforms, but has not been seen working on a device. **Everything else is built on both: the queue holds for Wi-Fi and for a disk limit and resumes by itself, queued downloads reorder from the downloads view, a download resumes after the app is killed because the record carries everything the transfer needs, and finishing a publication removes its download reversibly -- the bytes are moved aside, not deleted, so the ten-second undo is a real undo** |
| `opds-catalog` | built | Nothing outstanding. **All three requirements are built on both platforms: adding a catalogue with sign-in and certificate pinning, browsing sections and paginated grids with search, and fetching a publication and opening it. An OPDS 2.0 feed's groups are now shown as the feed declares them -- each named group is a section of its own with a row of covers, a group that carries a `self` link offers a way into the rest of it, and a group the feed left unnamed is poured into the page because there is no title to head it with. Choosing a publication opens its own screen rather than a menu: the cover large, the title, authors, series, description and date the feed reports, a Read button that takes the format the app would pick, every other readable format as a row beneath it, and a plain sentence for what the catalogue offers that the app will not take -- a format with no decoder, or a loan, a purchase or a subscription.** Driven end to end on the Android emulator against `pnpm opds`: two groups, a `See all` into a group's own feed, a publication opened from a group's row, and Read downloading the PDF and opening it, in light and dark and at the largest text size. **Not driven on iOS** -- the simulator would take no synthetic input from this machine, so the iOS half rests on its unit tests and on compiling for the Simulator |
| `kavita-server` | built | Nothing outstanding. **Built on both platforms: authentication with token renewal, version check, extracting a base and key from a pasted OPDS URL, and reading libraries, series, volumes, chapters and server search. A reader pastes a server's OPDS URL, browses a library as a grid of covers with progress, reads the server's own summary and people rather than the file's, resumes at the chapter Kavita reports as next, and reads it. Reading position travels both ways: it is sent when the reader closes the chapter or leaves the app, held on disk when the server is unreachable, and flushed on the next connection; what other devices have read shows on the series grid. A chapter can be marked read or unread by hand, and the mark reaches the server. A server's own collections and reading lists appear beside the local ones, each labelled with the source it came from, and open to a grid of covers or a numbered run of chapters. A chapter can be added to one of the server's lists, and a publication from anywhere else is told why it cannot go in one.** Built against Kavita's documented API and the mock in `scripts/kavita-server.mjs`, never against a live server |
| `network-share` | partial | SMB 3 transport encryption is unmet, and now measured rather than assumed: jcifs-ng carries the negotiate context and no cipher, SMBClient offers SMB 2 only. A server that *requires* encryption is refused with a sentence naming the setting to change, which `scripts/smb-server.sh --encrypted` tests. **Everything else is built on both platforms: add a share and be told which failure happened, pick one discovered on the network or type it by hand, browse its tree to pick a root, read a comic streamed from it with nothing written to the device and the next pages prefetched, confirm first on a metered connection, and keep reading across a dropped connection.** Driven against a real Samba server with `server signing = mandatory`, authenticated. Discovery is verified on iOS only -- the Android emulator's NAT does not carry the host network's multicast. [ADR-0010](../decisions/0010-smb-clients.md) records the two clients |
| `collections-and-reading-lists` | partial | Converting a local list to a server one. Downloading a selection copies its files into the app's own download store rather than fetching them, because everything the library holds is already a file on the device; a publication read off a share is still kept offline from the reader instead. **A reader can create, rename by re-creating, delete and populate local collections and ordered reading lists on both platforms, reorder a list, see their position in it, add any publication to any number of them, and be offered the next entry in list order rather than the next in series when they finish one. A collection with no cover of its own is drawn from the artwork it holds: the first four member covers by identity ascending, as quadrants, or one cover across the frame below four. The library has a selection mode in both the grid and the list, with the count visible and one action out of it, and what is picked can be added to any collection or reading list, kept on the device, or marked read — through the same paths one publication already takes, with one undo for the whole set for ten seconds. A whole collection or reading list can be downloaded or marked read from its own screen, the item count and total size stated before anything is copied** |

## What blocks what

`sources` is the keystone. Five capabilities wait on it, and so do the three open tasks
in `format-scope-and-libraries`:

```
sources (registry, credentials, cache, health)
  ├── opds-catalog          built on both
  ├── kavita-server ──── reading-progress: synchronisation
  ├── network-share
  └── offline-downloads ─ comic-reader: "offer to delete the download"   partial on both
                        └ settings-and-about: clearable downloads
                        └ format-scope-and-libraries 5.2, 5.3, 6.5
```

`collections-and-reading-lists` depends on `sources` only for its server-backed half. Its
local half — a collection a reader makes themselves — is built on both platforms.

## Where the two platforms differ

They are near line-for-line equal wherever code exists. The real divergences:

1. **Volume-button page turns are Android-only.** Deliberate: iOS cannot capture the
   volume buttons within App Store rules, and the app says so rather than shipping a dead
   switch.
2. **PDF text layer, search and outline are iOS-only.** `PDFKit` provides them and
   `PdfRenderer` does not. `format-scope-and-libraries` makes that an iOS-only feature for
   1.0. No UI calls them on either platform yet.
3. **Android has a manual library refresh; iOS has none.** Both now watch, by
   completely different means: iOS opens a kernel event source on each directory,
   because a vnode event says nothing about what happened a level down, and caps how
   many it opens; Android registers one content observer per picked folder, which
   already carries descendants. Neither has been watched working.
4. **The cleartext exception is wider on Android.** A self-hosted catalogue usually answers
4. **Reordering sources is a drag on iOS and two buttons on Android.** `List.onMove`
   gives iOS the drag the spec describes, and gives VoiceOver its own reorder actions with
   it. Compose has no drag-to-reorder; hand-rolling one is a long-press gesture, an
   auto-scroll and a set of semantics actions that a screen reader would still need spelled
   out — so Android mirrors the download queue in the same app, which chose two buttons for
   the same reason. Both persist the same order.

5. **Predictive back is drawn by StoryArc on Android and by the system on iOS.** Android's in-app
   navigation is the app's own state, so the app draws the preview of a screen leaving. iOS pushes onto
   a `NavigationStack`, which animates the interactive pop itself. Neither app is missing anything --
   the same gesture does the same thing, and only one of them had to be written.
5. **The cleartext exception is wider on Android.** A self-hosted catalogue usually answers
   over plain HTTP on a `.local` name or a private address. iOS says exactly that with
   `NSAllowsLocalNetworking`; Android's network security config can name a host or an IP
   literal but not a range, so the exception there covers every host. Both platforms
   complete a typed address with no scheme to `https`, so reaching a server over cleartext
   takes a reader typing `http://`.

## What the checks cover

| Check | Covers |
| --- | --- |
| `pnpm check` | specs *and changes*, tokens, generated notices, fixtures, both apps' lint and unit tests |
| `pnpm smoke:android` | thirteen routes still open without crashing |
| `pnpm a11y:android` | unnamed controls, raw values as names, targets under 48dp |
| `pnpm strings:ios` | every key a Swift source asks for exists, and is translated in all four languages. Part of `pnpm lint`, so it needs no device |
| `pnpm pseudo:android` | nine routes under `en-XA`: text that leaves its row, controls off the display, targets under 48dp |
| `AccessibilityAuditTests` | Apple's own audit, on the library only |
| `pnpm corpus:check` | The generated test library is well-formed in every format |
| `pnpm opds` | Not a check: a real OPDS server for the walkthrough a unit test cannot do |
| `pnpm kavita` | Not a check either: a mock Kavita, because nobody here has a real one |

CI runs the first four. Nothing checks a screenshot against a reference, which
`native-experience` asks for by name and is now the only one of the three gaps left: a
missing iOS string is gated by `pnpm strings:ios`, and the pseudo-locale is walked by
`pnpm pseudo:android`. Neither of those two covers the readers — the walk needs a
publication on the device, and the emulator has none.

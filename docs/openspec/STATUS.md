# Implementation status

What is built against what is specified, capability by capability. Written because the
change task lists track four changes and the specs describe seventeen capabilities, so
"how much of StoryArc exists" had no answer anywhere.

Keep this current. A status document that lags is worse than none, because it is believed.

## The count

The seventeen specs hold **316 scenarios** today, counted from `docs/openspec/specs/*/spec.md`.
Two of those specs -- `reading-themes` and `page-transitions` -- were split out of
`ebook-reader` and `comic-reader` on 2026-08-30 when the reader-theming change was finally
synced, which is why the audited count below is 266 rather than 315: their 31 scenarios were
read in their old homes, and the two new rows say so rather than claiming a fresh audit.
Every one of them has been read against the app source on both platforms, scenario by
scenario, with `path:line` evidence, over two audit rounds ending 2026-08-30. That produced
**269 verdicts**: three scenarios in `collections-and-reading-lists` carry two independent
clauses each, and averaging a half-built scenario is how a half-built scenario reads as
done, so those were scored twice. Where a verdict from the earlier round disagreed with the
code — reading aloud, Kavita server search, the five `sources` gaps, the two `comic-reader`
defects all landed after it was taken — the code won and the verdict was re-checked by hand
before it was written down here.

**These counts are summed from the rows below, 2026-08-31, and that is a change worth
saying out loud.** They used to be maintained by hand and had stopped agreeing with the
rows: the header claimed 21 `one platform only` while the rows named 17, and claimed 137
`built and tested` where the rows held 142. Four hands editing rows in one day, each
adjusting the header from whatever base its worktree was cut at, is how that happens and
would have kept happening. The header is now derived, so a row and the total cannot drift
apart again — and it means **the totals below no longer reproduce the 269 figure the
paragraph above describes**. The rows come to 271. Two verdicts are unaccounted for
somewhere between the two audit rounds and the double-scored clauses, and rather than move
a number to make the arithmetic close, this says so.

| Verdict | Count | Means |
| --- | --- | --- |
| built and tested | 148 | code on both platforms, and a test on each side asserts it |
| built, asserted by nothing | 89 | code on both platforms, no test anywhere touches it |
| one platform only | 17 | code on one side; on the other it is absent, or declared and never reached |
| missing | 16 | no running implementation on either platform |
| unclear | 1 | could not be settled from source (`localization`, long-translation layout) |

**Four of the seventeen `one platform only` are not gaps.** The `local-library` spec splits
*Adding a folder* and *Remembering an opened file* per platform by construction, so each
consumes a slot while being complete on both sides. The real divergences are thirteen, and
"Where the two platforms differ" below names them.

**None of those numbers says anyone watched the behaviour happen.** That is a third axis,
and it is per-row: a great deal of what landed in the last few rounds compiles, lints,
passes its unit tests and has been seen by nobody, because the Simulator control this repo
uses has been down for most of them and an emulator was not always available. Every row
below says which of its parts were driven, on what, and which were not. Where a row says
"not driven", read it literally.

A capability is **built** when every scenario has code on both platforms, **partial** when
some do, **absent** when none do. All fifteen are `partial`. Two rows were wrongly marked
`built` before this audit and are not any more; nothing here is marked `built` on optimism.

| Capability | State | Where it stands |
| --- | --- | --- |
| `publication-formats` | partial | 16 scenarios: 15 built and tested on both, 1 built and asserted by nothing. **A publication's publisher, description, series and series index are read out of an EPUB, on both platforms.** The series is stated two ways and both are read: EPUB 3 defines `belongs-to-collection` refined by `group-position`, EPUB 2 defines nothing at all, and Calibre's `calibre:series` filled that gap so widely that a reader's library is mostly that. A file carrying both and disagreeing is a file whose publisher knew better than its converter, so the defined form wins -- and `series.epub` in the corpus carries both, disagreeing on purpose, so a test can see which. **It also fixed a defect worth naming**: an EPUB's `series` was its own *title*, so every book was a series of one named after itself and `reading-themes`, which scopes a theme to the series, could not let two volumes of one work share a setting. **CB7 is deferred work, not an open question.** The direction was settled on 2026-08-30 -- the format should open rather than be refused for ever -- and the build was then deferred at its first step, which is vendoring: libarchive is already in `third_party/`, but the 7-Zip reader and LZMA are not among its 26 sources, so starting means taking on xz-utils as a second upstream, and xz is the project that shipped CVE-2024-3094 in its release tarballs. [ADR-0013](../decisions/0013-cb7-support.md) records the decision, the deferral and the vendoring constraints for whoever picks it up. Until then the named refusal stands and is covered by `comics/refused.cb7`, so nothing regresses and no promise is made that is not kept. This row should keep saying so rather than tidying it away. |
| `comic-reader` | partial | 28 scenarios: 16 built and tested on both, 11 built and asserted by nothing, 1 one-platform. **Image adjustments are built on both, the whole requirement**: brightness, contrast, inversion and greyscale, live and per series; sharpness is on iOS and on Android from API 33, where a runtime shader exists. Border trimming detects a uniform white or black margin per page and can be turned off for a page it gets wrong; the detection is covered by eight tests a side, and it refuses a gradient, a mid-grey band and a blank page. The sliders and switches were driven end to end on both, on a simulator and on an emulator; trimming itself has only been run against synthetic pages, because the fixture corpus is gradients and has no margins to trim. **Reading direction can be overruled and the reader's orientation locked, on both platforms.** A direction chosen while reading outranks the publication's metadata for that series alone, is remembered beside the theme and the transition, and applies without losing the page: the run of pages reverses and the reader stays on the one they were reading. The lock holds the app at the way up the reader is already at and hands the rest of the app back to the device on the way out; it is not remembered, because the spec scopes it to the reader. Neither has been driven on a device. The direction mapping and the per-series store are unit-tested on both platforms, and both controls were seen rendered in the reader on a simulator, but nothing here can rotate a simulator or an emulator, so the lock has never been watched holding. **A page opened on a cold launch straight into the reader arrives fitted.** It did not before: iOS sized the page against a scroll view UIKit had not laid out yet, recorded that fit as applied, and never tried again, so the page sat at its own pixel size in a corner until something else changed the page, the mode or the size. A fit is now recorded only once there is a laid-out view to apply it to, and the scroll view's own layout applies the one still owed. Driven both ways on the Simulator from the same cold launch -- unfitted before the change, fitted after it, and the settled screenshots byte-identical, so nothing else moved. Android needed no change and got none: its fit is keyed on the size `onSizeChanged` reports, which is only ever a size the layout really has, so a fit taken before the first measurement is discarded rather than recorded. That rule is pinned by a unit test on both sides. **The last five moved too**: the end of a volume offers the previous and the next, the slider shows the page it is heading for rather than moving under the finger, landscape pairs facing pages and can shift the pairing for a cover that throws the count off, a continuous scroll can draw a line where one page ends, and the prefetch gives way under memory pressure. Read on a simulator and an emulator; the pairing in landscape wants a device to be believed. **The two defects a scenario-by-scenario audit named are fixed on both platforms.** The page curl is interruptible: both readers used to stop the running settle and then compute the new drag from zero, so a settle halted at 0.8 jumped to 0.1 -- the snap the scenario exists to forbid. The drag is now an offset from where the page stands, which also lets a caught turn be pushed back rather than pinned at zero. The arithmetic moved out of the gesture handlers into `CurlTurn`, a pure type on each side asserted case for case by twelve tests apiece, because eleven of this capability's twenty-eight scenarios live in a view and that is why the defect survived. iOS needed one extra piece to get there: `withAnimation` moves its state to the destination at once and interpolates only what is drawn, so where the page *stands* is recovered through an `Animatable` view, and a settle a later drag took over no longer runs its completion and turns the page under a finger. **The fit choice is per series.** It was one key for the whole library, so fit-to-width chosen for a manga changed how every other comic opened; it now sits in `ShelfSettings` beside the six per-series reader choices that were already there, and Android's comment explaining the global storage is gone with the storage. The old global value is folded once into the fixed-layout default, so a reader who had chosen fit-to-width keeps opening at fit-to-width instead of finding the whole library back at fit-to-screen. Neither fix has been driven on a device: catching a settle needs a second touch inside a 300 ms window and neither `adb` nor the simulator will deliver one. **The system bars now go away with the chrome on both platforms.** Android drew edge to edge and never hid or dimmed anything, so a clock, a battery icon and the signal bars sat over every page of every reading session; the window's insets controller now follows the same `isChromeVisible` the reading controls follow, with `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`, so a swipe from an edge draws them back over the page without moving it and the centre tap that reveals the chrome brings the clock back for good. They are handed back on the way out, on resume, and on a configuration change -- a reader who cannot get their clock back would be the worse defect. The first of the three deviations at the end of this row still applies: the chrome is visible on open, so the bars are too, and both go four seconds later. Not driven on an emulator by the agent that wrote it. **One scenario is left one-platform.** Volume-button page turns are Android only -- deliberate, and iOS says so in Settings, but `AppSettings.turnPagesWithVolumeButtons` exists on iOS, is unit-tested there, and drives nothing. **Eleven scenarios have no test on either platform, and they are all the ones that live in a view**: the curl gesture, the chrome timeout, the edge zones, the system bars that follow it, the 400 ms spinner, the thumbnail strip, the orientation lock, the stitching and its separator. What *is* asserted a side is the prefetch table, the spread layout, the page-return rules, border cropping, the fit arithmetic, the transition choices and the direction inference. **Three deviations belong to the spec rather than the code, and are recorded here so nobody rediscovers them**: the chrome is *visible* on open on both platforms, not hidden as the scenario says; the page slider is deliberately not mirrored under right-to-left on both; and chapter navigation only ever means the neighbouring publication, because no publication's internal chapter markers are read anywhere |
| `ebook-reader` | partial | **33 scenarios, of which 26 were audited scenario by scenario.** The audited 26: 21 built and tested on both, 3 built and asserted by nothing, 2 missing on both. The other **7 were written into the spec after that audit**, as the work they describe landed — the five reading-aloud scenarios, the PDF scenarios synced out of the `format-scope-and-libraries` change, and the one added when WebView egress was denied. Each corresponds to code that shipped on both platforms with tests, but **none has been re-audited against the code the way the other 26 were**, so they are not counted in the verdicts above rather than being assumed good. |
| `reading-progress` | partial | 17 scenarios: 7 built and tested on both, 9 built and asserted by nothing, 1 missing on both. This is a good local store and a good merge table sitting on top of two wires nobody connected. **The reflowable progress badge stuck, and that is fixed.** On a two-chapter EPUB it climbed through chapter one and then read "50% read" on every page of chapter two, including the last page of the book. Placing a resource in the reading order was an exact string match on the locator's href, and a locator that came from a link carries the fragment the link pointed at while one from a search can carry a query -- neither is part of the resource's identity and neither matched, so the resource could not be placed at all. The lookup now lives in `TotalProgression` beside the arithmetic it feeds, ignores the fragment and the query on both sides of the comparison, and is pinned by six assertions a side including the last page of the last resource reading 100%. It cost more than a badge: the position is stored from the same number, so a wrong progression was a wrong resume point and a wrong "finished" flag. **Not confirmed on a device** -- the arithmetic and the matching are tested, but which of the two the emulator was hitting was never established. **Synchronisation pulls as well as pushes, on both platforms.** ADR-0006's table was written on both platforms, tested on both, and called by nothing -- `KavitaSync` only ever pushed, so a chapter read on a phone was invisible to a tablet until the tablet read it too. `ProgressPull` is the caller: it sorts each merge into records to write, records the server is behind on, and the conflicts that earn a sentence. Finding *which* publication a server's chapter is was the part that had to be right -- a server reports against its own chapter id and the local store keys on a publication identity, so this adds the inverse of the link Kavita already remembered and a lookup by stable id. A chapter this device never opened is skipped rather than adopted. The conflict notice is once, naming how many, and offers to take the other. Eight assertions a side on the merge itself. **And now it pushes what the pull found.** `ProgressPull.toPush` was computed and dropped on the floor -- `KavitaSync` consumed `toSave` and `conflicts` and nothing else -- so a chapter finished here stayed here until the other device read it too. The merge is sorted by `KavitaExchange`, a pure type in the Kavita layer with seventeen assertions a side, into what to write and what the server is owed; `KavitaSync.pull` queues the owed positions through `flush`, the one push path a failed report already uses, so a server that is not there leaves them queued rather than surfacing a failure. **Which makes ADR-0006's first rule reachable at last.** `syncedPosition` is written on every record whose position the server has actually taken -- and only then, because a position it never received is not synchronised -- so a remote that is merely ahead is adopted quietly instead of raising the alert. That needed one thing more than the write: positions compare by fraction now, because a store is entitled to keep a synced position as the one number that survives a change of typography and `ProgressStore.kt:320` does exactly that, so case equality could never match what it had just stored. A conflict the local position wins is pushed as well, which is what makes "told once" true -- unpushed, the next refresh found the same disagreement and raised the same notice. **The round trip has been driven against the mock, and the mock has a self-test of its own.** `scripts/kavita-server.mjs --self-test` starts a server on a port of its own and drives thirteen checks over HTTP -- a wrong key refused, an unauthenticated request refused, a `pageNum` of 3 reading back as four pages read and four pages read reading back as page 3, a finished chapter handing the continue point on, a mark and an unmark -- and `pnpm lint` runs it. The client's half of those same numbers is asserted on both platforms too: what `report` and `mark` post, and that the key travels as a bearer header and never in a URL. **Still nothing against a live server**, and `KavitaSync` itself is still asserted by nothing -- what is proven is the table, the sorting and the two writes underneath them. **The finished lifecycle is built on both**: a completion timestamp stamped once and never restamped -- reopening a finished publication writes a new position, not a new completion -- a finished publication that reopens at the beginning while keeping the record, and "Start from the beginning", confirmed, offered only where there is a position to clear. The restart was driven on an emulator. Room went to version 2 with a written migration, never a destructive fallback. Manual mark-read is built too: `AddToShelfMenu` and `AddToShelfSheet` carry it for both local publications and Kavita chapters, reaching `ProgressStore.mark` and `KavitaSync.mark`. **A publication now says what it is, not only where it is.** Every library publication used to be keyed by its path alone, so renaming or moving a file lost the reader's place -- data loss with no warning, in the one store this app promises never to lose. `contentDigest` had existed on both platforms the whole time and nothing called it: dead code on iOS, and on Android reached only for a file handed over from outside the app. It is taken now from the handle the indexer already opened to sniff the format, so both reads land on pages it is touching anyway and nothing is opened twice -- which covers every route into a publication on both platforms, a folder picked through the Storage Access Framework included, and leaves browsing what is already found no slower than it was. What is hashed is ADR-0006's shape and unchanged: the length, the first 512 KB and the last 512 KB. **Bytes, not a parse**, which is the part that had to be right -- a data descriptor zeroes the local headers, so a digest built from those would agree between two unrelated archives, and `truncated.cbz` earns an identity even though it will never index. The one thing the scheme cannot tell apart, two files of the same length differing only in the middle, is asserted rather than left to be discovered, and the algorithm itself is pinned to a literal hex digest that appears in *both* suites over a synthetic source each builds byte for byte -- the strongest guard this layer has against the drift that already cost it one silent divergence. **The key a publication is filed under deliberately did not move.** `stableID` keeps the path above the digest, and only there: collection members, reading-list entries, a download's id and the folder its bytes live in, and Kavita's chapter-to-publication table all hold that string, so a key that changed when the digest arrived would have emptied every shelf and orphaned every downloaded file on one launch -- a far larger loss than the one being fixed. It costs nothing, because no identity built in production carried both a path and a digest until now; `matches` keeps ADR-0006's order, and that is what resolves a moved file to its record. **And positions written before any of this exists have a migration.** `save` already filled missing identity components in, but only when the reader opened the publication again -- a reader who tidied their folder first never got that far. `ProgressStore.link` attaches what a scan has just learned without touching the position or `updatedAt`, because learning a file's digest is not reading it and a backfill that restamped the date would reorder "Continue reading" for the whole library on one launch. It writes nothing for a publication nobody has read and is idempotent, so a whole library can be passed through it every scan. **Its one caller is not written**: it belongs in the library view model, which the change that built this did not own. Until it is called, a pre-existing record migrates on the reader's next open rather than on the next scan, and a rename before that open still loses it -- the state before the change, pinned by an assertion a side so it cannot be quietly dropped. *Position identity* is closed on both platforms; **not confirmed on a device**. *Same publication from two sources* is not: identical bytes reached two ways now resolve to one record, but `PublicationIdentity.ServerIdentifier` is still never constructed in production on either platform, so a Kavita copy repackaged by the server and a folder copy remain two records. One further thing surfaced while this was built: **Android's instrumented `ProgressStoreTest` had not compiled since `10662ae3`**, which inserted `finishedAtEpochMillis` ahead of `updatedAtEpochMillis` and left nineteen positional call sites binding the wrong parameter. The whole file was dead, which means no assertion in it has run since -- worth knowing before believing anything it appears to cover. The arguments are named now. **Two things stay outstanding beside it.** *The conflict alert names a count, not the two positions the spec asks it to name*, and the clear-history confirmation does not state whether server-side progress is affected. *A source with no progress mechanism should say so on its own screen*, and there is no code and no string for it on either platform -- progress *is* kept locally for such a source by construction, because the store has no idea a server exists, so it is only the sentence that is missing. **Two smaller notes for whoever picks this up.** The four tests a side that look like they cover the finished timestamp assert `ReadingProgress.finished(_:at:)`, which has no production caller; the stamping the app actually performs lives in `ProgressStore.save`/`mark` and neither `ProgressStoreTests.swift` nor `ProgressStoreTest.kt` mentions `finishedAt`. And "Start from the beginning" is on the cover context menu on iOS and the add-to-shelf bottom sheet on Android, which is where the requirement's current wording puts it -- *Restart deliberately* says it is on that menu "rather than on a publication detail screen". **It did nothing at all in iOS's list layout until 2026-08-31.** `CoverList` opened the menu with a trailing closure, which under Swift's forward-scan rule binds to the first parameter with no default -- `onRefused` -- so the restart handler kept the empty closure it defaulted to while the menu drew the button regardless. A reader on the compact shelf long-pressed a cover, tapped the action, and got no confirmation, no clear and no message. Nothing failed, which is why no test caught it and why a test asserting the button exists would have passed too. Fixed at the call site and at the shape: the handler is optional, `RestartOffer` makes "someone is listening" one of the three conditions the button is drawn on, and the confirmation moved into one modifier the grid and the list share rather than a dialog the grid had and the list did not. Android's sheet already gated on a null handler and was immune by construction; it now asserts the same rule instead of merely happening to have it, four cases a side. That was recorded here as a deviation on the grounds that neither app had such a screen. **iOS has one now, and a reader can reach it**: `publication-detail`'s page shipped in a previous round with no caller anywhere, and every cover on every iOS surface leads to it as of 2026-08-31. Which makes the standing note the wrong way round -- the restart is not the clause that deviates. Two others now are. *Continue from the library* says a tap on a part-read publication "opens at the stored position without an intermediate screen", and *Restart deliberately*'s reason clause says "the library opens a publication when its cover is tapped"; on iOS the cover opens the page and the page carries the primary action. That is the `publication-detail` proposal's stated intent -- it words the two entry verbs as a deliberate risk -- but its delta does not list `reading-progress` as MODIFIED, so the requirement and the shipped app disagree in writing. **A spec amendment to make, not a defect to fix**, and it is Android's obligation as well the moment its own mirror lands |
| `local-library` | partial | 15 scenarios: 6 built and tested on both, 5 built and asserted by nothing, 4 one-platform. **Imported copies are built on both**: a publication picked from the file picker is copied into the download store, which already owns copied bytes and keeps them out of backups, and is listed under a fixed "On this device" source; the same file imported twice is one copy; an import is never swept away by "remove downloads after finishing", because nothing can be asked for it again; deleting one names the title, the space freed, and says the original elsewhere is untouched. **Watched changes are built on both**, and only their arithmetic mirrors: `FolderSnapshot` is the same type on both platforms and carries the two guards -- a walk that finds nothing removes nothing and does not overwrite a good snapshot -- while being *told* about a change is a dispatch source per directory on iOS, capped at 96, and one content observer per picked folder on Android. Whether either mechanism actually fires on iCloud Drive or on a given `DocumentsProvider` is exactly what has not been tried. **The scan is resumable on both**: it writes down what it has indexed every two dozen publications, and the next scan of that folder restores them and opens none of them again. The journal is cleared when a scan finishes, so it is not the metadata cache `sources` still wants. **Open-in from another app opens and refuses correctly on both**, the refusal naming the detected format. **Both iOS defects the last pass found are fixed, and what "remember it" means is settled rather than open.** *More than one folder library now gets scanned on iOS.* `LibraryScanning.scan(_:)` opened with `scanTask?.cancel()` and `restoreFolders()` called it once per restored folder, so every folder but the last was cancelled before it walked. The places a launch knows about are now walked one after another inside a single task -- which is what Android's `rescan` has always done over its trees -- and cancelling still stops all of it. A test restores two folders each holding one comic and fails against the old code. *A file another app hands over is remembered as one book on iOS.* `OpenedFile.remember` put the **file** into `FolderBookmarks`, the folder-library list, so a launch registered a local-folder source named after the comic and handed the file to the folder walk, which lists nothing and finished `found: 0`; and because it went round that loop last, it cancelled the scan of the library the reader had actually picked. `FolderBookmarks` now tells a folder from a file -- asked of the filesystem, so a bookmark written by the old build lands in the right list -- `LibraryScanner.scan(fileAt:)` indexes a single file through the same path a walk uses, and the publication is adopted under no source at all, the way the app's own Documents folder is: there is nothing there to rename, reconnect, refresh or remove. Two consequences of having no source are written into the spec: at most twenty files are kept, oldest dropped first, because nothing asks the reader whether they meant to keep a comic from a chat and nothing offers to forget one; and a remembered file that has gone is forgotten silently rather than reported as an unavailable folder the reader could re-pick, which it never was. `remember` also now holds its own security scope -- `index` stops the scope it started, and a bookmark made outside the scope of another app's URL is refused, so on a device the call was returning false and remembering nothing. *Android deliberately does not mirror it, and the spec says so.* An `ACTION_VIEW` or `ACTION_SEND` grant ends with the process unless the sending app made it persistable, which file managers do not, and there is no way to ask afterwards; beyond that, a persisted document `Uri` would land in the same `persistedUriPermissions` list `SafTree.persistedTrees` reads back as folder libraries, which is the iOS defect above rebuilt on Android, and a library row holds a filesystem path rather than a `Uri` a resolver would have to re-open. Importing a copy is what Android offers instead, and it is built. *Returning to the foreground now reconciles on both.* `FolderWatcher.watchingFolders(of:)` was written, documented and applied to no view, so `reconcileWatchedFolders()` was reachable only from the watcher's own callback; it is on `libraryColumn` in `LibraryView.swift` now, which is the scenePhase mirror of the `ON_RESUME` call Android makes from `LibraryScreen.kt:248`. Untested on both, and for the same reason: the trigger is a lifecycle callback in each platform's UI framework, and only the arithmetic behind it is reachable from a unit test. *One thing found while fixing this and not fixed:* `forgetVanished` compares a publication's stored location, which is standardised to `/var/...`, against the folder path, which a resolved bookmark spells `/private/var/...`, so the reconcile that drops what a walk no longer finds matches nothing on a bookmarked folder. It needs its own change and its own tests. **Two smaller corrections.** Imported copies' space is reported only inside the downloads total on the Privacy screen -- the `importedBytes` accessor written for it on both platforms is read by nothing. And the spec's "case-insensitive natural filename order" fallback is not natural on either platform: both tie-break on the display title with a plain case-insensitive collation, and Android's walk order is not numeric-aware at all. **What has no test on either side**: the file type registration (declared in both manifests, asserted by nothing), the two watchers, the imported-copy delete confirmation, Android's persisted URI permission, and the 10,000-item budget, which appears only in comments and has never been measured. **What was watched, and what was not.** The library was seen scanning and drawing on a booted simulator, light and dark, after a relaunch through the rewritten restore -- three comics in three separate folders, which is the path the two fixes changed. The hand-over itself was **not** driven end to end: staging one takes a second app sharing a file into StoryArc, and the simulators here are shared with other work, so what was on them was not this branch's. Everything the two fixes changed is covered by unit tests that fail against the old code -- two folders both scanned, a remembered file restored as a publication and not as a folder, a bookmark told apart by kind, the twenty-file limit. Nothing here has run on real hardware, and `remember` holding its own security scope is reasoned from the sandbox rules rather than watched, because a host test process has no sandbox to refuse it |
| `library-browsing` | partial | 18 scenarios: 15 built and tested on both, 3 built and asserted by nothing. **Unified library, search grouping by match kind, and per-scope layout are built on both platforms.** The shelf spans sources rather than folders: finished downloads join it, each attributed to the source its record names, so a chapter fetched from Kavita is in the library instead of being reachable only by browsing back to the server. Read that limit exactly as written -- the shelf holds scanned local folders **plus finished downloads**, and nothing else. A connected Kavita, OPDS or SMB source contributes nothing to it until a file has been downloaded from it, and SMB shares are browsed and never indexed. A scope selector narrows the whole view -- the shelf, the search, the filters and the continue row -- to one source; it persists until changed, and a scope naming a source that has since been removed opens the whole library rather than an empty one. **Built on both, and the iOS half was mounted and watched on 2026-08-30.** `ScopeMenu` was written, translated into four languages and mounted by nothing, so an iOS reader could not leave "all sources" at all. It is now first in `LibraryToolbar`, where Android puts its own, gated on the same rule: `ScopeMenu.offered(in:)` returns nothing below two sources, so the control is absent rather than a menu with a single answer, and six cases assert that gate against the `attributesPublications` Android gates on. **The crash the previous round reported does not reproduce.** That round recorded an `EXC_BAD_ACCESS` inside `ToolbarContentBuilder` on first layout and suspected the `Picker` tag -- `LibraryScope` being an enum that carries a `UUID` where the neighbouring `SortMenu`'s tag is a plain enum. The item was re-mounted verbatim as that comment described, built with `pnpm build:ios`, and run on a booted iPhone 17 Pro with two sources configured: the app launches, the menu opens, a source can be picked and the choice persists across a relaunch. The tag is not the mechanism -- `LibraryScope` is `Hashable` and hashes as its `UUID`, which is what a `Picker` tag needs -- so the `Picker` was kept rather than churned into a menu of buttons for a cause that was never established. Checked in both layouts, because the reported trace named `libraryColumn` and the stacked and split layouts share it: the split layout launches and draws the control on a booted iPad Pro 13-inch as well. What the earlier round could not verify, it was right to leave rather than ship. A publication carries its source's name only where more than one source is configured. Search results are grouped as series, titles, people and tags, the headings ordered by the best match rather than by a fixed list, and each publication appears once under the reason it ranked where it did. Grid or list is remembered per scope, falling back to the choice a reader made before the layout was per scope. The grouping, the scoping and the attribution are asserted against the same table in both suites; the download attribution is asserted in both persistence suites. **The iOS scope menu has since been watched** (see above). The group headings, the source line under a cover and the Android side of all three have still only been read -- no emulator was available in that round, and the Android per-scope layout is covered only by an instrumented test that was never run. **Two things this row used to list as outstanding are built and tested on both, and one is not what it looked like.** Date-added and file-size sorting are complete: both cases are in the enum, both are stamped by the scanners, both are ordered in `compare()`, both appear in the sort menus with translated strings, and both are asserted in both suites. **Recent searches are complete on both.** The value, its rules, its persistence and its tests were already on both platforms and Android already showed the list; on iOS `RecentSearchSuggestions` was dead code, because the library's `.searchable` attached no suggestions closure. It attaches one now, so an iOS reader who opens an empty search field is offered the terms they typed before, each running the search again, with a Clear action under them. **Watched on a booted simulator**: a term typed into the shelf's field, then cleared, came back under a "Recent searches" heading with its clock glyph and a destructive "Clear recent searches" beneath it -- once, not once per keystroke. That was one modifier, not a feature -- and this row previously listed the scenario as outstanding, which the code already contradicted. **The filter count was wrong in both halves.** The spec names ten facets, not eleven. Seven are built on both. Of the remaining three the spec's own Open Questions rule out two -- source is the scope selector, which is already built, and publication status has no source in any format this app reads -- which left **the "Downloaded" filter as the one real gap**. **It is built on both now**, and the answer to the question it raised is that the group and the availability axis are not the same control said twice. The axis keeps everything that opens with no network -- a folder a reader picked as much as a download the app fetched -- because it asks *will this open on a plane*. The group asks the narrower question `offline-downloads` owns: did this app fetch it and is it keeping it. A file a folder walk found answers yes to the first and no to the second, and the difference is the one the app already draws for the mark on a cover: the card can be pulled, the grant can lapse, the folder can be unmounted, and only the app's own copy carries the promise. So the group admits a subset of what the axis admits, which is what keeps *Filtering offline*'s clause -- only what reads without a network, whatever the source is doing -- true of it. The spec's own Open Question had said as much before either existed: what blocked download state was that "the library is assembled from a scan that never consults" the record of downloaded files, and the axis never needed that record. Three answers rather than two ticks, for the reason the decade group is a radio list -- a publication is downloaded or it is not, so ticking both is the same as ticking neither -- and it counts in the badge and goes when Clear filters goes, on both platforms. Ten cases a side assert the rule, including the one that would rot quietly: a chapter downloaded from a server that has since gone away stays on the shelf, because the rule never asks the registry. **Not driven on either platform**, and the two things worth watching are the menu at the largest text size and the empty state a reader reaches by asking for "Downloaded" with nothing downloaded. **The divergence it inherited is closed.** On iOS both the download group and the availability axis are `@AppStorage` and survive a cold launch; on Android both were `rememberSaveable`, so they came back after a rotation and reset on a relaunch. `LibraryPreferences` now holds a key for each, in the same preferences file the query and the per-scope layout already use -- which is the arrangement iOS reaches from the other side, its two `@AppStorage` keys landing in the `UserDefaults` the rest of the library's preferences live in. Neither is a field on `LibraryQuery` on either platform, and deliberately: the query is the value both encode, so a case added to it is a change to `:core:model` and to iOS's mirror of it. Nothing is migrated on either side. Stored as the enum's name and not its ordinal, because an ordinal is a position in a source file; a name the app cannot read widens the shelf rather than narrowing it, which is the direction that cannot strand a reader. Eleven cases assert it -- the round trip and the three keys' independence in `:core:persistence`, the name mapping and its fallback in `:feature:library`. **Not driven**: what a test cannot reach is the composition that reads the store on first draw, so the proof that a shelf left narrowed comes back narrowed is a relaunch on a device. **The widen-scope offer is now on both.** `NarrowedToNothing` was constructed on iOS with neither `scopeName` nor `widen`, so the "search all sources" button never drew and `LibraryModel.widenToAllSources` had no caller: an iOS reader scoped to one source who searched and found nothing was told "No publication matches the active filters" and offered only a Clear filters button. Both arguments are passed now, the same way Android passes them -- the offer present only when the view is scoped somewhere narrower than every source. Neither platform asserts it: the offer is a branch inside a view, Android's view-model cannot be built in a JVM unit test, and neither app has a UI test target. **Watched end to end on a booted iPhone 17 Pro, now that the scope can be changed**: an unscoped library that matches nothing offers Clear filters and no widen button, which is correct; narrowing to a source that holds nothing says "Nothing from Attic NAS is on this device yet" and puts a "Search all sources" button above Clear filters; tapping it returns the shelf to every source. Light and dark, at the default and the largest accessibility text size, where the menu wraps and stays legible. The branch itself is still asserted by neither platform, for the reasons above, but `LibraryModel.widenToAllSources` now has two cases of its own -- including that widening leaves the search and the filters where the reader put them, which is what keeps it a different offer from Clear filters. **The last missing scenario is built and tested on both, and this row had it wrong in a way worth recording.** *Mixed local and server search* was scored missing on the grounds that "nothing merges anything" and that "the library's own search never asks a server". Both halves of that were already false: `LibrarySearch` fans one question out to every Kavita and OPDS source, and `SearchAnswers` merged the answers append-only. What was genuinely absent is the other two words of the scenario -- **ranked**, and **each labelled with its source** -- and the labelling was absent *on purpose*, because the implementation followed the unarchived `one-library-three-destinations` delta, which modifies this very scenario to say "no result is labelled with the source that supplied it". **The main spec is the contract and the two disagree in writing; that conflict is now the one thing left here and it is a spec decision, not a defect.** What was built follows the main spec. **The unlabelled merge was also losing answers, and it was photographed doing it.** `SearchAnswers` folded a remote row into any local row of the same kind and title, across libraries. On 2026-08-31 a search for *Fine Print* with a catalogue and a folder configured produced `200 GET /opds/all?q=Fine%20Print` in the server's own log and not one catalogue row on the screen: its single match had the title the device already held, so it was folded out of existence with nothing on screen to say a catalogue had answered. `ios-search-remote-and-away-dark.png` is that screen. `SearchListing` replaces the merge in `LibraryFeature` and `:feature:library`: it keeps the append-only promise -- a late answer adds rows below and never moves one -- and folds a duplicate **only when the same library said it twice**, which still folds a server's copy of a chapter downloaded from that same server, because a download carries its library's identity. `SearchRank` is the ranking, applied to each answer as it lands including the device's own: title strength first -- is, begins with, begins a word, contains, elsewhere -- then held before away, then the shorter title, then the folded title. **A server's better match outranks a local worse one, and place only breaks a tie**, because the reader asked for a book and not for a place; the pair that answer equally are settled by the one that opens with no network. Both types are pure and mirrored, thirty-three cases a side, plus integration cases over the exact two documents `scripts/opds-server.mjs` serves -- Android's four drive them through a real server on the loopback interface and assert the app asks for `q=Fine%20Print`, iOS's three assert the same outcome from the same bytes through its own parser. Each row now carries a third line naming its library, reusing the shelf's own "From %@" and "On this device" in the four languages they already ship in. **The rule for when that line is drawn is not the shelf's**, and an independent review of the branch is what caught the difference: the shelf shows a source only when more than one source is *configured*, and one server plus files another app handed over is one source -- so the label was hidden in the commonest mixed search there is. It is drawn when more than one *place* could have answered, the places being the origins of what the device matched plus every library asked, decided once when the question is asked so it cannot appear on the rows already on screen when the second library replies. A remote row still leads to its server's own browser and never to the publication page, which resolves against the library's own set. **Not driven on either platform**, and the screen to capture is exactly the one that was photographed failing. **What the append-only rule does and does not promise is now stated exactly, because it was overstated.** `rows` only ever grows and no row is removed, replaced or reordered against another -- but `groups` partitions by kind, so a late row whose kind already has a heading lands inside it and pushes every later heading down. A row can move *down*; it can never move up, past another row, or onto a different heading. Appending late rows at the bottom under a fresh heading of their own would fix the position and was rejected: ranking runs across kinds within one answer, so the device's own answer would already shatter into repeated headings, and one heading per kind is what *Typing a query* asks for. The stronger clause the doc comments used to quote -- "the arrival of remote results never reorders or displaces a result the reader is already reaching for" -- **is not in the main spec either**; it is in the same unarchived delta as the labelling clause, and the two want settling together. A case a side renders the groups and pins both halves. **Four mirror divergences were found and fixed on the way**, which is what the mirrored-logic rule exists for: `String.folding(options:)` full-case-folds "Straße" to "strasse" where Kotlin's `lowercase` cannot, the length key counted grapheme clusters against UTF-16 units, the title tie-break ordered by scalar against UTF-16 unit, and the trim borrowed two platform defaults that disagree about the non-breaking spaces. All four are now spelled out identically on both sides and pinned by cases a side. `SearchAnswers`, its tests, `SearchResultGroup` and core's `SilentSource` are **deleted** -- the merge had to be written beside them while those directories belonged to other agents, and they were removed once that wave landed. What *also* exists, and is a different scenario belonging to `kavita-server`, is the offer beside the local matches to put the same query to a scoped Kavita server, which is built on both. **The two that used to sit beside it are built.** *Filtering offline* is the "Downloaded" filter above. *Overriding a curated order* -- a reading list had no sort control, no return-to-curated action and no label on the order it was in, so a reader could not tell that the order in front of them had been chosen for them, could not sort it their own way, and had nowhere to get back to; the third clause held only because nothing could violate it. It has all three now, on both platforms, built out of the library's own sort rather than a second one: the order the list is in is one of the answers rather than a separate idea, and the control carries the name of the order it is in, which is what labels the curated order as curated -- on the chip on Android, and in the line under the count on iOS, where a toolbar menu draws its glyph and not its title. While a sort is overriding the list a second control appears named for where it goes, which is the one tap the requirement asks for. The third clause is now enforced rather than merely unviolated: nothing is written back, sorting goes through `LibraryIndex.arrange` so a list collates the way the shelf does, an entry the library can no longer answer for keeps the tail in the list's own order, a row's number stays its place in the list rather than its place on screen, and **rearranging is offered only in the curated order** -- `EditButton` gives its place to the way back on iOS and the arrows leave the rows on Android -- because a move reports the position it landed in *as drawn*, and writing that into someone else's reading path is the failure the clause exists to prevent. Twelve cases a side. **Neither has been driven**: a reading list with entries has to be built by hand first, and no simulator or emulator was reached in this round. **A chosen order lasts as long as the screen does**, on both platforms, which is the shortest honest reading of "for that session" -- leaving the list lands back on the order that means something. **The cover grid now keeps both bounds on both.** Android used `GridCells.Adaptive(minSize = 108.dp)`, which takes no maximum, so the Adaptive-columns scenario's second clause -- cover size stays within the readable range -- held on iOS only, and the Kotlin comment claiming the same bounds was wrong. `BoundedAdaptive` replaces it: below the cap it computes exactly what `Adaptive` computes, remainder pixels included, and above it every column stops at 168 dp, which is what a narrow window needed -- one column was stretching to the whole width and a single cover filled the screen. Seven cases assert it, including a sweep over every window width from 1 to 2400 px; there is no iOS mirror because SwiftUI owns the same arithmetic inside `GridItem(.adaptive(minimum:maximum:))`, and the pair of bounds is what the two platforms share. Seen on the emulator at a width where the cap does not bite, which is every width a phone has -- the band it changes is 169 to 231 dp, which is a freeform or split window, and none was driven. The *catalogue* grid is a different grid and is still uncapped on both, matched at `Adaptive(140.dp)` against `.adaptive(minimum: StoryArcSpace.huge * 2)`; the scenario names the cover grid, so that is left as it is rather than changed on one side. **Two scenarios exist on both with no test**: progress on covers, and the volume/chapter ordering of an opened series -- for which no local-library series screen exists at all. The third used to be the curated default order of a reading list, which `ListOrderTests`/`ListOrderTest` now assert on both sides. **Two stale in-code comments to sweep when this area is next touched**: `LibraryIndex.swift:10-13` and `LibraryIndex.kt:14-16` still name grouping by match kind *and* the curated order of a reading list as absent, and both have been built since; and `LibraryQuery.swift:63-70` and `LibraryQuery.kt` still say download state "needs the library to know what has been downloaded", which it now does. All four sit in `StoryArcCore` and `:core:model`, which no agent this wave owned, so they were left rather than half-corrected on one side. **Two layout defects the 2026-08-31 emulator captures recorded are fixed on Android and neither has been re-photographed.** The filter chip row ran off the window at `font_scale 2.0` with *Filter* half out of it and nothing on screen saying the row scrolled -- it did scroll, and reachable through an interaction a reader has no reason to attempt is not reachable. It is a `FlowRow` now, so at the largest text size the four controls take two or three lines and all of them are simply on screen; each chip is boxed with the menu it opens so a popup that measures as nothing cannot take a slot of nothing and push the next chip onto a line it did not need. And the tablet's detail pane drew a back arrow while the shelf sat permanently beside it, with nowhere to go back to; Material's own `ListDetailPaneScaffold` hides that affordance when both panes are visible and `AppContent` composes its pair by hand, so `HostedScreen` carries the rule as `isBesideList` and the bar leaves its navigation slot empty. Back itself is untouched -- the gesture and the pane's own "this is gone" button still pop the page and return the pane to its one sentence. **Neither is asserted**: both are branches inside a composable, and neither Android module has a UI test target. iOS has no counterpart to the second -- its split layout pushes the page onto the column's own stack, where a back control is correct and the system draws it |
| `settings-and-about` | partial | 14 scenarios: 8 built and tested on both, 5 built and asserted by nothing, 1 one-platform. **Search points at a single setting rather than only at its group**: ten anchors a side, mirrored case for case, and choosing a match navigates, scrolls the row into view and tints it for two seconds before it fades. Privacy clears downloads beside the cache and the reading history, each row stating its own size -- that row used to say nothing downloads yet, which stopped being true when `offline-downloads` landed. The two search indexes had drifted, "cache" reaching Downloads on iOS and Privacy on Android; they are realigned term for term and a test a side asserts that every anchor is reachable. Driven on an emulator in light and dark: searched, navigated, scrolled and watched the tint fade. **Reporting a problem now carries all three facts on both platforms.** Android's link pre-filled the version and the platform and not the device class, while `BuildInfo.kt` claimed otherwise in its own comment and `Diagnostic.kt` computed the value a few files away; `issueUrl(context)` takes a `Context` now and both readers share one `BuildInfo.deviceClass`, so a report and an export cannot disagree about the device they describe. The body is assembled by a pure `issueBody(platform:deviceClass:)` on both platforms, and two assertions a side -- mirrored -- pin that it carries the three facts in order and nothing else, where there had been none anywhere. Still a class and never a model: `Build.MODEL` and the iOS model identifier both narrow a person far more than "tablet" does. **Not driven**: the link was not opened on a device, and the class Android derives from `smallestScreenWidthDp` has not been read off a real tablet. **One thing is still outstanding, and it is the same report.** The diagnostic export reports `configured = 0` as a literal on both platforms -- `Diagnostic.swift:97` and `Diagnostic.kt:81` -- so a reader with four sources shares a report that says none. Safe, and false. **Five scenarios have no test anywhere on either platform**: the privacy statement, the three clear paths (`clearing()` and `clearCache` are called by no test at all), About's contents, the support link's exclusivity, and the acknowledgements list. What *is* tested a side is appearance persistence, the reader theme staying separate, the reading defaults, the reset, and the search index twice over. **Reporting a problem from the About screen is no longer one-platform**: both have the link and both fill in what the spec asks for. The one-platform count above is the audit's, not a re-count |
| `localization` | partial | 12 scenarios: 4 built and tested on both, 5 built and asserted by nothing, 2 one-platform, 1 that source alone could not settle. **That split predates the reader fix below and was not re-derived with it**: the override is now built on both platforms and asserted by nothing on either, which moves one scenario out of `one-platform`, and saying which of the two it was needs the scenario-by-scenario audit this round did not run. **Four languages ship complete on both.** Key parity holds across all five Android modules, and `pnpm strings:ios` is the missing-key gate iOS never had: a `LocalizedStringKey` that matches nothing is not an error, not a warning and not a crash -- it renders the key -- and four shipped strings were doing exactly that. `about.version`, `theme.fontSize.position` and `theme.pageColour.refused` stored their keys positionally (`%1$@`), which SwiftUI never derives, and `catalogue.acquire.other` declared no placeholder for the argument its own translation spends. The About screen had been reading `about.version 0.1.0 1` on a simulator. The gate also fails on any key untranslated in any of the four languages, which is the clause the spec words as a build failure. Plurals, locale byte formatting and date formatting go through platform services on both. **The override is not what this row used to claim, and the difference matters.** Android does *not* carry the language into the composition: it applies it in `MainActivity.attachBaseContext` and rebuilds the activity on change. `InterfaceLanguage.kt:11-24` says the composition route was tried, measured on an emulator and abandoned, because a `Popup` is its own window and every menu stayed in the system language -- so the row was crediting the app with the bug that was fixed. **And the override now reaches the EPUB reader on Android, which it did not before 2026-08-31.** `EpubReaderActivity` is a separate activity in a separate module and had no `attachBaseContext`, so 109 string keys across 94 `stringResource` call sites stayed in the system language while the rest of the app was in the reader's. It carries the override now, and `speaking()` and `chosenLanguage()` moved from `:app` -- which `:feature:epubreader` cannot depend on -- into `:core:persistence`, which already owns the `SettingsStore` they read. Both activities call the one mechanism; there are exactly two activities in the app and no third place for this to be forgotten. A blank stored tag now means no choice rather than the root locale, which `Locale.forLanguageTag("")` would otherwise answer. iOS never had the hole: `.speaking(settings.language)` is on all four scenes. **Re-judged 2026-08-30, and it is not a platform rule.** Android's own per-app language *is* an OS feature with its own rules and it was tried: `InterfaceLanguage.kt:21-24` records `LocaleManager.applicationLocales` as measured on an emulator and rejected, because it arrives at API 33 against a floor of 31 and tore the activity down rather than recreating it. That is not why the reader is in the wrong language. The reason is narrower: the override is an `attachBaseContext` on `MainActivity` and the reader is a second activity that does not have one. The fix was three lines in `EpubReaderActivity` plus that move, and it landed on 2026-08-31. The scenario was right as written and did not want amending. **What has not been watched is the result**: the reader has not been opened in French on an emulator since, so this row credits the code and not a photograph. **Sorting does not follow the chosen language either.** `library-browsing`'s article-stripping and locale collation are built and tested on both, with matching en/es/fr cases in both suites -- and both call sites take the process default rather than the chosen language, so the ordering is correct until a reader overrides it and silently wrong afterwards. **`pnpm pseudo:android` is a walk, not a gate.** The debug build sets `isPseudoLocalesEnabled`, without which `en-XA` is not compiled in and the walk would pass because nothing changed; it navigates by position, never by label, since under a pseudo-locale there is no label to match, and it reports text that leaves its row, controls off the display, and targets under 48dp. Nine routes pass with every string accented and half again as long. It is Android-only, none of the nine routes is a reader, and it is in neither `pnpm lint` nor `pnpm check` nor CI -- so it runs when somebody remembers. **Long-translation resilience is the one scenario source could not settle**: the walk reads bounds, and an ellipsised label stays inside its own bounds, so nothing here can detect a truncated label -- `scripts/pseudo-locale.mjs:16-17` says so in its own header. **The app has never been run under a right-to-left system language.** The walk uses `en-XA` and never `ar-XB`; what is tested is the publication-side reading direction, which is independent of the interface by construction. **The reader walk still only covers the library and the seven settings screens**, because the two readers need a publication on the device and the emulator has none |
| `native-experience` | partial | 28 scenarios: 7 built and tested on both, 15 built and asserted by nothing, 3 one-platform, 3 missing on both. **Three things this row used to list as missing are built.** Haptics are wired on both, with a deliberately two-word vocabulary -- completion and refusal -- through `SensoryFeedback` on iOS and `HapticFeedbackConstants.CONFIRM`/`REJECT` on Android, called from both comic readers. Increase Contrast is built and asserted on both, including a live contrast listener on Android. The cover-derived accent is derived, contrast-floor-adjusted and asserted on both. It reached exactly one screen -- the end-of-publication screen in each reader -- for as long as the publication's page was unreachable. **On iOS it now reaches two**: the page sets `Theme.coverAccent` from its own wash, and the page has callers as of 2026-08-31. On Android it is still the one screen. **A wide window gets the platform's own split navigation on both**, and the iOS half of that sentence was wrong: there is no `NavigationSplitView` anywhere in the app, and deliberately so. `AppShell` is a `TabView` with `.tabViewStyle(.sidebarAdaptable)`, which the platform draws as a tab bar on a phone and as a sidebar on an iPad from one destination set -- `LibraryView` records why a split view inside one of those destinations would be a second, disagreeing navigation. The consequence, found while settling `publication-detail` task 4.3: iOS has **no second pane**, so the delta's "one sentence before a publication is chosen" has nowhere to be shown, and `PublicationDetailPlaceholder` was deleted rather than wired. So: a sidebar on iOS, a Material 3 navigation rail on Android, holding the library, every catalogue, server and share, the collections and settings -- the same places a narrow window keeps in a strip and two toolbar icons. The choice is a window width and nothing else: one number, 600, mirrored on both platforms and unit-tested there, so a Split View drag, a rotation, an Android multi-window resize and a fold are one event with one answer. That is also the whole of the foldable requirement bar the hinge: the activity already declares the fold a configuration change it handles, so nothing is recreated and nothing is lost, and the test exists precisely to keep a posture branch from creeping in. **The long-press menu reaches every cover**: it was on the grid and missing from the list layout on both platforms, and from Android's Continue reading row, so marking a publication read was a thing you could only do in one layout. **Scroll edge effects are declared where the app floats its own chrome**: iOS carries the catalogue strip and the library's notices as `safeAreaBar` with a soft edge and puts the strip on Liquid Glass with the Reduce Transparency fallback it had been missing; Android's top bar takes its scrolled container colour as the shelf passes under it, and the stretch at the end of a scroll was already Compose's own. **Quick actions and continuity are built on both.** The menu is three entries and the list is decided once, in `QuickActions`, mirrored case for case and pinned by ten assertions a side: the publication in progress named first because it is why anyone holds an icon down, the library always because it is the one destination a fresh install has, and downloads only once there is something in them -- an entry that opens onto an empty screen is a promise the app cannot keep. A publication with a whitespace-only title is refused, which `offline-downloads` has already met once. The entries survive the app being killed because the *system* stores them, which is also why the entry carries a stable identifier rather than a path: a menu can outlive the scan that last placed the file, and on Android a `content://` grant does not outlive the process it was given to. Both platforms then have to wait -- a quick action lands on a cold start with an empty shelf -- so both poll the library for five seconds and then give up and land on the library, rather than throwing the reader into a book minutes after they asked. Continuity diverges on purpose, because the platforms do: iOS publishes one `NSUserActivity` doing two jobs, handoff and Spotlight, because they describe the same fact and two would drift, and it carries an identifier so the receiving device looks in its own library and a book it does not have is a book it does not open; Android has no Handoff and there is no backend to invent one with, so the mirror is `pushDynamicShortcut` when a publication is opened -- published *and* reported as used, which is what lets the launcher rank it and the Assistant answer for it. Neither carries a reading position across devices. iOS needs a scene delegate installed through `configurationForConnecting`, because SwiftUI surfaces neither the cold-launch shortcut nor `performActionFor`. **None of the last three paragraphs has been watched working.** There was no simulator and no emulator in those rounds, so the sidebar, the rail, the glass strip, the edge effects, the quick-action menu, its icons, the Spotlight entry and the handoff banner have been compiled and reasoned about and not seen -- and the scene-delegate route in particular is the half most likely to be wrong in a way only a device would show. **Three things were missing on both, and only the first was on this row before; one of the three, the in-app predictive-back preview, has since been built on Android, and the counts in this row's first sentence are left where they are because re-summing them means moving the header table, which the delivery note already lists as its own job.** A fourth, the Material You opt-out, was built on 2026-08-31 and is described below. *Widgets*, deferred deliberately by [ADR-0011](../decisions/0011-home-screen-widgets.md), which names the two blockers: a shared reading snapshot a second process can read, and an iOS signing team to provision the App Group that would hold it. *The in-app predictive-back preview.* ~~Missing.~~ **Built on 2026-08-31.** The manifest opt-in was already set, so the *system* drew the app-level preview, and `BackPreview`/`backPreview()` were implemented and unit-tested; `PredictiveBackHost`, the only thing that applies the shrink-and-round transform, **had zero call sites anywhere in `apps/android`**. It now has two. The first wraps `SettingsScreen`, which is where the app's only two `PredictiveBack` handlers already were and where, with no host above them, the callback fired and nothing was drawn -- the KDoc at `PredictiveBack.kt` says exactly that. The second wraps the shell's navigation area, where the app's one consolidated back rule lives: what was a plain `androidx.activity.compose.BackHandler` at `AppShell.kt:83`, with no progress and no preview, is a `PredictiveBack` handler inside a host. Two hosts and not one, because nesting is the arrangement rather than a conflict -- the composition local is static, so Settings' inner host owns the gesture through its three levels and the outer one owns every other in-app destination, and each applies its transform exactly once. The host sits inside the navigation area rather than around the whole shell: back pops a screen off the current destination's path or returns to the start destination, so the screen is what leaves and the bar or rail is what stays. `PredictiveBack.kt`'s KDoc still says "installed once so far, around `SettingsScreen`" -- it is in `:core:designsystem`, which the slice that added the second host did not own, and it is one sentence to sweep. This has **not been watched on a device**: the transform is unit-tested as a value and the composition that applies it is reachable by no test in this repository, so what a reader sees is unproven. **The Material You opt-out is built, on Android only, and counted as one platform rather than as missing.** The scenario requires "a setting to use the StoryArc palette instead"; `useDynamicColor` was a hardcoded `true` at both production call sites with no `AppSettings` field and no row in `AppearanceGroup`, so the only route back to the ember palette was OLED Dark -- a different setting meaning a different thing. There is now a field defaulting to true, a switch in the Appearance group in all four languages, and both `MainActivity` and `EpubReaderActivity` read it. The switch agrees with OLED Dark rather than fighting it: `StoryArcTheme` has always let true black win over the wallpaper, so under OLED Dark the switch is disabled and its note says why, and the reader's own answer is kept for when they leave it. One platform rather than a gap, because iOS has no dynamic colour to opt out of -- hence no `SettingsAnchor` counterpart there either. **Not yet photographed**: the palette this restores has not been seen on an emulator. *The library's list fallback at accessibility text sizes.* **The navigation bar's own half of that requirement is settled and the library's is not.** At `font_scale 2.0` "Downloads" filled its third of a 360 dp window edge to edge and sat against the display boundary -- `after-2026-08-31/android-shelf-caption-scale2-light.png` is the evidence -- and French's "Téléchargements" is six characters longer again. Material's own navigation bar does not scale its labels with the system font size at all (`NavigationBarItemView` removes it; `labelFontScalingEnabled` defaults to off), which is why every stock Material app draws small navigation labels at a large font scale. That rule is now ported into `AdaptiveNavigation`, because the Compose `ShortNavigationBarItem` has no equivalent -- the label is the caller's composable -- and scoped to the bar and the collapsed rail, which are the two controls that ration a fixed width between a fixed number of destinations. The expanded rail keeps the reader's text size, because it has room and is the only control that draws the secondary entries. The three ways out are all closed by somebody: Material says a label does "not truncate or wrap" and must not shrink to fit, and this capability says a destination is never an unlabelled icon. It is a deliberate exception to `design.md`'s "all scale with the Android font scale", scoped to the eight words in the navigation control, and TalkBack is unaffected because the label is still the item's name. **The Downloads shelf was a third instance of the same defect and is fixed.** Its `coverMinimum()` held `design.md` §4's three tiers and ignored the font scale entirely, so at an accessibility text size it kept 104 dp columns while the library shelf widened to 146 -- two shelves in one app laying out differently. iOS had the identical divergence in `OnDeviceShelf`, where Apple's audit reported it as five clipped labels. **The rule now has three implementations and that is the standing finding**: `:feature:library` owns `coverMinimumWidth`, `coverMaximumWidth` and `BoundedAdaptive` and all three are `internal` to it, so `:app` cannot call them; they belong in `:core:designsystem`, which both modules already depend on. Three test files now assert the same table so the copies cannot drift silently. **The filter chip row above the shelf is the same defect unfixed**: at the same font scale it runs off the window with no affordance that it scrolls, and it belongs to `:feature:library`. The library's own grid-to-list fallback is still absent on both platforms: Nothing on either platform reads the text size to choose a layout -- grid-versus-list is a stored reader preference, and a grep for `dynamicTypeSize`, `isAccessibilitySize` and `fontScale` across both trees returns four hits, none of them in the library. The one accessibility-size adaptation that does exist is the reader's fit picker becoming a menu on iOS, with no Android counterpart. **Three more clauses are absent on both and belong on this list**: a page's focal area may still fall across a foldable's hinge, and nothing reads a hinge rectangle -- no `androidx.window` dependency, no `WindowInfoTracker`, no `FoldingFeature`, only two prose comments; no key toggles the reader chrome, though page turns are bound on both; and `accessibilityAction`/`CustomAccessibilityAction` appear nowhere in either tree, so a VoiceOver or TalkBack user gets only the pager's own scroll. **Every performance number in this capability is unmeasured.** There is no benchmark harness of any kind in this repository -- no launch budget, no memory budget, nothing that would fail if either were blown. **The visual-proof requirement is the sharpest gap, because this file is the evidence for it.** "The project's device matrix" is named nowhere in the repository; `AGENTS.md` specifies light and dark at the default and the largest text size and no devices. There is no reference to compare a capture against and no comparison harness: no baseline directory, no image-diff script, nothing in `package.json`. Thirty-five PNGs are committed under `docs/designs/screenshots` and thirty-two of them are Android settings screens. Two device-driven Android scripts exist because a `@Preview` once lied -- `smoke-android.mjs` walks every route on a connected device and asks logcat whether the process died, and `a11y-scan.mjs` reads the real accessibility tree at real density -- and iOS has one simulator-bound UI test, scoped to the library and currently `XCTExpectFailure`d, and nothing that walks routes. Neither platform *gates* anything: no check refuses a change whose only evidence is a preview. The both-appearances matrix is Android's alone: eight settings screens at the full light/dark x default/largest grid, against three iOS screenshots in total, none at the largest text size -- and `AccessibilityAuditTests.launch(contentSize:)` exists precisely to force one and no test in the file passes it |
| `sources` | partial | 16 scenarios: 12 built on both, 2 built and asserted by nothing, 2 one-platform. **The five gaps an audit found are built on both platforms, and none of them has been driven.** `Diagnosing a source` has a screen now: tapping a source in Settings opens its state, its last successful sync, the last error in plain language, what it holds and what its downloads weigh -- and offers the five actions the scenario names. Which of them a given source gets is `SourceDiagnosis`, a value with no pixels in it, asserted by eleven mirrored cases per platform; the last error is derived from the state rather than remembered beside it, so a source that failed an hour ago and is answering now reports none. `Credentials rejected` has its one control: it re-opens the sheet the source was added through, address filled in and secret blank, and what comes back carries the same identifier -- so `SourceRegistry.replacing` puts it back in place with its position, its downloads and the reader's own name for it intact, where the "remove and re-add" an iOS comment used to name loses all three. `Reordering` decides something at last: `SourcePrecedence` ranks by position in the registry, an unattributed find and a removed source tie for last, and the row opens the copy it says it came from. **Removing a source now takes its downloads and its stored secret with it**, which the requirement asked for and neither platform did -- the secret first and unconditionally, deleted by the reference the registry holds rather than one re-derived from `source.id`, because a source whose id and credential reference disagreed (as every iOS Kavita source's did) would otherwise keep its key for ever. The folder lookup used to gate the whole method, so removing anything that was not a folder did nothing at all. **`Source becomes unreachable` was answered by amending the spec, not by caching.** The clause "its cached contents remain browsable" was only ever true for a local folder; OPDS and Kavita responses are deliberately never written to disk. Caching them means a second catalogue store per source type, in a caches directory the system may evict mid-browse -- so the "not available offline" screen would still be needed -- and it puts server-supplied acquisition URLs on disk, which is where an embedded credential could survive a launch. So opening an unreachable server says it cannot be reached, that its downloads stay readable, and offers to try again, instead of fetching, waiting and landing on an empty list. The scenario was rewritten to say that, and the caching question is recorded as open with those reasons. **The dead iOS reconnect loop is wired**, at `LibraryView.swift:286`. It runs from the same `task` that makes the first probe, *after* it: a loop started before any answer has arrived finds nothing unreachable and stops, which is why Android now asks once before it starts scheduling too -- its probe and its loop used to be launched side by side. **Neither platform reconnects while the reader is reading**, which the scenario's "does not interrupt reading" clause covers, and that is left as it was; neither retries on regained connectivity or on returning to the foreground either, because there is no reachability observer wired to the probe on either side. iOS also has pull-to-refresh now, which the spec names and which it had no equivalent of at all. **Verification is compilation and unit tests, not use.** Both apps build, both lint clean, the iOS and Android suites pass, and the iOS app was installed and launched on a booted simulator -- but no new screen was reached on it: the Simulator control this repo uses is down and `osascript` has no assistive access here, so nothing can drive the UI, and no Android emulator was running. **Nothing in the paragraph above was seen working**: the source detail screen, the reconnect sheet, the offline notice, pull-to-refresh, or the precedence rule with two sources holding one title. **Still true from before**: the metadata cache is built -- the catalogue is written to disk when a walk finishes and restored before the next one starts, covers likewise, keyed by publication *and* pixel size, both in the caches directory, which is what makes them evictable independently of downloads without a line of code for it. A refresh updates incrementally, and a publication a walk no longer finds is removed while its progress stays. Neither the removal nor the write fires on a walk that saw *nothing at all*: that is far more likely to be a folder it could not read than a reader who deleted every book. One honest limit remains: the "cached" notice leaves when a walk completes, even a walk that saw nothing, because the scanner does not report whether it could read a folder -- the shelf survives such a walk, but stops saying it is cached. **Two gaps in the tests rather than the behaviour**: the source-store round trip is asserted on iOS and by nothing on Android, where there is no `SourceStoreTest` at all; and nothing in either library feature module asserts the empty state, the cached notice, the incremental refresh or the disappearance removal |
| `offline-downloads` | partial | 19 scenarios: 6 built and tested on both, 11 built and asserted by nothing, 1 one-platform, 1 missing on both. Six of those eleven are partials -- built on both with a clause of their own scenario absent on both -- and they are named below, because "untested" flatters them. **A download is written where a removal looks for it, on both platforms.** The store named a file after the publication and the Settings screen deleted the one named after the identifier, so removing a downloaded publication dropped the record, left the bytes, and the storage total on that same screen never went down. The optional name a caller could leave out is gone: the store takes the record, or the three things that determine the path, and there is no spelling that omits the title. A removal deletes the download's whole directory rather than one file, so bytes written under any earlier naming go with it -- a delete that does not depend on the stem cannot disagree with the write about it. Two workarounds went with the split: `KeepOffline` named by identity on purpose to survive it, and `keepForOffline` recorded every copy as `application/octet-stream` while naming the file from the URL. Five assertions a side, including one that writes a file the old way and proves the new removal still takes it. Found on iOS while mirroring: a whitespace-only title survived `safe` and produced `  .cbz`, which Android already treated as no title at all. **Background transfer is built on both. Android is verified end to end**: a foreground service holds the process, a download started on the screen finishes with the app off it, and the service stops itself when the queue empties. **iOS is verified only in the foreground**: the transfer goes through a background `URLSession`, which is what lets the system carry it, and a download completes and lands through that path -- but the Simulator's transfer daemon never delivers a completion that lands while the app is suspended, and keeps reporting the task as live, so the suspended case needs a real device to confirm. A completion that arrives with nobody waiting is adopted, and a download nothing is carrying goes back in the queue. **Four scenarios are built and asserted on both**: a re-fetch of something already downloaded is suppressed, the failure/retry/backoff loop, automatic cleanup with a real ten-second undo (the bytes are moved aside, not deleted), and backup exclusion. Automatic cleanup has not been seen working on a device. **The largest gap is the one nobody had written down: a download shows no size and no progress, ever.** `advancing` is called exactly once per download, at completion, on both platforms -- `DownloadQueue.swift:337` and `DownloadQueue.kt:278`. iOS's `BackgroundTransfers` implements no `didWriteData` delegate, Android reads the whole body into a `ByteArray`, and `OpdsAcquisition` parses no `length`. So the spec's first scenario -- "its size is shown, and progress is visible" -- is served by an indeterminate spinner from tap to finish, and the 400 MB comic the spec's purpose names is held entirely in memory on Android. **Two of the three scenarios that had no implementation now have one, mirrored and asserted; the third is still missing and the reason is outside this capability.** *Low device storage* is built and tested on both. `StorageHeadroom` is the shared rule -- a 256 MB floor checked against the volume, and against the size about to be written where one is known -- and each download store asks its own volume, which is the only genuinely platform-shaped part. A shortage pauses every queued and running download as `Download.Pause.outOfSpace`, which is the state and the four translated sentences that had been unreachable since the queue was written, evicts the cover cache once and before any publication, and deletes no download: the bytes stay and the transfer resumes from them when there is room. A download the reader paused keeps their reason rather than being resumed by the shortage lifting, and a volume that declines to report its free space leaves the queue running -- AGENTS.md §2 forbids turning a normal condition into a failure a reader has to dismiss. Six assertions a side on the rule and six more on the two library operations. *Overriding a metered connection once* is built and tested on both. `MeteredDownload` answers two questions: whether to ask, which turns on the connection alone and not on the Wi-Fi-only setting, and whether a queued download may start, which is where "for that item only" lives. The grant is held against the publication's id in memory, so it dies with the app instead of becoming a standing permission; the Wi-Fi hold is now per download rather than global, so one granted publication runs while the rest of the queue waits, and `held` stops claiming the queue is stopped while bytes are arriving. The confirmation is one modifier on iOS and one composable on Android, reached from both the catalogue shelf and the publication's format rows, reusing the title, the confirm label and Cancel already translated for the share that asks this question before streaming. **The size is stated where the app has one and its absence is stated where it does not** -- an OPDS acquisition link carries no `length`, so before a first download the honest answer is usually a sentence rather than a number, and `Download.expectedBytes` supplies the figure on a retry. Seven assertions a side. **The one part of it that cannot be settled at a desk** is whether the link is really cellular: `NWPathMonitor` and `ConnectivityManager` both report Wi-Fi to a simulator and an emulator whatever the host is on, so the branch that decides *to ask at all* wants a device with a SIM or a tethered hotspot. *Reading while downloading* is **still missing**, and only its second clause was ever reachable from here. The wait before the first page is shorter: `DownloadLibrary.promoting` moves the download a reader is waiting on to the head of the queued run -- order, not priority, so nothing is cancelled or preempted -- which on a metered link, where the bound is one, was the difference between a five-megabyte comic and a four-hundred-megabyte wait. Five assertions a side. But the scenario's first clause is "opens immediately by streaming", and nothing in the app can stream an HTTP acquisition. The architecture's extension point exists and is used -- `ComicArchiveOpener.register` and `PublicationAccess.register` hand `PublicationIndexer` a `RandomAccessSource` for `smb://` today -- but **no source over HTTP range requests is registered, no OPDS acquisition link carries a `length`, and `scripts/opds-server.mjs` serves no `Accept-Ranges`**, so there is nothing to stream from and nothing in the corpus to prove it against. Those three live in `Catalogue`, `core:catalogue` and `scripts/`. **Two claims that used to sit in this row are false and are worth naming exactly.** *"Resumes by itself" is not built.* The queue does hold for Wi-Fi and for a disk limit, and it says nothing about why it is waiting; `reconsider()` exists on both platforms and has **zero callers** in either tree; there is no `NetworkCallback` on Android and iOS's `NWPathMonitor` handler only stores the path. A queue held for Wi-Fi restarts only when the reader re-enters a catalogue page, because that is what constructs a new queue. *"Resumes after the app is killed" is a restart, not a resume.* There is no Range header, no `bytes=`, no resume data and no `downloadTask(withResumeData:)` anywhere in either tree, and the scenario asks the app to state which of the two happened -- no string on either platform does. **Three more partials to cost honestly**: per-item and global pause, resume and cancel are absent from the downloads view on both, and the in-file comments at `DownloadsSettings.swift:198` and `DownloadsGroup.kt:269` admit it; the storage view has no cover-cache size and no per-source subtotal; and the storage limit stops the queue and never offers to free room. Downloading a collection shows no "n of m complete". **Verifying a download no longer crashes Android.** Both platforms verify by indexing the file before the record is marked finished, so a truncated archive is caught at landing rather than at the first page turn. iOS caught `PublicationIndexer.IndexError` and reported it in plain language as non-retryable; Android's `attempt` caught only `OpdsError` and `IOException`, and `IndexException` extends `Exception`, not `IOException` -- so a download whose bytes will not index threw out of `attempt`, out of `transfer`, and out of a coroutine launched on `Dispatchers.Main.immediate` in a `SupervisorJob` scope with no handler, and took the app down. Android now has the branch iOS had, naming the format for an unsupported container and saying the bytes could not be read otherwise, in all four languages, marked non-retryable the same way. One assertion a side pins the fact the crash depended on: an index failure is its own type and is not an I/O failure. **Not driven**: neither platform's failure path has been watched on a device, because it needs a server that serves bytes that will not open. **The scenario's "a failed verification re-queues it once" is now built and asserted on both.** The two ways indexing can fail are not the same event and no longer get the same answer: an unsupported format is not a failed verification -- the bytes are what the server holds, re-fetching produces the same format, and it stays terminal -- while unreadable bytes are, the likeliest cause is the transfer, and they go back in the queue exactly once. `Download.verificationFailures` counts separately from the three transfer attempts and is persisted on both platforms, so a download killed between its two chances does not come back with both; a re-queued download no longer tells a reader waiting to open it that it failed, because it has not. Seven assertions a side. Still not driven against a server that serves bytes which will not open |
| `opds-catalog` | partial | 13 scenarios: 11 built and tested on both, 2 built and asserted by nothing. **Adding a catalogue with sign-in and certificate pinning, sections with their counts, paginated grids, facets, the publication screen and the format choice are built on both platforms**, the parsing and the format ranking well tested at thirteen matched tests per platform. An OPDS 2.0 feed's groups are shown as the feed declares them -- each named group is a section of its own with a row of covers, a group that carries a `self` link offers a way into the rest of it, and a group the feed left unnamed is poured into the page because there is no title to head it with. Choosing a publication opens its own screen rather than a menu: the cover large, the title, authors, series, description and date the feed reports, a Read button that takes the format the app would pick, every other readable format as a row beneath it, and a plain sentence for what the catalogue offers that the app will not take -- a format with no decoder, or a loan, a purchase or a subscription. Driven end to end on the Android emulator against `pnpm opds`: two groups, a `See all` into a group's own feed, a publication opened from a group's row, and Read downloading the PDF and opening it, in light and dark and at the largest text size. **Not driven on iOS** -- the simulator would take no synthetic input from this machine, so the iOS half rests on its unit tests and on compiling for the Simulator. **Searching a catalogue is built on both, in both of its shapes.** Android's browser had no search field at all -- `CatalogueBrowser.search` and `searchUrl` existed and nothing called them, and `catalogue_search_prompt` and `catalogue_search_local` sat in all four string files referenced from no Kotlin. It has one now, submitted rather than debounced on both platforms, because a keystroke here can be a request to somebody's home server. A server-answered search opens as its own page, like entering a section; a catalogue with no search filters what is loaded and says so, in the sentence those unused strings were written for. **And the commoner trigger for that scenario is followed now, which it was on neither.** A catalogue that advertises search through an OpenSearch *description document*, which is what OPDS 1.2 servers usually do, was parsed into `searchDescription` and fetched by nothing; only a template carried inline in a link's href reached a server. `OpenSearchDescription` reads that document -- once per page, and only when a reader actually searches -- prefers the `Url` whose type names OPDS over one that names Atom over one that names an HTML page, and resolves a relative template against the document's own address with the placeholder braces intact. Eight matched cases a side, from the same fixtures. **One accident fixed in both directions**: iOS escaped a search term with `addingPercentEncoding`, Android with `URLEncoder`, so a term with a space in it was `%20` on one platform and `+` on the other -- the same search asking a server two different questions, and a literal plus wherever a template puts the term in a path segment. Both now write the same string for every input, and ten matched cases a side assert it. What still has no test on either platform is the browser's own state: it is an `AndroidViewModel` a JVM unit test cannot build, so what is asserted is the pure pair underneath -- where a term goes, and which entries it keeps. **Android's search was driven end to end on the emulator against `pnpm opds`**, in light and dark and at the largest text size: a term typed into the catalogue's own field and submitted opened the server's answer as a page of its own titled by the term, and the same term submitted inside a sub-feed that advertises no search filtered what was loaded and said so, in the sentence those four string files were written for. **The description-document half was not driven, on either platform.** The repo's own fixture server, `scripts/opds-server.mjs`, advertises search only as an inline template, so there is no address on this machine that exercises it; a route serving an OpenSearch document is the one thing standing between the eight matched tests a side and a walkthrough. **The iOS half was not driven at all** -- the simulator on this machine mangles injected punctuation, so no catalogue address could be typed into it. **Two behaviours this row calls built rest on no assertion at all.** Pagination -- following the `next` link as the reader scrolls, the six-row threshold, the single-flight guard, the id-matched dedupe -- is matched on both platforms and covered by nothing, because neither `CatalogueBrowser` has a test. The whole certificate path is the same: the system-first evaluation, the fingerprint, the refusal, the one-host-one-certificate pin. A grep for `CertificatePins`, `UntrustedCertificate` or `fingerprint` across every test directory on both platforms returns nothing, and no self-signed server has been driven |
| `kavita-server` | partial | 19 scenarios: 11 built on both, 7 built and asserted by nothing, 1 one-platform. **Built on both platforms: authentication with one-shot token renewal, the version floor, extracting a base and key from a pasted OPDS URL, and reading libraries, series, volumes, chapters and server search.** A reader pastes a server's OPDS URL, browses a library as a grid of covers with progress, reads the server's own summary and people rather than the file's, resumes at the chapter Kavita reports as next, and reads it. Reading position travels both ways: sent when the reader closes the chapter or leaves the app, held on disk when the server is unreachable, and flushed on the next connection; what other devices have read shows on the series grid. A chapter can be marked read or unread by hand, and the mark reaches the server. A server's own collections and reading lists appear beside the local ones, each labelled with the source it came from, and open to a grid of covers or a numbered run of chapters. A chapter can be added to one of the server's lists, and a publication from anywhere else is told why it cannot go in one. **Searching a Kavita source reaches the server.** The client method was written and tested on both platforms and called by nothing; there is a field now on the libraries screen, the series grid and the chapter list -- one search for the whole server rather than one per level, so it is a search of the *source* the way the requirement says. It reads all five kinds the scenario names rather than the series alone. Two wire shapes had to be corrected to make it work at all -- a chapter's title is `titleName` in a search result and `title` in a volume, and a series' identity is `seriesId` in one and `id` in the other -- so a found chapter read as a bare number and a found series as series zero. A found series is fetched by identity before it opens, because Kavita keys progress by library *and* series and a search result does not always carry the library. **Scoping the library search to a Kavita source reaches the server too**, as an offer beside the local matches rather than a substitution -- the same shape `library-browsing` uses for "widen the scope to all sources", and built on both: `KavitaServerSearchOffer.swift` on iOS, the row at `LibraryScreen.kt:481` on Android. **The unreachable counterpart degrades rather than fails**, following the pattern `opds-catalog` already set instead of inventing a second one: the search filters what the device holds, says results are limited to cached content, and a cached row opens the file rather than pointing at a series the server cannot serve. **A chapter can be kept, and keeping one makes a real download.** "A downloaded Kavita publication" did not exist before: every chapter the browser fetched went to the caches directory, which nothing lists, nothing attributes and the system may reclaim. Keeping goes through `DownloadStore` like every other kept publication -- same record, same per-source attribution, same removal, same naming -- and opening still writes a cache file, which was the good half of the comment that sent everything there. **What the server said is cached with it**, in the one Kavita answer that reaches disk: a card with no URL and no secret, carrying the whole library-series-volume-chapter chain, because a progress post missing one of the four is refused. `KavitaCard.applied(to:)` lays it over the publication indexed from the file and marks the result authoritative, which answers both metadata scenarios with one function -- a field the card is silent about keeps what the file said, and page count, format and identity are never touched. Removing a source takes its cards with its downloads. **The revoked key explains itself on both.** The marking was right and the `reconnect` action already existed; the sentence did not. iOS opened the browser anyway -- the key is still in the keychain, so a page could be built -- and printed `keyRejected` at the reader; Android swallowed every failure into an empty library. Both now say which of "this device lost the key" and "the server refused it" happened, and point at the action. `KavitaFind` is the pure half, mirrored line for line with fifteen matching tests a side; `KavitaCardStore` has five a side. **Seven scenarios are built on both and asserted by nothing, and they are the reader-facing half.** What the Kavita tests cover is transport and parsing: the address, the version, the 401 retry, the library/series/volume decoding, and list create and delete. No test file on either platform mentions `KavitaSync` or `KavitaProgressStore`, so the continue point, the metadata block, the collections and lists, the append, the offline queue and the flush have nothing standing behind them. **The two writes underneath them do now**: what `report` and `mark` post is asserted on both sides -- against a real loopback server on Android and a stubbed transport on iOS -- along with the key travelling as a bearer header and never in a URL a proxy would log, and `scripts/kavita-server.mjs --self-test` asserts the server's half of the same numbers on every `pnpm lint`. **Verification is compilation and unit tests, not use.** Both apps build, both lint clean, the iOS and Android suites pass, and `pnpm build:ios` is green -- and **nothing in this row has been seen on a screen**: no simulator or emulator was driven in that round. Not watched working: the search field at any of the three levels, the grouped results, the "limited to what is on this device" notice, the keep action, a kept chapter appearing on the shelf under the server's own title, the offline overlay, the library's "search this server instead" row, or the revoked-key screen on either platform. Still built against Kavita's documented API and the mock in `scripts/kavita-server.mjs` -- which answers `Search/search` with all five kinds, serves `Series/{id}`, and now self-tests its own progress round trip -- **never against a live server**. **Two limits remain**: removing a single kept chapter from inside the Kavita browser is not offered (Settings and the library both do it), and a cached search can only match what has been kept, because nothing else about a server is written to disk and `sources` records why. **The thing the row used to be silent on is half built.** The *Metadata* requirement names seven fields; the series screen showed five, because `ageRating` and `publicationStatus` arrive as bare integers and nothing read either. **Android displays both now**, on the screen the live answer already lands on, each as a named line rather than joining the run of facts -- a rating dropped unlabelled into "2020 · Ada Lovelace · Drama · Teen" is a rating a reader would take for a genre, and this is the one field where being mistaken for something else matters. The integers are Kavita's own enums, copied from `Kavita.Models/Entities/Enums/AgeRating.cs` and `PublicationStatus.cs` rather than guessed, and both tables are asserted position by position -- an off-by-one here shows *Teen* where the server said *Mature 17+*, and nothing throws. A number outside the table is left unsaid on both fields, and Kavita's `Unknown` and `Not Applicable` draw no rating line at all, because telling a parent a book has been assessed when nobody assessed it is the one mistake this line must not make. **The status is translated into the four languages and the rating is not**: the rating is an issued mark from ComicInfo.xml v2.1's own vocabulary, half of it codes (`G`, `PG`, `MA15+`, `R18+`, `X18+`), and a paraphrased rating is a rating no body gave; only the words introducing the two are the app's. **Two limits, both named rather than hidden.** iOS still decodes neither, so this stays one platform. And the downloaded-offline path still shows five of seven: `KavitaCard` in `:core:model` has no field for either, so *Reading a downloaded Kavita title offline* falls back to the file's `ComicInfo.xml` for exactly those two -- two fields on the card and two lines in `KavitaKeep` close it, on both platforms, and `KavitaKeep`'s own KDoc says so. **Proven against the mock and not on a screen.** `scripts/kavita-server.mjs` served `ageRating: 0` for every series, so the whole stated-rating path was exercised by nothing; it now cycles Kavita's own numbers, and seven new self-test checks hold the corpus to Kavita's ranges, to at least one unrated series and at least one rated one, and to more than one of each. Driven over HTTP against the running mock on 2026-08-31: eleven series, five distinct ratings, three distinct statuses, three of the eleven correctly drawing no rating line. The Compose block that renders them has been seen by no emulator |
| `network-share` | partial | 12 scenarios: 3 built and tested on both, 6 built and asserted by nothing, 1 one-platform, 2 missing on both. **SMB 3 transport encryption is unmet on both platforms**, and now measured rather than assumed: jcifs-ng carries the negotiate context and no cipher, SMBClient offers SMB 2 only. [ADR-0010](../decisions/0010-smb-clients.md) records the two clients and states this outright. A server that *requires* encryption is refused with a sentence naming the setting to change **on Android only**, which `scripts/smb-server.sh --encrypted` tests; iOS cannot tell that refusal from a wrong password and deliberately says the commoner thing. **An SMB 1 server is now refused by name on both.** iOS declared `SmbError.protocolUnsupported`, translated it, displayed it at `SmbConnection.swift:191` and threw it nowhere, so such a server read "The share could not be reached." It is thrown from `SmbClient.meaning(of:isHandshake:)` now, off the statuses only a server that would not agree an SMB 2 dialect sends: `STATUS_NOT_SUPPORTED`, which MS-SMB2 mandates when no dialect is common, and the five CIFS error-class statuses an SMB 2 server has no way to send. Read only during the handshake, because `STATUS_NOT_SUPPORTED` means something far narrower once a share is open. Each platform reads its own client: jcifs-ng never reaches an NT status for it and says "dialect" in prose, and the three sentences it can say were read out of the shipped 2.1.10 classes rather than guessed. Four assertions on iOS and four on Android, mirrored, where there had been none on either. The message names the protocol version and the setting to change and nothing else. **Neither half has been driven against an SMB 1 server** -- `scripts/smb-server.sh` serves SMB2_02 upward and there is no `--smb1` mode, so both readings are asserted against statuses and sentences rather than against a server. **Everything else is built on both platforms**: add a share and be told which failure happened, pick one discovered on the network or type it by hand, browse its tree to pick a root, read a comic streamed from it with the next pages prefetched, confirm first on a metered connection, and keep reading across a dropped connection. Driven against a real Samba server with `server signing = mandatory`, authenticated. **"Nothing written to the device" is true of CBZ, CBT and EPUB and false of PDF and CBR**: both platforms copy the whole archive into the cache first, because PDFKit, `PdfRenderer` and libarchive all want a real file. **Most of this capability is built and asserted by nothing**: the 2-second indicator, the 60-second offer to download and leave, the metered confirmation, discovery, and the browse-and-pick step have no test on either platform, and both suites skip themselves silently when no server is listening on 4445. Reopen-on-next-read is asserted on Android only -- `SmbReconnectTests.swift:6-12` says in its own header that the iOS suite cannot reach that branch. Discovery is verified on iOS only: the Android emulator's NAT does not carry the host network's multicast. **Two scenarios are missing on both.** *The transport line the source detail screen should show* is only in the add-share sheet on both platforms; Android additionally reports whether the session is signed, iOS reports neither, per ADR-0016. *A denied local-network permission is never explained.* Discovery hides itself and manual entry still works -- both deliberate, both on both platforms -- and nothing detects the denial to begin with (iOS sets no `NWBrowser` state handler) and no string in either app tells a reader once how to turn discovery back on in system settings |
| `collections-and-reading-lists` | partial | 15 scenarios, scored as 18 clauses because three of them carry two independent halves: 10 built and tested on both, 4 built and asserted by nothing, 4 missing on both. Nothing here is one-platform -- the mirroring across Swift and Kotlin is unusually exact, down to matching test names. **A reader can create, rename by re-creating, delete, and populate local collections and ordered reading lists on both platforms, reorder a locally-defined list, see their position in it, add any publication to any number of them, and be offered the next entry in list order rather than the next in series when they finish one.** A collection with no cover of its own is drawn from the artwork it holds: the first four member covers by identity ascending, as quadrants, or one cover across the frame below four. The library has a selection mode in both the grid and the list, with the count visible and one action out of it, and what is picked can be added to any collection or reading list, kept on the device, or marked read -- through the same paths one publication already takes, with one undo for the whole set for ten seconds. A whole collection or reading list can be downloaded or marked read from its own screen, the item count and total size stated before anything is copied. Downloading a selection copies its files into the app's own download store rather than fetching them, because everything the library holds is already a file on the device; a publication read off a share is still kept offline from the reader instead. **A local reading list can be copied onto a Kavita server**, so it syncs and other Kavita clients can see it. Only a server that has just answered when asked for its reading lists is offered -- reachable and able to hold one, which an empty answer cannot tell apart from no answer, so the two are kept apart. Before anything is sent the reader is shown, per server, how many of the entries that server already holds and how many it does not; the ones it does not are named, with the reason: this app has no backend and never uploads a file, so a publication the server has never seen cannot join one of its lists. Those entries are left out and the local list keeps them -- the list is copied, not moved. The copy is undoable for ten seconds through the same bar every other bulk action uses, and the undo asks the server to drop the list again. When no server can take one the action is disabled with the reason beside it, rather than failing after the reader has confirmed it. Built against the documented `ReadingList/create` and `ReadingList` DELETE endpoints and the mock in `scripts/kavita-server.mjs`, never against a live server, and the sheet has not been seen on a screen. **An edit made to a server-backed reading list while the server is away is kept rather than lost, on both platforms.** Adding a publication to one writes the edit down before anything is sent, in a durable queue of its own that survives the app being killed -- which is the point, because the reader who edits on a train has closed the app long before the server is back. The reader sees it twice over: a line under the shelf's name saying how many edits are waiting, and the entry itself on the list, in place, marked as waiting for the server -- grey rather than red, because `sources` makes offline a normal state. The shelves screen reconciles every list it can reach each time it appears: what the server has taken since is forgotten silently, because an edit that arrived is not a change worth a sentence; what is still owed is sent through the same queue that already carries writes to a server, so nothing is pushed twice; and a list that moved underneath a waiting edit keeps its own version for membership and order. The discarded edit leaves the transport queue as well, so a later flush cannot resurrect what the server has already overruled, and the reader is told once, naming the list and the entries that were not added. Once is durable rather than incidental: the notice is written down, shown, and deleted when it is dismissed, so a refresh cannot repeat it and a launch cannot lose it. The rule itself is a five-row table in `ShelfMerge`/`ShelfPull`, mirrored line for line on both platforms with seventeen matching tests each, the way `ProgressMerge`/`ProgressPull` hold ADR-0006's -- including the row that says a list this device has never seen is pushed to rather than second-guessed, because without a baseline "did the server change" is unanswerable and losing the edit is the one outcome offline must never produce. **Neither offline scenario has been seen on a screen.** Driving them needs a Kavita server, a publication in the library that carries a Kavita origin, and the server taken away between two refreshes; the simulator booted, built and ran the app in that round, but the tooling that drives its interface stopped responding before the shelves screen was reached, and no emulator was driven at all. **Four clauses have no code on either platform, and this row used to claim there were only two limits.** *Deleting a collection has no confirmation.* The scenario is, in its entirety, a confirmation and a sentence saying the publications themselves are not deleted, and there is neither: `ShelvesView.swift:81-85` deletes on the first swipe, `ShelvesScreen.kt` on the first tap, the write is saved immediately, and neither platform's strings hold the sentence. There is no undo on this action either, unlike every other bulk action in this capability. *A reader cannot choose a collection's cover.* `settingCover` and `coverMemberID` are exercised by tests, round-tripped by `ShelvesStore` and honoured by `CompositeCover`, and called by no view, view model or menu on either platform -- so the spec's "unless the user sets a specific one" is unreachable in the running app. A symbol search looks convincing until you follow the callers. *A collection can only be created locally.* Nothing lets a reader choose a server at creation, and no server-collection write exists in either client. *A server-backed reading list cannot be reordered at all*, online or off, on either platform, so the "sent to the server" half of the ordering scenario has nothing to send -- even though the mock already serves `/api/ReadingList/update-by-multiple`. A Kavita list opens read-only on both. **Two divergences worth recording for an estimate**: Android reorders a local list with up and down buttons rather than the drag the spec names, which is a defensible accessibility choice and is still a divergence; and a bulk download writes entries into the download store as *completed* records rather than queueing them, so they later appear in the downloads view with no queue position, no progress and no pause, and an image-folder or `smb://` publication is skipped without saying so. **Two limits remain in the offline half.** The push waits for the shelves screen to be looked at, because the reconnect loop it should hang off was being rewritten in the same round; and the only edit that can be queued is adding an entry, because appending is the one reading-list write `KavitaClient` exposes |
| `reading-themes` | partial | **Split out of `ebook-reader` when the reader-theming change was synced on 2026-08-30, so this row is new and its scenarios were scored under their old home.** Eleven axes, six presets named rather than numbered -- Original, Quiet, Paper, Bold, Calm, Focus, which replaced the Paper/Sepia/Night/High Contrast the spec had named for months while the code shipped something else. The presets are drawn as specimens of real letterforms rather than the letters "Aa", because that is what shipped and the spec now says so. **Outstanding: the live preview inside the theme sheet** (task 3.6) -- the preset cards preview each theme's colours and face, and the page *behind* the sheet updates live, but there is no preview within the sheet itself; and **the reader-chosen light/dark pair**, where `ThemePreset.matching` is a fixed Light-to-Paper and any-dark-to-Quiet mapping with no setting for a pair, and the link is resolved when the reader is constructed rather than when the system flips, so a switch to dark mid-book does nothing until the book is reopened. Not audited scenario by scenario in its own right; the verdicts above were taken against `ebook-reader` before the split |
| `page-transitions` | partial | **Split out of `comic-reader` and `ebook-reader` when the reader-theming change was synced on 2026-08-30, so this row is new.** Slide, fast fade, page curl and continuous scroll, chosen per shelf and remembered there. **The curl is interruptible as of this session** -- both readers previously stopped the running settle and then computed the new drag from zero, so a settle halted at 0.8 jumped to 0.1, which is the snap the scenario exists to forbid; the arithmetic now lives in `CurlTurn`, a mirrored pure type, and carries the base. Fixing it surfaced three more: an Android tap during a settle froze the page mid-curl, Android's flick test was unsigned so a fast backward drag completed the turn, and iOS could not read where the page stood at all because `withAnimation` sets state to its destination immediately. **Outstanding: curl on reflowable content** (nothing rasterises a reflowable page, and both readers refuse it and say why), and **a double-page spread curling as one surface** -- the curl container takes one decoded page and knows nothing of the spread layout the paged and scroll containers use. Hardware input is specified as it shipped: where a platform does not let an app observe the volume buttons, no setting is offered and the reason is stated once |

## What blocks what

`sources` is the keystone. Five capabilities wait on it, and so do the three open tasks
in `format-scope-and-libraries`:

```
sources (registry, credentials, cache, health)
  ├── opds-catalog        built on both except catalogue search, which is iOS-only
  ├── kavita-server ──── reading-progress: synchronisation pulls, and drops what it
  │                       would push; the silent-adopt rule is unreachable
  ├── network-share
  └── offline-downloads ─ comic-reader: "offer to delete the download"   built on both,
                          as a gated sweep with a ten-second undo rather than an end-screen offer
                        └ settings-and-about: clearable downloads
                        └ format-scope-and-libraries 5.2, 5.3, 6.5
```

`collections-and-reading-lists` depends on `sources` only for its server-backed half. Its
local half — a collection a reader makes themselves — is built on both platforms, with two
holes in it that belong to no server: a collection is deleted without confirmation, and its
cover cannot be chosen.

## Where the two platforms differ

They are near line-for-line equal wherever code exists. Twenty scenarios are not equal,
and every one of them is named in its own row above. These are the divergences that are
deliberate — the rest are gaps, not decisions.

1. **Volume-button page turns are Android-only.** Deliberate: iOS cannot capture the
   volume buttons within App Store rules, and the app says so rather than shipping a dead
   switch. `AppSettings.turnPagesWithVolumeButtons` nonetheless exists on iOS and drives
   nothing.
2. **A PDF's document outline is iOS-only.** `PDFKit` reads one and
   `android.graphics.pdf` exposes none, so the find sheet has a third tab on one platform
   and not the other -- absent, never empty. Selection, copy, marking and in-publication
   search are on both, over each platform's own PDF library.
   [ADR-0012](../decisions/0012-pdf-text-on-android.md).
3. **Android has a manual library refresh; iOS has none.** Both watch, by completely
   different means: iOS opens a kernel event source on each directory, because a vnode
   event says nothing about what happened a level down, and caps how many it opens;
   Android registers one content observer per picked folder, which already carries
   descendants. Neither has been watched working, and on iOS the foreground reconcile
   that would back them up is applied to no view.
4. **Reordering sources is a drag on iOS and two buttons on Android.** `List.onMove`
   gives iOS the drag the spec describes, and gives VoiceOver its own reorder actions with
   it. Compose has no drag-to-reorder; hand-rolling one is a long-press gesture, an
   auto-scroll and a set of semantics actions that a screen reader would still need spelled
   out — so Android mirrors the download queue in the same app, which chose two buttons for
   the same reason. Both persist the same order. A reading list reorders the same way on
   each platform, for the same reason.
5. **Predictive back is drawn by the system on iOS and, inside the app, by the app on
   Android.** iOS pushes onto a `NavigationStack`, which animates the interactive pop
   itself. Android's in-app navigation is the app's own state, so the app has to draw the
   preview of a screen leaving. **The divergence in mechanism stands; the gap in behaviour
   is closed.** `PredictiveBackHost` had no call sites at all until 2026-08-31. It wraps
   `SettingsScreen`, and it now also wraps the shell's navigation area, where the app's one
   consolidated back rule lives — that rule was a plain `BackHandler` and is a
   `PredictiveBack` handler. Two hosts and not one: the composition local is static, so
   Settings' inner host owns the gesture while a reader is inside its three levels and the
   outer one owns every other in-app destination, each applying its transform once. Around
   the navigation area rather than the whole shell, because back never takes the navigation
   control away — the screen leaves and the bar or rail stays. **The wiring has not been
   watched on a device**: the transform is unit-tested as a value (`BackPreview`,
   `backPreview()`), the composition that applies it is reachable by no test in this
   repository, and what a reader sees is a frame rather than a value.
6. **The reader's selection menu diverges on purpose.** iOS refuses the system edit menu
   and anchors a popover of five swatches to the words; Android puts named colours into
   the `ActionMode` the platform already raises, which is where an Android reader looks
   and is also what a screen reader can read out. Both offer the four things the spec names.
7. **The cleartext exception is wider on Android.** A self-hosted catalogue usually answers
   over plain HTTP on a `.local` name or a private address. iOS says exactly that with
   `NSAllowsLocalNetworking`; Android's network security config can name a host or an IP
   literal but not a range, so the exception there covers every host. Both platforms
   complete a typed address with no scheme to `https`, so reaching a server over cleartext
   takes a reader typing `http://`.
8. **Device-driven verification is Android's.** `pnpm smoke:android` walks every route on
   a connected device and asks logcat whether the process died; `pnpm a11y:android` reads
   the real accessibility tree at real density; `pnpm pseudo:android` walks nine routes
   under `en-XA`. iOS has one simulator-bound UI test, scoped to the library and currently
   `XCTExpectFailure`d. This one is not deliberate — it is where the tooling happened to
   get built.

## What the checks cover

| Check | Covers |
| --- | --- |
| `pnpm lint` | specs *and changes*, tokens, generated notices, fixtures, the libarchive pin, the iOS lockfile, the corpus, and `strings:ios` |
| `pnpm check` | `pnpm lint`, then both apps' lint and unit tests |
| `pnpm smoke:android` | sixteen routes open without crashing — the three destinations, the publication page, both readers and nine settings screens. Needs a device |
| `pnpm a11y:android` | unnamed controls, raw values as names, targets under 48dp. Needs a device |
| `pnpm strings:ios` | every key a Swift source asks for exists, and is translated in all four languages. Needs no device |
| `pnpm pseudo:android` | nine routes under `en-XA`: text that leaves its row, controls off the display, targets under 48dp. Needs a device |
| `AccessibilityAuditTests` | Apple's own audit, on the library only, and never at a forced content size |
| `pnpm corpus:check` | the generated test library is well-formed in every format |
| `pnpm opds` | not a check: a real OPDS server for the walkthrough a unit test cannot do |
| `pnpm kavita` | not a check either: a mock Kavita, because nobody here has a real one |
| `pnpm smb` | not a check: a real Samba server, including `--encrypted` |

**CI is narrower than that table looks.** `.github/workflows/contract.yml` runs
`spec:validate`, `tokens:check`, `tokens:verify`, `notices:check`, `libarchive:pin`,
`lockfile:ios`, `fixtures:check` and two self-tests — it does **not** run `pnpm lint`, so
`strings:ios` and `corpus:check` are local-only gates today. The iOS and Android workflows
run each platform's own lint, unit tests and build. Nothing device-driven runs in CI, which
is why `smoke:android`, `a11y:android` and `pseudo:android` are walks somebody remembers
rather than gates.

Nothing anywhere compares a screenshot against a reference, which `native-experience` asks
for by name. There is no device matrix written down, no baseline directory and no image-diff
script; the thirty-five committed captures are ad hoc, and thirty-two of them are Android
settings screens. The pseudo-locale walk and the accessibility sweep both stop at the two
readers, because a reader needs a publication on the device and the emulator has none.
